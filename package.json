{
  "name": "typescriptc",
  "version": "0.0.1",
  "description": "Transpile TypeScript to C and compile to a native binary",
  "type": "module",
  "bin": {
    "tsc2c": "./bin/tsc2c"
  },
  "scripts": {
    "build": "tsc -p .",
    "test": "bun tests/e2e/run.ts",
    "cli": "bun src/cli.ts"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "typescript": "^5.6.3"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^22.7.5"
  },
  "engines": {
    "node": ">=18"
  }
}
