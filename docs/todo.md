# TODO — remaining work

Scope of the full project (from the approved plan): **full ECMAScript 2026 + transpile arbitrary `node_modules`**.

Items are grouped by how soon they unblock the most user value. Within each group the ordering reflects dependency order and implementation leverage. Completed work belongs in [`done.md`](done.md), not in this TODO file.

---

## 1. Next-up unblockers

- `Promise.withResolvers<T>()` is now covered as a bounded typed resolver-record surface: boxed records preserve the `Promise<T>` type of `.promise` while resolver functions retain runtime thenable adoption; richer resolver-record operations remain deferred. Test: `promise_with_resolvers`.

- The dedicated long-chain fixture now verifies fourteen sequential awaited locals across declarations, class methods, async function values, and branch fallthroughs; the broader multi-suspension state-machine and nested suspension graphs remain deferred. Test: `async_await_eight_step_chain`.

This is the next item that most directly expands what programs can be written against `tsc2c`.

- **Phase 6 — `async`/`await` + libuv event loop**
  - Remaining work: general multi-suspension `await` state-machine lowering beyond the current bounded direct tail-await, awaited-alias, assignment-awaited-alias, single-await, embedded return-await, local initializer-return, assignment-return, assigned-prelude awaited-local return, source `try`/`catch`/`finally`, branch-block awaited-local return including branch-local initialized and assigned preludes plus synchronous post-await locals/control-flow and terminal post-await `throw`, branch-block source `try` / `catch` / `finally` awaited returns, and branch-block leading awaited-local chains, and leading awaited-local continuation subsets; the direct leading awaited-local chain subset is now verified through fourteen awaited locals and await-free expressions plus initialized or assigned locals carried between multiple suspension points, while broader `await` inside nested control-flow constructs and loops remains beyond the covered single-await post-await synchronous statement subsets for methods/declarations/function values, branch-block awaited-local returns with branch-local initialized/assigned preludes and post-await locals/`if`/`switch`/loop return/break/continue plus post-await `try`/`catch`/`finally` before final return and terminal post-await `throw` beyond the covered branch-arm and fallthrough-tail post-`await` if/try/switch/loop-return/loop-control/terminal-throw subsets, source `try`/`catch`/`finally` awaited returns beyond the covered direct `return await` source/try/catch/finally prelude-routing subset, combined catch-plus-finally routing/prelude/finally-throw override subset, branch-block source-try branch subset, and branch-block source-try fallthrough-tail subset, direct `return await` beyond the covered branch-local and fallthrough-tail local/expression-statement prelude subsets, branch-block fallthrough-tail embedded-await return expressions beyond the covered single-embedded-await, template embedded-await, conditional mixed sync/await, and short-circuit RHS-/left-operand-await subsets, and branch-block leading awaited-local chains beyond the covered void-sequencing, six-await fallthrough-tail, and fourteen-await branch/fallthrough subsets; broader awaited expressions outside the current leading local declaration, expression-statement, `return await`, and source `try` recognizer edge cases beyond the covered transparent fulfilled/catch return, catch throw, catch-prelude wrapper, and terminal post-await throw subset; broader locals that must survive across general suspension points beyond the current primitive, dynamic, typed Promise, typed array/object/class/function, typed collection, typed RegExp, typed Node crypto/events/fs object, Event/EventTarget, Date/Error/AggregateError, URL/URLSearchParams, Buffer/ArrayBuffer/DataView, TextEncoder/TextDecoder, and symbol prelude captures; broader thenable assimilation edge cases outside the current immediate and microtask-delayed settled/nested/self-resolution/cycle coverage and dynamic `Promise.all`/`race`/`any`/`allSettled` iterable input shapes beyond covered delayed nested-array, delayed nested-Set, Map entry-iteration, string iterable, generator-backed iterable, custom iterable class including inherited `[Symbol.iterator]`, custom iterator-object class, and direct self-iterable iterator object cases; full async function edge cases beyond the current declaration, instance/static class method, object-literal method, named/lifted function-value, closure function-value, block-bodied async-arrow function-value, and expression-bodied async-arrow continuation routing; and future async I/O on top of libuv.

  - Verified bounded leading-chain extension: await-free `if` statements with assigned-before-use branch-local `let` or `var` declarations are now accepted between suspension points across declarations, methods, function values, and branch paths; nested awaits, destructuring, and broader branch state remain deferred. Test: `async_await_eight_step_chain`.
  - Verified bounded leading-chain extension: top-level await-free interstitial statements now accept initialized or assigned-before-use uninitialized `var` declarations, including ordered multi-declaration lists before `switch`, loop, `try`, and `if` preludes, across declarations, methods, function values, and branch paths; destructuring and broader mutable state remain deferred. Test: `async_await_eight_step_chain`.
  - Verified bounded leading-chain extension: await-free `switch` statements with simple local assignments are now accepted between suspension points across declarations, methods, function values, and branch paths; nested awaits and broader control-flow state remain deferred. Test: `async_await_eight_step_chain`.
  - Verified bounded leading-chain extension: await-free `while` statements with a simple local assignment are now accepted between suspension points across declarations, methods, function values, and branch paths; nested awaits and broader loop state remain deferred. Test: `async_await_eight_step_chain`.
  - Verified bounded leading-chain extension: await-free `do…while` statements with a simple local assignment are now accepted between suspension points across declarations, methods, function values, and branch paths; nested awaits and broader loop state remain deferred. Test: `async_await_eight_step_chain`.
  - Verified bounded leading-chain extension: await-free ordinary `for` statements with a simple loop-local assignment are now accepted between suspension points across declarations, methods, function values, and branch paths; nested awaits and broader loop state remain deferred. Test: `async_await_eight_step_chain`.
  - Verified bounded leading-chain extension: await-free `for…of` statements over a one-element array with a simple local assignment are now accepted between suspension points across declarations, methods, function values, and branch paths; nested awaits and broader iterator state remain deferred. Test: `async_await_eight_step_chain`.
  - Verified bounded leading-chain extension: await-free `for…in` statements over a one-property object with a simple local assignment are now accepted between suspension points across declarations, methods, function values, and branch paths; nested awaits and broader iterator state remain deferred. Test: `async_await_eight_step_chain`.
  - Verified bounded leading-chain extension: await-free `try`/`finally` statements with simple local mutation are now accepted between suspension points across declarations, methods, function values, and branch paths; nested awaits and broader exception state remain deferred. Test: `async_await_eight_step_chain`.
  - Verified bounded leading-chain extension: await-free `try`/`catch` statements with a bounded conditional throw and simple local mutation are now accepted between suspension points across declarations, methods, function values, and branch paths; nested awaits and broader exception state remain deferred. Test: `async_await_eight_step_chain`.
  - Verified bounded leading-chain extension: await-free `try`/`catch`/`finally` statements with a bounded conditional throw, catch mutation, and finalization are now accepted between suspension points across declarations, methods, function values, and branch paths; nested awaits and broader exception state remain deferred. Test: `async_await_eight_step_chain`.
  - Verified bounded post-await nested-`if` join lowering: after a direct awaited local, both arms may sequence unequal numbers of direct awaits, including nested awaited `if`/`else` steps with single or equal-length multiple direct awaits per arm, await-free expression/control-flow preludes before nested suspension with supported initialized or assigned-before-use escaping `var` captures, an implicit await-free false arm for a nested `if` without `else`, await-free explicit `else` postludes beside a suspending arm, and bounded unequal nested sequences when shorter arms contribute only await-only expression stages, assigned awaited locals carried through await-free arm postludes, branch-local awaited declarations, and await-free nested `if`/`switch`/counted-`while`/`for`/`do…while`/`for…of`/`for…in`/`try`/`finally` control flow with escaping `var` captures before later branch awaits, before shared `return await` and `throw await` terminals; declaration, instance-method, and lifted async function-value paths preserve branch-specific values and rejection reasons, while broader nested suspension graphs remain deferred. Test: `async_await_branch_post_await_nested_suspend`.
  - Verified bounded loop-body continuation extension: a direct awaited expression now preserves supported await-free postlude statements, including simple locals, before `continue`, resumes through the counted-`for` incrementor, and re-enters an awaited loop condition; broader awaited loop-body control flow remains deferred. Test: `async_await_loop_body_continue`.
  - Verified bounded multi-await loop-body continuation: multiple direct awaited expression statements now sequence before `continue`, with rejection propagation and resumed loop incrementor/condition re-entry; awaited locals and broader body suspension graphs remain deferred. Test: `async_await_loop_body_multi_continue`.
  - Verified bounded multi-await loop-body control preludes: await-free prefix/suffix statements may surround multiple direct awaited expression statements before `break` or `continue`, preserving counted-`for` incrementor and fallthrough routing. Tests: `async_await_loop_body_multi_break`, `async_await_loop_body_multi_continue`.
  - Verified the direct awaited loop-body `continue` subset for an awaited `while` condition with one supported await-free pre-loop local declaration; broader pre-loop declaration graphs and while-body suspension graphs remain deferred. Test: `async_await_while_body_continue`.
  - Verified bounded awaited `do…while` body continuation: multiple direct awaited body steps may now be surrounded by await-free prefix/suffix statements before `continue`, with re-entry and repeated body suspension; broader `do…while` body/control-flow graphs remain deferred. Test: `async_await_do_body_continue`.
  - Verified bounded awaited `do…while` body `if` continuation: an await-free initial-iteration branch now selects the awaited body path or alternate condition re-entry before subsequent awaited-condition iterations; broader `do…while` branch graphs remain deferred. Test: `async_await_do_body_if_continue`.
  - Verified bounded awaited `do…while` body awaited-`if` continuation: the initial nested awaited condition now routes through the conditional body adapter before subsequent awaited-condition re-entry; broader `do…while` branch graphs remain deferred. Test: `async_await_do_body_await_if_continue`.
  - Verified bounded awaited `do…while` body awaited-`if/else` continuation: both initial nested-condition branches may now carry direct awaited body work before subsequent awaited-condition re-entry; broader `do…while` branch graphs remain deferred. Test: `async_await_do_body_await_if_else_continue`.
  - Verified bounded awaited `do…while` body awaited-`if` break continuation: the alternate nested-condition branch may now break through post-loop fallthrough while the selected branch continues after awaited body work; broader `do…while` branch graphs remain deferred. Test: `async_await_do_body_await_if_break`.
  - Verified bounded awaited `do…while` body awaited-`if` return continuation: the alternate nested-condition branch may now route direct awaited prelude steps into `return await` while the selected branch continues; broader `do…while` branch graphs remain deferred. Test: `async_await_do_body_await_if_return`.
  - Verified bounded awaited `do…while` body awaited-`if` throw continuation: the alternate nested-condition branch may now route direct awaited prelude steps into `throw await` while the selected branch continues; broader `do…while` branch graphs remain deferred. Test: `async_await_do_body_await_if_throw`.
  - Verified bounded symmetric awaited `do…while` body return/continue continuation: the initial alternate nested-condition branch may continue after awaited body work and the re-entered selected branch may resolve through `return await`; broader `do…while` branch graphs remain deferred. Test: `async_await_do_body_await_return_continue`.
  - Verified bounded symmetric awaited `do…while` body throw/continue continuation: the initial alternate nested-condition branch may continue after awaited body work and the re-entered selected branch may reject through `throw await`; broader `do…while` branch graphs remain deferred. Test: `async_await_do_body_await_throw_continue`.
  - Verified bounded symmetric awaited `do…while` body break/continue continuation: the initial alternate branch may continue after awaited body work and the re-entered selected branch may break after awaited body work; broader `do…while` branch graphs remain deferred. Test: `async_await_do_body_await_if_break_continue`.
  - Verified bounded multi-await awaited `do…while` body `if/else` continuation: both initial nested-condition branches may sequence multiple direct awaited body steps with await-free prefix/suffix statements before condition re-entry; broader `do…while` branch graphs remain deferred. Test: `async_await_do_body_await_if_multi_continue`.
  - Verified bounded awaited `do…while` body awaited-`if` synchronous return continuation: the initial nested-condition terminal branch may route awaited prelude steps into a synchronous `return` while the other branch continues after awaited body work; broader `do…while` branch graphs remain deferred. Test: `async_await_do_body_await_if_sync_return`.
  - Verified bounded awaited `do…while` body awaited-`if` synchronous throw continuation: the initial nested-condition terminal branch may route awaited prelude steps into a synchronous `throw` while the other branch continues after awaited body work; broader `do…while` branch graphs remain deferred. Test: `async_await_do_body_await_if_sync_throw`.
  - Verified bounded awaited `do…while` multi-await condition continuation: two sequential direct awaited condition operands now resume after the initial awaited body continuation with short-circuiting and repeated body re-entry; broader condition/loop graphs remain deferred. Test: `async_await_do_body_multi_condition_continue`.
  - Verified bounded awaited `do…while` flat multi-await condition continuation: three or more sequential direct awaited operands now resume after the initial awaited body continuation with flat logical short-circuiting and repeated body re-entry; broader condition/loop graphs remain deferred. Test: `async_await_do_body_three_condition_continue`.
  - Verified bounded awaited `do…while` conditional condition continuation: a three-await `?:` condition now resumes after the initial awaited body continuation, preserving branch selection, repeated body re-entry, and captured loop-local state; broader conditional/loop graphs remain deferred. Test: `async_await_do_body_conditional_condition_continue`.
  - Verified bounded awaited `do…while` switch-prelude continuation: an await-free `switch` now executes on the initial iteration and repeated iterations before the awaited body and condition re-entry; broader body control-flow graphs remain deferred. Test: `async_await_do_body_switch_continue`.
  - Verified bounded awaited `do…while` try/finally-prelude continuation: an await-free `try`/`finally` now executes on the initial iteration and repeated iterations before the awaited body and condition re-entry; broader body exception graphs remain deferred. Test: `async_await_do_body_try_finally_continue`.
  - Verified bounded awaited `do…while` body try/finally suspension: a single direct awaited expression inside a `try` now resumes through a synchronous `finally` before outer `continue` condition re-entry, carrying captured loop-local state; broader nested suspension and exception graphs remain deferred. Test: `async_await_do_body_try_finally_await_continue`.
  - Verified the same bounded awaited body try/finally suspension in a regular `while` loop, preserving loop-local state through synchronous finalization and condition re-entry; broader nested suspension and exception graphs remain deferred. Test: `async_await_while_body_try_finally_await_continue`.
  - Verified bounded awaited `do…while` try/finally multi-suspension: one direct awaited body expression may be followed by one direct awaited expression at the start of `finally`, then synchronous cleanup, before outer `continue` condition re-entry; body rejection still runs cleanup and is preserved when cleanup fulfills. Broader nested suspension graphs remain deferred. Tests: `async_await_do_body_try_finally_await_cleanup_continue`, `async_await_do_body_try_finally_await_cleanup_reject`.
  - Verified bounded awaited `do…while` try/catch body suspension: one direct awaited expression may reject into a synchronous catch, preserve await-free post-catch statements, and re-enter the awaited loop condition; broader catch/finally suspension graphs remain deferred. Test: `async_await_do_body_try_await_catch_continue`.
  - Verified bounded awaited `do…while` try/catch/finally body suspension: one direct awaited expression now routes both fulfillment and rejection through synchronous catch/finally cleanup before postlude and awaited condition re-entry; broader nested catch/finally suspension graphs remain deferred. Test: `async_await_do_body_try_await_catch_finally_continue`.
  - Verified bounded awaited `do…while` awaited catch recovery: a direct body await rejection may enter one direct awaited expression in a variable-free catch, then preserve loop postlude and awaited condition re-entry; broader catch suspension graphs remain deferred. Test: `async_await_do_body_try_await_catch_await_continue`.
  - Verified bounded awaited `do…while` awaited catch recovery with a named catch binding: the original rejection reason is available to the direct awaited recovery expression before loop postlude and condition re-entry; destructuring and broader catch suspension graphs remain deferred. Test: `async_await_do_body_try_await_named_catch_await_continue`.
  - Verified bounded awaited `do…while` synchronous catch postlude with a named catch binding, preserving the original rejection reason before loop postlude and condition re-entry; destructuring and broader catch suspension graphs remain deferred. Test: `async_await_do_body_try_await_named_catch_continue`.
  - Verified bounded awaited `do…while` awaited catch recovery with synchronous `finally`: cleanup runs after both successful and rejected recovery awaits, with recovery rejection preserved; broader catch/finally suspension graphs remain deferred. Tests: `async_await_do_body_try_await_catch_await_finally_continue`, `async_await_do_body_try_await_catch_await_finally_reject`.
  - Verified bounded awaited `do…while` two-stage catch/finally suspension: the body await and catch recovery each precede one awaited `finally` cleanup, with cleanup still running on recovery rejection and rejection precedence preserved; broader nested catch/finally suspension graphs remain deferred. Tests: `async_await_do_body_try_await_catch_await_finally_await_continue`, `async_await_do_body_try_await_catch_await_finally_await_reject`.
  - Verified the same two-stage awaited catch/finally suspension in a regular `while` body, preserving condition re-entry and loop-local state; broader nested catch/finally suspension graphs remain deferred. Test: `async_await_while_body_try_await_catch_await_finally_await_continue`.
  - Verified the two-stage awaited catch/finally suspension also routes a terminal `break` through awaited post-loop fallthrough, preserving cleanup counts; broader nested catch/finally break graphs remain deferred. Test: `async_await_do_body_try_await_catch_await_finally_await_break`.
  - Verified the two-stage awaited catch/finally suspension also settles a terminal `return await` after cleanup, preserving awaited result values; broader nested catch/finally return graphs remain deferred. Test: `async_await_do_body_try_await_catch_await_finally_await_return`.
  - Verified the same two-stage awaited catch/finally suspension in a counted `for` body, preserving the await-free incrementor across `continue` and awaited condition re-entry; broader nested catch/finally suspension graphs remain deferred. Test: `async_await_for_body_try_await_catch_await_finally_await_continue`.
  - Verified bounded awaited `do…while` caught try/catch/finally-prelude continuation: an await-free caught throw and finalization now execute on the initial and repeated iterations before the awaited body and condition re-entry; broader body exception graphs remain deferred. Test: `async_await_do_body_try_catch_finally_continue`.
  - Verified bounded awaited `do…while` finally-throw prelude: an await-free throwing `finally` now rejects before awaited body continuation and does not emit unreachable body/fallthrough capture code; broader exception graphs remain deferred. Test: `async_await_do_body_try_finally_throw_continue`.
  - Verified bounded awaited `do…while` late finally-throw re-entry: a later iteration may reject from an await-free `finally` prelude after the initial body await and condition re-entry; broader exception graphs remain deferred. Test: `async_await_do_body_late_finally_throw_continue`.
  - Verified bounded awaited `do…while` catch-throw prelude: an await-free `catch` throw still runs `finally` and rejects before awaited body continuation; broader exception graphs remain deferred. Test: `async_await_do_body_catch_throw_continue`.
  - Verified bounded awaited `do…while` for-of-prelude continuation: a simple await-free `for…of` iterator declaration now executes on the initial iteration and repeated iterations before the awaited body and condition re-entry; broader iterable loop state remains deferred. Test: `async_await_do_body_for_of_continue`.
  - Verified bounded awaited `do…while` for-in-prelude continuation: a simple await-free `for…in` iterator declaration now executes on the initial iteration and repeated iterations before the awaited body and condition re-entry; broader iterable loop state remains deferred. Test: `async_await_do_body_for_in_continue`.
  - Verified bounded awaited `do…while` ordinary-for-prelude continuation: a simple await-free ordinary `for` initializer, condition, and incrementor now execute on the initial and repeated iterations before the awaited body and condition re-entry; broader loop state remains deferred. Test: `async_await_do_body_for_continue`.
  - Verified `for…of` body continuation with the two-stage awaited body/catch/finally chain, preserving iterator progression across `continue`; broader iterator suspension graphs remain deferred. Test: `async_await_for_of_body_try_await_catch_await_finally_continue`.
  - Verified `for…in` body continuation with the two-stage awaited body/catch/finally chain, preserving iterator progression across `continue`; broader iterator suspension graphs remain deferred. Test: `async_await_for_in_body_try_await_catch_await_finally_continue`.
  - Verified direct awaited `for…in` body continuation with delayed body promises, preserving iterator progression across `continue`; broader iterator suspension graphs remain deferred. Test: `async_await_for_in_body_await_continue`.
  - Verified direct awaited `for…of` body continuation with delayed body promises, preserving iterator progression across `continue`; broader iterator suspension graphs remain deferred. Test: `async_await_for_of_body_await_continue`.
  - Verified delayed direct awaited `for…in` body break, preserving a separately delayed post-loop fallthrough await after the first iterator item; broader iterator suspension graphs remain deferred. Test: `async_await_for_in_body_await_break`.
  - Verified delayed direct awaited `for…in` body return, settling a separately delayed terminal await after the first iterator item; broader iterator suspension graphs remain deferred. Test: `async_await_for_in_body_await_return`.
  - Verified delayed direct awaited `for…of` body break and return, preserving separately delayed post-loop and terminal awaits; broader iterator suspension graphs remain deferred. Tests: `async_await_for_of_body_await_break`, `async_await_for_of_body_await_return`.
  - Verified direct awaited `for…of` body break and return, preserving post-loop fallthrough and awaited result settlement; broader iterator suspension graphs remain deferred. Tests: `async_await_for_of_body_await_break`, `async_await_for_of_body_await_return`.
  - Verified delayed direct awaited `for…in` and `for…of` body throws after suspended body work, preserving awaited rejection settlement; broader iterator suspension graphs remain deferred. Tests: `async_await_for_in_body_await_throw`, `async_await_for_of_body_await_throw`.
  - Verified the two-stage awaited body/catch/finally chain through terminal `break` in both `for…in` and `for…of` bodies, preserving cleanup and post-loop fallthrough; broader iterator suspension graphs remain deferred. Tests: `async_await_for_in_body_try_await_catch_await_finally_await_break`, `async_await_for_of_body_try_await_catch_await_finally_await_break`.
  - Verified the same two-stage awaited body/catch/finally chain through terminal `return await` in both `for…in` and `for…of` bodies, preserving cleanup and returned counts; broader iterator suspension graphs remain deferred. Tests: `async_await_for_in_body_try_await_catch_await_finally_await_return`, `async_await_for_of_body_try_await_catch_await_finally_await_return`.
  - Verified cleanup rejection precedence in the two-stage awaited iterator body/catch/finally chain for both `for…in` and `for…of`, preserving cleanup counts and rejecting with the cleanup reason; broader iterator suspension graphs remain deferred. Tests: `async_await_for_in_body_try_await_catch_await_finally_await_reject`, `async_await_for_of_body_try_await_catch_await_finally_await_reject`.
  - Verified named catch bindings in awaited `for…in` and `for…of` body catch/finally chains, preserving the original rejection through awaited recovery and iterator progression; destructuring and broader iterator suspension graphs remain deferred. Tests: `async_await_for_in_body_try_await_named_catch_await_finally_continue`, `async_await_for_of_body_try_await_named_catch_await_finally_continue`.
  - Verified synchronous named catch postludes plus awaited `finally` cleanup in `for…in` and `for…of` bodies, preserving the original rejection and iterator progression; destructuring and broader iterator suspension graphs remain deferred. Tests: `async_await_for_in_body_try_await_named_catch_finally_continue`, `async_await_for_of_body_try_await_named_catch_finally_continue`.
  - Verified awaited `try/finally` iterator bodies for both `for…in` and `for…of`, preserving finalizer execution and original body rejection; broader iterator suspension graphs remain deferred. Tests: `async_await_for_in_body_try_await_finally_await_continue`, `async_await_for_of_body_try_await_finally_await_continue`.
  - Verified delayed successful progression across two `for…in` and `for…of` iterator items with awaited body/finalizer resumes and awaited fallthrough; broader iterator suspension graphs remain deferred. Tests: `async_await_for_in_body_try_await_finally_await_success`, `async_await_for_of_body_try_await_finally_await_success`.
  - Verified bounded awaited `do…while` nested-loop-control prelude: an await-free nested ordinary loop may route its own `continue` and `break` before the awaited body and outer condition re-entry; broader nested loop state remains deferred. Test: `async_await_do_body_nested_loop_control_continue`.
  - Verified bounded awaited `do…while` nested iterator-loop-control prelude: await-free nested `for…of` and `for…in` loops may route their own `continue` and `break` before the awaited body and outer condition re-entry; broader iterator loop state remains deferred. Test: `async_await_do_body_nested_iter_loop_control_continue`.
  - Verified bounded awaited `do…while` nested while/do-loop-control prelude: await-free nested `while` and `do…while` loops may route their own `continue` and `break` before the awaited body and outer condition re-entry; broader nested loop state remains deferred. Test: `async_await_do_body_nested_while_do_loop_control_continue`.
  - Verified bounded same-iteration awaited `do…while` nested-`if` terminal split: `return await` and `throw await` arms now route both branch outcomes from the initial body condition; broader `do…while` terminal graphs remain deferred. Test: `async_await_do_body_await_if_return_throw`.
  - Verified bounded same-iteration awaited `do…while` nested-`if` both-return split: both condition branches may preserve multiple awaited prelude steps before their awaited returns, with both outcomes covered; broader `do…while` terminal graphs remain deferred. Test: `async_await_do_body_await_if_both_return`.
  - Verified bounded same-iteration awaited `do…while` nested-`if` both-throw split: both condition branches may preserve multiple awaited prelude steps before `throw await`, with both rejection outcomes covered; broader `do…while` terminal graphs remain deferred. Test: `async_await_do_body_await_if_both_throw`.
  - Verified bounded same-iteration awaited `do…while` nested-`if` both-synchronous split: both condition branches may preserve multiple awaited prelude steps before synchronous `return`/`throw` terminals, with both outcomes covered; broader `do…while` terminal graphs remain deferred. Test: `async_await_do_body_await_if_both_sync`.
  - Verified bounded awaited `do…while` body break: await-free prefix/suffix statements may surround multiple direct awaited body steps before `break` and post-loop fallthrough. Test: `async_await_do_body_break`.
  - Verified bounded awaited loop-body `if` continuation: one await-free condition selects an arm containing direct awaited body steps before `continue`, while the false arm re-enters the awaited loop condition; nested awaited branches and broader body-control graphs remain deferred. Test: `async_await_loop_body_if_continue`.
  - Verified bounded awaited loop-body `if/else` continuation: the awaited `continue` arm now coexists with an await-free `else` postlude before condition re-entry; awaited else arms and broader branch suspension graphs remain deferred. Test: `async_await_loop_body_if_else_continue`.
  - Verified bounded awaited loop-body `if/else` multi-suspension: both arms may now sequence direct awaited expression steps before `continue` with branch-specific rejection routing; nested branches and broader suspension graphs remain deferred. Test: `async_await_loop_body_if_else_await_continue`.
  - Verified bounded awaited loop-body `if` loop-control split: the awaited `continue` arm now coexists with an await-free `else { break; }` exit through awaited fallthrough; awaited break arms and broader branch control remain deferred. Test: `async_await_loop_body_if_break_continue`.
  - Verified bounded nested awaited loop-body condition: a direct awaited boolean now selects the bounded awaited/await-free `continue` arms before re-entering the awaited loop condition; broader nested awaited branch graphs remain deferred. Test: `async_await_loop_body_await_if_continue`.
  - Verified bounded nested awaited loop-body `if/else`: a direct awaited boolean now selects direct awaited body continuations in both branches before `continue`; broader nested awaited branch graphs remain deferred. Test: `async_await_loop_body_await_if_else_continue`.
  - Verified bounded nested awaited loop-body loop-control split: a direct awaited boolean now routes direct awaited body expressions in a nested `else { break; }` arm through the awaited post-loop fallthrough after the true arm's awaited `continue`; broader nested break graphs remain deferred. Test: `async_await_loop_body_await_if_break`.
  - Verified bounded nested awaited loop-body return split: a direct awaited boolean now routes a direct nested `else { return await ...; }` arm after the true arm's awaited `continue`; broader nested return/control-flow graphs remain deferred. Test: `async_await_loop_body_await_if_return`.
  - Verified bounded nested awaited loop-body throw split: a direct awaited boolean now routes a direct nested `else { throw await ...; }` arm with rejection propagation after the true arm's awaited `continue`; broader nested throw/control-flow graphs remain deferred. Test: `async_await_loop_body_await_if_throw`.
  - Verified symmetric bounded nested awaited loop-body loop-control split: direct awaited body expressions now support a true-arm `break` and false-arm `continue`, routing the break through awaited post-loop fallthrough. Test: `async_await_loop_body_await_if_break_continue`.
  - Verified symmetric bounded nested awaited loop-body terminal split: direct awaited `return` and `throw` in the true arm now coexist with direct awaited body continuation in the false arm. Tests: `async_await_loop_body_await_return_continue`, `async_await_loop_body_await_throw_continue`.
  - Verified bounded nested awaited loop-body synchronous terminal split: synchronous `return` and `throw` alternate arms now resolve or reject the surrounding async result after the true arm's awaited `continue`. Tests: `async_await_loop_body_await_if_sync_return`, `async_await_loop_body_await_if_sync_throw`.
  - Verified bounded nested awaited loop-body no-reentry terminal split: await-free expression and non-abrupt control-flow preludes followed by synchronous or direct awaited return/throw combinations in both nested `if` branches preserve captured loop-initializer state and settle without loop re-entry. Tests: `async_await_loop_body_await_if_both_return`, `async_await_loop_body_await_if_return_throw`, `async_await_loop_body_await_if_both_sync`.
  - Verified bounded nested awaited loop-body terminal preludes: each nested terminal branch may preserve await-free prefix and suffix statements around a sequence of direct awaited expression statements before synchronous or awaited return/throw settlement without loop re-entry. Tests: `async_await_loop_body_await_if_both_return`, `async_await_loop_body_await_if_terminal_prelude`, `async_await_loop_body_await_if_sync_terminal_prelude`, `async_await_loop_body_await_if_both_sync`.
  - Verified asymmetric nested awaited loop-body terminal preludes: the alternate synchronous return/throw branch may preserve await-free prefix and suffix statements around direct awaited expressions while the other arm continues, retaining resolution and rejection routing. Tests: `async_await_loop_body_await_if_sync_return`, `async_await_loop_body_await_if_sync_throw`.
  - Verified asymmetric nested awaited loop-body awaited terminals: the alternate awaited return/throw branch may preserve await-free prefix and suffix statements around direct awaited expressions while the other arm continues. Tests: `async_await_loop_body_await_if_return`, `async_await_loop_body_await_if_throw`.
  - Verified asymmetric nested awaited loop-body break branches: the break arm may preserve await-free prefix/suffix statements around direct awaited expressions before the awaited post-loop fallthrough, including the symmetric break/continue shape. Tests: `async_await_loop_body_await_if_break`, `async_await_loop_body_await_if_break_continue`.
  - Verified nested awaited loop-body continue branches: selected `if` and both `if/else` arms may preserve await-free prefix/suffix statements around direct awaited expressions before awaited condition re-entry. Tests: `async_await_loop_body_await_if_continue`, `async_await_loop_body_await_if_else_continue`.
  - Verified non-nested awaited loop-body `if/else` continuations: both arms may preserve await-free prefix/suffix statements around direct awaited expressions before awaited condition re-entry. Test: `async_await_loop_body_if_else_await_continue`.
  - Verified non-nested awaited loop-body `continue` arms: direct awaited work may retain await-free prefix/suffix statements before condition re-entry, including the continue side of the `break`/`continue` split. Tests: `async_await_loop_body_if_continue`, `async_await_loop_body_if_break_continue`.
  - Verified non-nested awaited loop-body `break` arms: direct awaited work may retain await-free prefix/suffix statements before awaited post-loop fallthrough in the `break`/`continue` split. Test: `async_await_loop_body_if_break_continue`.
  - Bounded awaited loop conditions now resolve synchronous direct `return` bodies before the false fallthrough in `while` and initializer-free `for` forms across async declarations, class methods, and function values. Test: `async_await_while_condition`.
  - Bounded awaited loop conditions now reject synchronous direct `throw` bodies before the false fallthrough in `while` and initializer-free `for` forms across async declarations, class methods, and function values. Test: `async_await_while_condition`.
  - Bounded awaited loop conditions now preserve await-free expression and simple local declaration preludes before synchronous direct `return` bodies across async declarations, class methods, and function values. Test: `async_await_while_condition`.
  - Bounded awaited loop conditions now accept initializer-free `for` incrementors when the supported true-condition body returns immediately, preserving the equivalent false fallthrough across async declarations, class methods, and function values without executing the unreachable incrementor. Test: `async_await_while_condition`.
  - Bounded awaited loop conditions now preserve one await-free expression initializer on `for` loops before the first condition suspension, across async declarations, class methods, and function values; declaration, nested-await, and function-valued initializers remain deferred. Test: `async_await_while_condition`.
  - Bounded awaited loop conditions now preserve simple await-free `const`/`let`, initialized and uninitialized `var` declaration-list initializers on `for` loops, including carrying referenced values through the awaited condition, direct synchronous/`return await`/`throw await` continuations, multiple ordered declarations, bounded body assignment, uninitialized `let` condition reads and false-condition fallthroughs across async declarations, instance methods, static methods, object-literal methods, and async function values, nullable typed uninitialized `let` `typeof` reads, initialized `var` mutation followed by `break` and a post-loop return, multiple ordered escaping `var` mutations before `break`, await-free `if`, `try`/`catch`/`finally`, `switch`, bounded nested-`while`, and bounded nested-`do…while` preludes before escaped-`var` `break`, escaping `var` state through second post-loop `return await` and `throw await` continuations on both true and false conditions across declarations, instance methods, and async function values with receiver capture, uninitialized dynamic escaped `var` assignment and `undefined` fallthrough through both return and throw awaits, and initialized or uninitialized `var` false-condition fallthroughs; broader escaping `var` mutation graphs and unsupported declaration forms remain deferred. Test: `async_await_while_condition`.
  - Bounded awaited loop conditions now support expressionless `return` in both `while` and initializer-free `for` true-condition bodies and false fallthroughs for `Promise<void>` async declarations, class methods, and function values. Test: `async_await_while_condition`.
  - Bounded awaited loop conditions now preserve await-free expression and simple local declaration preludes before synchronous direct `throw` bodies, including initializer-free `for` conditions, across async declarations, class methods, and function values. Test: `async_await_while_condition`.
  - Bounded awaited loop conditions now preserve await-free non-abrupt `if` control-flow preludes before synchronous direct `return` bodies across async declarations, class methods, and function values. Test: `async_await_while_condition`.
  - Bounded awaited loop conditions now preserve await-free non-abrupt `try` / `finally` preludes before synchronous direct `return` bodies across async declarations, class methods, and function values. Test: `async_await_while_condition`.
  - Bounded awaited loop conditions now preserve await-free exhaustive `switch` preludes before synchronous direct `return` bodies across async declarations, class methods, and function values. Test: `async_await_while_condition`.
  - Bounded awaited loop conditions now preserve simple top-level `for…of` and `for…in` iterator declarations in await-free preludes before synchronous direct `return` bodies across async declarations, class methods, and function values. Test: `async_await_while_condition`.
  - Bounded awaited loop conditions now preserve simple top-level ordinary `for` initializer declarations in await-free preludes before synchronous direct `return` bodies across async declarations, class methods, and function values. Test: `async_await_while_condition`.
  - Bounded awaited loop conditions now preserve nested await-free loop `break` / `continue` control flow in preludes before synchronous direct `return` bodies across async declarations, class methods, and function values. Test: `async_await_while_condition`.
  - Bounded awaited loop conditions now preserve nested await-free `for…of` / `for…in` `break` / `continue` control flow in preludes before synchronous direct `return` bodies across async declarations, class methods, and function values. Test: `async_await_while_condition`.
  - Bounded awaited loop conditions now preserve await-free `if`, `try` / `finally`, and nested loop-control preludes before synchronous direct `throw` bodies across async declarations, class methods, and function values. Test: `async_await_while_condition`.
  - Bounded awaited loop conditions now preserve caught synchronous throws through await-free `try` / `catch` preludes before terminal synchronous `throw` bodies across async declarations, class methods, and function values. Test: `async_await_while_condition`.
  - Bounded awaited loop conditions now preserve await-free `try` / `catch` / `finally` preludes before terminal synchronous `throw` bodies, including finalization on the caught path, across async declarations, class methods, and function values. Test: `async_await_while_condition`.
  - Bounded awaited loop conditions now preserve synchronous throws handled by await-free `catch` preludes before synchronous direct `return` bodies across async declarations, class methods, and function values; uncaught prelude throws remain rejection paths. Test: `async_await_while_condition`.
  - Bounded awaited loop conditions now preserve synchronous rethrows from await-free `catch` preludes as rejection paths before synchronous direct `return` bodies across async declarations, class methods, and function values. Test: `async_await_while_condition`.
  - Bounded awaited loop conditions now preserve caught synchronous throws through await-free `finally` cleanup before synchronous direct `return` bodies across async declarations, class methods, and function values. Test: `async_await_while_condition`.
  - Bounded awaited loop conditions now preserve synchronous throws from await-free `finally` blocks as the final rejection before synchronous direct `return` bodies across async declarations, class methods, and function values. Test: `async_await_while_condition`.
  - Bounded awaited loop conditions now preserve synchronous throws from await-free `finally` blocks in `try`/`finally` preludes without a `catch` clause as rejection paths before synchronous direct `return` bodies across async declarations, class methods, and function values. Test: `async_await_while_condition`.
  - Direct awaited `if` conditions in bounded async declarations, methods, and function values now select the branch after the awaited boolean settles; the same subset also accepts an awaited condition nested under an await-free outer `if`, while broader nested control flow remains deferred. Test: `async_await_if_condition`.
  - Direct `return await` aliases now preserve preceding simple await-free locals declared in the same `const`/`let` statement; declarations after the awaited alias, nested awaits, and broader declaration state remain deferred. Test: `async_await_while_condition`.
  - Direct assignment-awaited aliases now preserve preceding simple await-free locals in the same `let` statement before the awaited target; declarations after the target, nested awaits, and broader assignment state remain deferred. Test: `async_await_while_condition`.
  - Direct `throw await` aliases now preserve preceding simple await-free locals declared in the same `const`/`let` statement; declarations after the awaited alias, nested awaits, and broader throw state remain deferred. Test: `async_await_while_condition`.
  - Leading async/await chains now split multiple simple all-awaited declarators in one `const`/`let` statement into sequential suspension steps, preserving source order and local value flow; mixed initializers, destructuring, and broader declaration graphs remain deferred. Test: `async_await_while_condition`.
  - Multiple all-awaited declarator coverage now includes async class methods and arrow function values; broader suspension graphs remain deferred. Test: `async_await_while_condition`.
  - Branch-block async/await analysis now applies the same sequential split to multiple simple all-awaited declarators inside a branch arm, preserving branch-local value flow; mixed initializers, destructuring, and broader branch graphs remain deferred. Test: `async_await_if_condition`.
  - Source `try` / `catch` leading async/await routing now preserves multiple simple all-awaited declarators in the try block before catch recovery; mixed initializers, destructuring, and broader exception graphs remain deferred. Test: `async_await_if_condition`.
  - Source `try` / `finally` and combined `try` / `catch` / `finally` leading routing now preserve multiple simple all-awaited declarators before finalization and catch recovery; mixed initializers, destructuring, and broader exception graphs remain deferred. Test: `async_await_if_condition`.
  - Conditional leading async/await analysis now splits multiple simple all-awaited declarators independently in each selected arm, preserving equal-length branch value flow; unequal or broader conditional suspension graphs remain deferred. Test: `async_await_if_condition`.
  - Awaited `switch` branch analysis now splits multiple simple all-awaited declarators in supported clauses, preserving clause-local value flow; mixed initializers, destructuring, and broader switch suspension graphs remain deferred. Test: `async_await_if_condition`.
  - Awaited loop assignment aliases now preserve preceding simple synchronous locals in the same `let` declaration before the uninitialized awaited target; nested awaits, target-after locals, and broader loop assignment state remain deferred. Test: `async_await_while_condition`.
  - Awaited loop initialized aliases now preserve preceding simple synchronous locals in the same `const`/`let` declaration before the awaited target; nested awaits, target-after locals, and broader loop alias state remain deferred. Test: `async_await_while_condition`.
  - Bounded awaited loop bodies now support sequential simple all-awaited declarators before direct `return` or `throw`, including false fallthrough and rejection propagation; broader nested loop suspension graphs remain deferred. Test: `async_await_while_condition`.
  - Multi-await loop-body coverage now includes the equivalent initializer-free `for` condition path; broader loop suspension graphs remain deferred. Test: `async_await_while_condition`.
  - Leading async/await chains now split supported all-awaited declaration statements after synchronous prelude locals; broader prelude control-flow and declaration graphs remain deferred. Test: `async_await_while_condition`.
  - Loop-body leading chains now preserve bounded await-free, non-abrupt control-flow preludes before the first suspension; awaited or abrupt nested control flow remains deferred. Test: `async_await_while_condition`.
  - Shared leading-chain coverage now includes await-free `if` and `try` / `finally` preludes before multiple awaited locals; broader prelude control-flow graphs remain deferred. Test: `async_await_while_condition`.
  - Leading async/await chains now preserve initialized and assigned-before-use uninitialized `var` locals before multiple awaited declarations, including bounded mutation between suspension points and terminal post-await `throw` across declarations, methods, and function values; otherwise uninitialized `var` state and broader escaping graphs remain deferred. Test: `async_await_while_condition`.
  - Bounded pending-await continuation lowering now supports `while (await promise) return ...; return ...` and the equivalent initializer-free/incrementor-free `for (; await promise;) return ...; return ...` in async function declarations, methods, and function values, including one non-nested awaited operand inside logical, conditional, or boolean-valued nullish conditions, nullable `Promise<boolean | null>` operands coalesced with `??`, plus flat arbitrary-length direct-awaited `&&` / `||` logical conditions (verified through six operands), pending condition fulfillment and false fallthrough, receiver/parameter references in the condition and return arms, multiple await-free expression statements before the loop return, multiple await-free expression statements before one initialized loop-local `const` / `let` returned immediately, bounded await-free `if` / `else` branches inside the loop body, and bounded two-stage direct body `return await` or `throw await` after one awaited operand in a direct, logical, or nullish condition, including the exact transparent nested body form `return await (await promise)`, await-free expression and non-abrupt control-flow preludes with simple branch-local declarations, ordered initialized or assigned-before-use loop-local `const` / `let` body-promise locals, body rejection/throw propagation, and false fallthrough; broader loop-local state, awaited nested control flow, and general loop bodies remain deferred. Test: `async_await_while_condition`.
  - Bounded awaited loop-condition outer-`continue` lowering now stages flat three-or-more direct-awaited `&&`, `||`, and `??` conditions before an awaited return/throw fallthrough, preserving short-circuiting, rejection routing, receiver capture, and function-value capture; mixed operators and broader nested suspension graphs remain deferred. Test: `async_await_while_condition`.
  - Bounded awaited loop conditions now route direct two-await `&&`, `||`, and `??` bodies ending in an outer `break` through awaited return/throw fallthrough, preserving await-free body preludes; broader two-await loop-control graphs remain deferred. Test: `async_await_while_condition`.
  - Multi-await outer-`break` loop conditions now accept initializer-free `for` incrementors as unreachable skipped work, preserving the awaited post-loop fallthrough; incrementor execution on `continue` and broader `for` suspension graphs remain deferred. Test: `async_await_while_condition`.
  - Multi-await outer-`break` loop conditions now preserve a synchronous expression initializer before the first condition suspension, including alongside a skipped incrementor; declaration initializers, awaited initializers, and broader captured initializer state remain deferred. Test: `async_await_while_condition`.
  - Bounded two-await outer-`break` loop conditions now carry initialized simple `for` declaration values through both condition suspension stages and body fallthrough; broader loop-initializer capture graphs remain deferred. Test: `async_await_while_condition`.
  - Bounded two-await outer-`break` loop conditions now also carry simple uninitialized `var` declaration storage through both condition suspension stages and body fallthrough; broader uninitialized declaration and mutation graphs remain deferred. Test: `async_await_while_condition`.
  - Bounded three-or-more direct-awaited logical outer-`break` loop conditions now carry initialized simple `for` declaration values through every condition stage and body fallthrough; conditional conditions and broader loop-initializer capture graphs remain deferred. Test: `async_await_while_condition`.
  - Bounded conditional three-await outer-`break` loop conditions now carry initialized simple `for` declaration values through the condition and selected branch stage before body fallthrough; broader conditional initializer graphs remain deferred. Test: `async_await_while_condition`.
  - Direct two-await outer-`continue` loop conditions now execute an await-free initializer-free `for` incrementor before condition re-entry; awaited incrementors and multi-stage `continue` incrementor graphs remain deferred. Test: `async_await_while_condition`.
  - Direct two-await outer-`continue` loop conditions now compose a synchronous expression initializer with an await-free `for` incrementor before condition re-entry; declaration initializers and broader composed loop state remain deferred. Test: `async_await_while_condition`.
  - Bounded two-await outer-`continue` loop conditions now carry initialized simple `for` declaration values through condition re-entry and an await-free incrementor; broader captured continuation state remains deferred. Test: `async_await_while_condition`.
  - Direct three-or-more awaited logical outer-`continue` loop conditions now execute an await-free initializer-free `for` incrementor before stage-0 condition re-entry; conditional and awaited incrementor graphs remain deferred. Test: `async_await_while_condition`.
  - Direct three-or-more awaited logical outer-`continue` loop conditions now compose a synchronous expression initializer with an await-free `for` incrementor before stage-0 re-entry; declaration initializers and broader composed loop state remain deferred. Test: `async_await_while_condition`.
  - Direct three-or-more awaited logical outer-`continue` loop conditions now carry initialized simple `for` declaration values through every condition stage, re-entry, and await-free incrementor; broader captured continuation state remains deferred. Test: `async_await_while_condition`.
  - Conditional three-await outer-`continue` loop conditions now execute an await-free initializer-free `for` incrementor before condition re-entry; awaited incrementors and broader conditional continuation graphs remain deferred. Test: `async_await_while_condition`.
  - Conditional three-await outer-`continue` loop conditions now compose a synchronous expression initializer with an await-free `for` incrementor before condition re-entry; declaration initializers and broader composed conditional state remain deferred. Test: `async_await_while_condition`.
  - Conditional three-await outer-`continue` loop conditions now carry initialized simple `for` declaration values through the selected branch and await-free incrementor; broader captured conditional state remains deferred. Test: `async_await_while_condition`.
  - Bounded multi-await direct and conditional loop-control continuations now also carry simple uninitialized `var` declaration storage through condition stages, outer `break`/`continue`, and await-free incrementors; broader declaration and mutation graphs remain deferred. Test: `async_await_while_condition`.
  - Bounded multi-await direct and conditional loop-control continuations now also carry simple uninitialized `let` declaration storage through condition stages, outer `break`/`continue`, and await-free incrementors; broader declaration and mutation graphs remain deferred. Test: `async_await_while_condition`.
  - Bounded multi-await loop-control continuations now preserve multiple ordered simple uninitialized `var` declarators through condition suspension and outer `break`; destructuring and broader declaration graphs remain deferred. Test: `async_await_while_condition`.
  - Bounded three-leaf mixed awaited loop conditions now stage `&&`→`||` and `||`→`&&` short-circuit transitions through outer `continue` re-entry; mixed nullish operators and broader expression graphs remain deferred. Test: `async_await_while_condition`.
  - Bounded three-leaf mixed `&&`→`||` loop conditions now also carry a simple uninitialized `var` declaration through outer `continue` re-entry and an await-free incrementor; broader mixed declaration graphs remain deferred. Test: `async_await_while_condition`.
  - Bounded three-leaf mixed `||`→`&&` and `??`→`||` loop conditions now also carry a simple uninitialized `var` declaration through outer `continue` re-entry and an await-free incrementor; broader mixed declaration graphs remain deferred. Test: `async_await_while_condition`.
  - Bounded left-associated mixed `&&`→`||` and `||`→`&&` loop conditions now also route outer `break` through awaited fallthrough while carrying a simple uninitialized `var` declaration; broader mixed loop-control graphs remain deferred. Test: `async_await_while_condition`.
  - Bounded left-associated mixed `||`→`??` and `??`→`||` loop conditions now also route outer `break` through awaited fallthrough while carrying a simple uninitialized `var` declaration; broader mixed-nullish loop-control graphs remain deferred. Test: `async_await_while_condition`.
  - Bounded right-associated `&&`→`||` and `||`→`??` loop conditions now also route outer `break` through awaited fallthrough while carrying a simple uninitialized `var` declaration; broader right-associated loop-control graphs remain deferred. Test: `async_await_while_condition`.
  - Bounded right-associated `??`→`||`, `&&`→`??`, and `??`→`&&` loop conditions now also route outer `break` through awaited fallthrough while carrying a simple uninitialized `var` declaration; broader right-associated loop-control graphs remain deferred. Test: `async_await_while_condition`.
  - Bounded right-associated `&&`→`||` loop conditions now also carry a simple uninitialized `var` declaration through outer `continue` re-entry and an await-free incrementor; broader right-associated declaration graphs remain deferred. Test: `async_await_while_condition`.
  - Bounded right-associated `||`→`??`, `&&`→`??`, and `??`→`&&` loop conditions now also carry a simple uninitialized `var` declaration through outer `continue` re-entry and an await-free incrementor; broader right-associated declaration graphs remain deferred. Test: `async_await_while_condition`.
  - Bounded left-associated `||`→`??` and right-associated `??`→`||` loop conditions now also carry a simple uninitialized `var` declaration through outer `continue` re-entry and an await-free incrementor; broader mixed-nullish declaration graphs remain deferred. Test: `async_await_while_condition`.
  - Mixed flat `&&`/`||` loop-condition staging now skips multiple short-circuited await leaves without reading uninitialized continuation state; mixed nullish operators and broader expression graphs remain deferred. Test: `async_await_while_condition`.
  - Bounded three-leaf mixed-nullish loop conditions now stage left-associated `??`→`||` and `||`→`??` transitions plus the right-associated `||`→`&&`, `&&`→`||`, `||`→`??`, `??`→`||`, `&&`→`??`, and `??`→`&&` shapes; broader mixed expression trees remain deferred. Test: `async_await_while_condition`.
  - Bounded conditional loop conditions now stage `await flag ? await whenTrue : await whenFalse` through outer `continue` re-entry and awaited return/throw fallthrough across declarations, methods, and function values; broader conditional suspension trees remain deferred. Test: `async_await_while_condition`.
  - Bounded conditional three-await loop conditions now preserve await-free body preludes ending in an outer `break` before awaited return/throw fallthrough; broader conditional loop-control graphs remain deferred. Test: `async_await_while_condition`.
  - Bounded three-or-more direct-awaited loop conditions now preserve await-free body preludes ending in an outer `break` before awaited return/throw fallthrough; broader loop-control and initializer/incrementor graphs remain deferred. Test: `async_await_while_condition`.
  - The bounded awaited loop-return subset now also covers a direct initialized `const` / `let` alias (`const value = await promise; return value;`) after the awaited condition. Test: `async_await_while_condition`.
  - The same bounded loop subset now covers direct initialized awaited-local aliases on `throw` (`const reason = await promise; throw reason;`); broader loop-local state remains deferred. Test: `async_await_while_condition`.
  - The bounded loop subset now also covers an awaited-local alias assigned after an uninitialized `let` declaration before direct `return` or `throw`; broader loop-local state remains deferred. Test: `async_await_while_condition`.
  - Awaited loop-local aliases may now feed a synchronous final return/throw expression after the suspension; broader post-await body state remains deferred. Test: `async_await_while_condition`.
  - The same loop alias subset now allows bounded await-free expression-statement postludes before the final return/throw expression; declarations and nested control flow after suspension remain deferred. Test: `async_await_while_condition`.
  - Assigned awaited-local aliases now share that bounded expression-statement postlude support; broader post-await local state remains deferred. Test: `async_await_while_condition`.
  - Bounded postludes may now include one initialized `const` / `let` local before the final alias expression; broader post-await declarations and control flow remain deferred. Test: `async_await_while_condition`.
  - Awaited-loop aliases now preserve multiple bounded await-free expression/local postlude statements in source order; broader post-await control flow remains deferred. Test: `async_await_while_condition`.
  - The same bounded postlude subset now accepts await-free non-abrupt control-flow statements such as `if`; abrupt branches and nested awaits remain deferred. Test: `async_await_while_condition`.
  - Bounded postludes now also support uninitialized `let` locals assigned synchronously before the final alias expression; uninitialized `const` and broader local state remain deferred. Test: `async_await_while_condition`.
  - Awaited loop locals may now be used only as bounded sequencing points before the final return/throw expression; broader ignored-local and multi-suspension state remains deferred. Test: `async_await_while_condition`.
  - Awaited loop aliases may now be synchronously mutated in bounded postludes before the final return/throw; broader mutable post-await state remains deferred. Test: `async_await_while_condition`.
  - Bounded post-await aliases now accept non-abrupt await-free `try` / `catch` / `finally` postludes before the final return/throw; nested awaits and abrupt exception branches remain deferred. Test: `async_await_while_condition`.
  - Bounded post-await aliases now also accept self-terminating await-free `while` postludes without `break`/`continue`; awaited loop bodies and broader loop state remain deferred. Test: `async_await_while_condition`.
  - Bounded post-await aliases now also accept initializer-free self-terminating `for` postludes without `break`/`continue`; loop declaration lists and awaited loop bodies remain deferred. Test: `async_await_while_condition`.
  - Bounded post-await aliases now also accept self-terminating await-free `do…while` postludes without `break`/`continue`; awaited loop bodies and broader loop state remain deferred. Test: `async_await_while_condition`.
  - Bounded post-await aliases now also accept exhaustive await-free `switch` postludes without `break`/`continue`; non-exhaustive switches and awaited case bodies remain deferred. Test: `async_await_while_condition`.
  - Bounded post-await aliases now also accept await-free `for…of` postludes without declaration lists or abrupt control; suspended iterators and broader iterable loop state remain deferred. Test: `async_await_while_condition`.
  - Bounded post-await aliases now also accept await-free `for…in` postludes without declaration lists or abrupt control; the bundled core shim now supplies the global `Extract` utility needed by this typed loop path. Test: `async_await_while_condition`.
  - Bounded post-await aliases now also accept `for…of` postludes with one simple `const`/`let` iterator binding; nested awaits, declaration-list variants elsewhere, and abrupt control remain deferred. Test: `async_await_while_condition`.
  - Bounded post-await aliases now also accept `for…in` postludes with one simple `const`/`let` iterator binding; nested awaits, declaration-list variants elsewhere, and abrupt control remain deferred. Test: `async_await_while_condition`.
  - Bounded post-await aliases now also accept simple mutable `let` iterator bindings in await-free `for…of` and `for…in` postludes; destructuring, nested awaits, and abrupt control remain deferred. Test: `async_await_while_condition`.
  - Bounded post-await loop postludes now preserve loop-scoped `break` and `continue` in await-free `while`, `for`, `do…while`, `for…of`, and `for…in` statements, including multiple simple nested `const`/`let` declarations in ordinary `for` initializers; top-level abrupt postludes, nested awaits, destructuring, and broader nested loop state remain deferred. Test: `async_await_while_condition`.
  - Bounded post-await `switch` postludes now preserve clause-scoped `break` alongside the existing non-fallthrough boundary; `continue`, nested awaits, and broader switch state remain deferred. Test: `async_await_while_condition`.
  - Bounded post-await `switch` postludes now accept one or more simple branch-local `const`/`let` declarations in a case clause; destructuring, nested awaits, and broader declaration flow remain deferred. Test: `async_await_while_condition`.
  - Bounded post-await `try` / `catch` / `finally` postludes now accept one or more simple `const`/`let` locals directly in an exception clause; nested awaits, destructuring, and broader exception state remain deferred. Test: `async_await_while_condition`.
  - Bounded post-await `if` / `else` postludes now accept one or more simple `const`/`let` locals directly in a branch block; nested awaits, destructuring, and broader branch state remain deferred. Test: `async_await_while_condition`.
  - Nested await-free `for…of` / `for…in` postludes now preserve multiple simple `const`/`let` locals inside supported `if` and `switch` clauses; nested awaits, destructuring, and broader iterator state remain deferred. Test: `async_await_while_condition`.
  - Bounded post-await direct declaration postludes now accept multiple simple `const`/`let` locals in source order; destructuring, nested awaits, and broader local state remain deferred. Test: `async_await_while_condition`.
  - Bounded post-await direct declaration postludes now accept initialized `var` locals; uninitialized/escaping `var`, destructuring, nested awaits, and broader local state remain deferred. Test: `async_await_while_condition`.
  - Bounded post-await direct declaration postludes now accept uninitialized `var` locals assigned synchronously before the final return; escaping `var`, destructuring, nested awaits, and broader local state remain deferred. Test: `async_await_while_condition`.
  - Bounded awaited-loop bodies now accept an initialized `var` prelude before the body’s awaited alias; locals that must survive additional suspension points, destructuring, nested awaits, and broader state remain deferred. Test: `async_await_while_condition`.
  - Bounded awaited-loop bodies now accept an uninitialized `var` prelude assigned before the body’s awaited alias; locals that must survive additional suspension points, destructuring, nested awaits, and broader state remain deferred. Test: `async_await_while_condition`.
  - Bounded awaited-loop bodies now accept multiple simple declaration locals in one prelude statement before the body’s awaited alias; destructuring, nested awaits, and broader state remain deferred. Test: `async_await_while_condition`.
  - Assigned awaited-loop aliases now preserve multiple bounded await-free postlude statements before the final return/throw; nested awaits, broader assignment graphs, and unsupported control flow remain deferred. Test: `async_await_while_condition`.
  - Bounded post-await `for…of`/`for…in` postludes now accept one simple `var` iterator binding alongside `const`/`let`; destructuring, nested awaits, and broader iterator state remain deferred. Test: `async_await_while_condition`.
  - Bounded nested await-free loop postludes now accept simple `var` declarations in addition to `const`/`let`; uninitialized `var`, destructuring, nested awaits, and broader loop state remain deferred. Test: `async_await_while_condition`.
  - The same bounded interstitial extension now covers await-free `switch` statements without branch-local declarations; awaited branch bodies and general state-machine lowering remain deferred.
  - The bounded interstitial extension also covers await-free `while` / `do` / `for` loop statements without loop-local declarations; loops containing awaits or declaration lists remain deferred.
  - Single-statement `throw await promise` bodies now use the leading continuation adapter, preserving declaration, method, and async-arrow receiver/parameter evaluation before the suspension and rejecting with the fulfilled awaited value; broader throw-await expression graphs remain deferred.
  - Leading chains now also support a terminal `throw await promise` after an earlier awaited local, carrying the earlier awaited value into the final source across declarations, methods, function values, and branch-block arms; broader throw-await expression graphs remain deferred.
  - Terminal `throw await promise` chains may now begin after await-free captured local or expression preludes across declarations, methods, and async-arrow function values; broader pre-suspension mutable state remains deferred.
  - Conditional leading await branches now join a common terminal `throw await promise` across declarations, methods, and async-arrow function values; broader conditional throw-await expression graphs remain deferred.
  - Unequal conditional leading await branches now also join a common terminal `throw await promise` after bounded divergent sequencing lengths across declarations, methods, and async-arrow function values; broader unequal throw-await graphs remain deferred.
  - Single terminal throw expressions with one bounded non-conditional awaited operand now use the expression continuation adapter and reject with the transformed value across declarations, methods, and async-arrow function values; multiple awaited operands and conditional/short-circuit throw expressions remain deferred.
  - The same single-awaited throw-expression subset now preserves await-free captured local preludes across declarations, methods, and async-arrow function values; broader mutable pre-suspension throw state remains deferred.
  - Terminal throw expressions now also support two left-associated non-conditional awaited operands through the staged expression adapter across declarations, methods, and async-arrow function values; larger or conditional throw expression trees remain deferred.
  - The staged terminal throw-expression subset now extends to three left-associated non-conditional awaited operands across declarations, methods, and async-arrow function values; larger or conditional throw expression trees remain deferred.
  - The staged terminal throw-expression subset now extends to five-or-more left-associated non-conditional awaited operands through the generic sequence adapter across declarations, methods, and async-arrow function values; bounded two-arm conditional and `&&` / `||` / `??` short-circuit throw expressions are also covered, while broader conditional and short-circuit throw trees remain deferred.
  - The bounded conditional throw-expression subset now also preserves await-free captured local preludes across declarations, methods, and async-arrow function values; broader mutable conditional throw state remains deferred.
  - Source `try` / `catch` now catches bounded single-, flat three-await short-circuit, and flat five-await non-short-circuit throw expressions through the staged sequence adapter, including source rejection handoff and transformed catch recovery across declarations, methods, and async-arrow function values; broader awaited throw/catch graphs remain deferred.
  - Source `try` / `catch` awaited throw expressions now preserve bounded initialized local preludes inside the try block across declarations, methods, and async-arrow function values; broader mutable try-local state remains deferred.
  - The same source `try` / `catch` awaited throw-expression subset now preserves bounded await-free expression-statement preludes inside the try block; nested awaits and broader control-flow preludes remain deferred.
  - The same subset now also preserves bounded await-free non-abrupt `if` / `switch` / loop / `try` control-flow preludes inside the try block; declarations, abrupt paths, nested awaits, and broader state-machine control flow remain deferred.
  - Source `try` / `catch` awaited throw expressions now also preserve bounded await-free non-abrupt `if` / `switch` / loop / `try` control-flow preludes before catch recovery; declarations, abrupt paths, nested awaits, and broader catch state remain deferred.
  - Combined source `try` / `catch` / `finally` awaited-return chains now preserve the same bounded await-free non-abrupt control-flow preludes before catch recovery; declarations, abrupt paths, nested awaits, and broader combined exception state remain deferred.
  - Combined source `try` / `catch` / `finally` now catches bounded single-, flat three-await short-circuit, and flat five-await non-short-circuit throw expressions, routing transformed values and source rejections through catch recovery before finally execution; broader combined awaited throw graphs remain deferred.
  - Source `try` / `finally` now preserves bounded single-, flat three-await short-circuit, and flat five-await non-short-circuit throw expressions, executing finally before rejecting with the transformed value or original awaited rejection; broader try/finally awaited throw graphs remain deferred.
  - Branch-block fallthrough returns now support bounded short-circuit `&&` / `||` / `??` expressions with a direct awaited left operand and synchronous RHS across declarations, methods, and function values; multiple awaits and broader branch short-circuit graphs remain deferred.
  - Branch-block fallthrough returns now also support bounded short-circuit `&&` / `||` / `??` expressions with direct awaits on both operands, short-circuiting after the first fulfillment or sequencing the second await; longer flat chains are covered below, while broader nested or structurally mixed short-circuit graphs remain deferred.
  - Branch-block fallthrough returns now support flat left-associated `&&` / `||` / `??` chains with three-or-more direct awaited operands through the generic sequence adapter, validated through five awaits, short-circuiting at each fulfilled stage or continuing to the next await; broader nested or structurally mixed short-circuit graphs remain deferred.
  - Branch-block fallthrough conditional return expressions now compose that flat three-or-more-await short-circuit subset in their selected arms across declarations, methods, and function values; broader nested, unequal, or structurally mixed conditional short-circuit graphs remain deferred.
  - Terminal throw expressions now also support bounded short-circuit `&&` / `||` / `??` trees with direct awaits on both operands, including first-operand short-circuit rejection and second-operand awaited rejection; longer flat chains are covered below, while broader nested or structurally mixed short-circuit trees remain deferred.
  - Terminal throw expressions now support flat left-associated `&&` / `||` / `??` chains with three-or-more direct awaited operands through the generic sequence adapter, validated through five awaits, preserving short-circuit rejection values and later-await rejection values; broader nested or structurally mixed short-circuit trees remain deferred.
  - Conditional terminal throw expressions now compose that flat three-or-more-await short-circuit subset in each selected arm across declarations, methods, and function values; broader nested, unequal, or structurally mixed short-circuit conditional trees remain deferred.
  - Branch-block fallthrough returns now support flat non-short-circuit expression sequences with two, three, and five-or-more direct awaited operands in conditional arms through the staged/generic sequence adapters; nested or structurally broader branch embedded-await graphs remain deferred.
  - Branch-block fallthrough returns now also cover supported structural multi-await expressions through the generic sequence adapter, including array elements, object-literal properties, multi-await call arguments, and multi-await template expressions; dynamic arrays are boxed for generic Promise callbacks while typed array returns retain their native representation, and unsupported structural forms plus broader branch embedded-await graphs remain deferred.
  - Async source `try` / `catch` / `finally` recognizers now safely decline blocks consumed entirely by await-free preludes; empty post-prelude try bodies no longer crash recognition and remain deferred. Regression: `async_await_branch_return_await_try_prelude`.
  - Leading chains now also accept bare `Promise<void>` sequencing awaits between awaited locals; general awaited expressions and nested awaited control flow remain deferred.
  - Await-free interstitial `try` / `catch` / `finally` statements with simple initialized or assigned-before-use `const` / `let` locals are now covered; source try blocks containing awaited returns and broader declaration/destructuring forms remain deferred.
  - Leading chains now cover a terminal direct `return await`; general direct-return-await control-flow and nested awaited expressions remain deferred.
  - Direct branch `return await` arms now preserve safe initialized branch-local declaration preludes across declarations, methods, function values, and branch-block fallthrough; awaited, escaping, and broader branch return-await graphs remain deferred.
  - The same direct branch `return await` subset now preserves branch-local `let` declarations assigned before the awaited source; assignments after suspension and broader mutable branch state remain deferred.
  - Direct `return await` with await-free expression-statement and bounded control-flow preludes is covered for async declarations; nested awaits and broader state-machine forms remain deferred.
  - The same direct-prelude `return await` subset is covered for class methods and function values; broader nested-await forms remain deferred.
  - Direct-prelude `return await` accepts await-free initialized locals and uninitialized assignments before the awaited source; general locals crossing nested suspension points remain deferred.
  - Direct-prelude `return await` now also accepts an uninitialized `var` local assigned before the awaited source across declarations, class methods, and async arrows; unassigned or escaping `var` state remains deferred. Test: `async_await_while_condition`.
  - Direct branch `return await` now covers bounded `switch` statements with a terminal or reordered `default`, empty fallthrough case labels, and terminating clauses across async declarations, methods, and function values; suspension-bearing case expressions, unresolved trailing fallthrough, and broader switch state remain deferred.
  - Direct branch `return await` now also preserves captured locals mutated by await-free `while`, `do while`, `for`, `for-in`, and `for-of` preludes with simple initialized or assigned-before-use `const` / `let` locals plus initialized `var` locals used within the control-flow body across declarations, methods, and function values; the bounded loop-prelude subset now also carries assigned-before-use uninitialized `var` state across all five loop shapes and those surfaces, plus simple `var` declarations inside all five loop bodies with initialized and undefined fallthrough paths; destructuring, nested awaits, and broader loop state remain deferred.
  - Direct branch `return await` now also preserves captured locals mutated by an await-free `try` / `finally` prelude across declarations, methods, and function values; the bounded exception-prelude subset also hoists a simple `var` declared in a caught `try` / `catch` / `finally` path before the final await; awaited exception paths, destructuring, and broader branch exception state remain deferred.
  - Direct branch `return await` now also lowers a single optional awaited expression statement in an `if` arm before the final `return await`, including an explicit await-free `else` arm, preserving exact synchronous branch entry and awaited-branch sequencing across declarations, class methods, and function values; a bounded initialized or uninitialized local, including a top-level dynamic `any` capture, may also be assigned from the awaited arm and the synchronous arm before the final await, with undefined fallthrough covered when the optional arm is not entered, and await-free nested `if` preludes covered inside the awaited arm, while awaited nested control flow and broader multi-suspension branches remain deferred. Leading awaited-local chains now also hoist nested dynamic `var` captures declared by await-free `if` preludes across declarations, methods, and function values, including undefined fallthrough; broader nested suspension graphs remain deferred.
  - Branch-local source `try` / `catch` now routes direct `return await` fulfillment and rejection paths plus flat five-await throw recovery across declarations, methods, and function values; broader nested branch exception graphs remain deferred.
  - Branch-local source `try` / `finally` now runs finally bodies on direct `return await` fulfillment and rejection paths plus bounded single-, flat three-await short-circuit, and flat five-await non-short-circuit throw paths across declarations, methods, and function values; broader nested finally suspension graphs remain deferred.
  - Branch-local source `try` / `catch` / `finally` now composes direct `return await` fulfillment, catch recovery, finally routing, bounded finally-throw overrides, and flat five-await throw recovery across declarations, methods, and function values; broader nested exception graphs remain deferred.
  - The exact nested `return await (await promise)` form and one embedded inner await in a non-Promise outer expression are covered across declarations, methods, and expression-bodied async arrows; nested awaits embedded in broader expression trees and general multi-suspension expressions remain deferred.
  - Bounded two-, three-, and four-await staged inner expressions, bounded two-await `&&` / `||` / `??` inner expressions, and flat five-await non-short-circuit inner expressions with a final stable synchronous leaf are covered in direct `return await` operands for non-Promise outer expressions, including closure captures in expression-bodied async arrows; broader nested suspension trees remain deferred.
  - Nested direct `return await` now also covers bounded multi-await template expressions and call argument expressions across declarations, methods, and expression-bodied async arrows; broader structural suspension trees remain deferred.
  - Nested direct `return await` now also covers bounded multi-await array elements and object-literal properties across declarations, methods, and expression-bodied async arrows; broader structural suspension trees remain deferred.
  - Nested direct `return await` now also covers awaited property receivers, dynamic element indices, and bounded multi-await constructor arguments across declarations, methods, and expression-bodied async arrows; broader structural suspension trees remain deferred.
  - Nested direct `return await` now also covers bounded multi-await tagged-template substitutions and awaited dynamic call targets, including typed function values with or without an explicit `this` parameter, across declarations, methods, and expression-bodied async arrows; broader structural suspension trees remain deferred.
  - Nested direct `return await` now also covers an awaited typed member receiver combined with bounded multi-await method arguments across declarations, methods, and expression-bodied async arrows; broader structural suspension trees remain deferred.
  - Nested direct `return await` now also covers awaited dynamic constructor targets, including typed class values, combined with bounded multi-await constructor arguments across declarations, methods, and expression-bodied async arrows; broader structural suspension trees remain deferred.
  - Conditional nested direct `return await` now also covers selected multi-await array, call, template, and object-literal arms across declarations, methods, and expression-bodied async arrows; broader structural branch graphs remain deferred.
  - Conditional nested direct `return await` now also covers selected two-await `&&` / `||` short-circuit arms across declarations, methods, and expression-bodied async arrows; broader structural branch graphs remain deferred.
  - Conditional nested direct `return await` now also covers flat three-await `&&` / `||` short-circuit arms across declarations, methods, and expression-bodied async arrows; broader structural branch graphs remain deferred.
  - Conditional nested direct `return await` now also covers flat three-await `??` short-circuit arms across declarations, methods, and expression-bodied async arrows, including typed nullable-union awaited values; broader structural branch graphs remain deferred.
  - Conditional nested direct `return await` now also covers selected structural element-access arms and awaited constructor-property arms across declarations, methods, and expression-bodied async arrows; broader structural branch graphs remain deferred.
  - Conditional nested direct `return await` now also covers awaited property receivers and dynamic element indices in selected arms across declarations, methods, and expression-bodied async arrows; broader structural branch graphs remain deferred.
  - Conditional nested direct `return await` now also covers awaited dynamic callable targets and awaited member receivers combined with multi-await call arguments in selected arms across declarations, methods, and expression-bodied async arrows; broader structural branch graphs remain deferred.
  - Conditional nested direct `return await` now also covers recursively nested conditional selections of multi-await array and object arms across declarations, methods, and expression-bodied async arrows; broader structural branch graphs remain deferred.
  - Conditional nested direct `return await` now also covers awaited dynamic constructor targets with multi-await constructor arguments in selected arms across declarations, methods, and expression-bodied async arrows; broader structural branch graphs remain deferred.
  - Conditional nested direct `return await` now also covers nested structural arms in static class methods and object-literal async methods; broader async function edge cases remain deferred.
  - Conditional nested direct `return await` now also covers nested structural arms in lifted and named async function values; broader async function edge cases remain deferred.
  - Exact two-, three-, and four-sibling-await forms plus left-associated five-or-more sibling-await binary expressions, literal leaves, side-effect-free `void` leaves, mixed literal/direct-await array elements and object properties, mixed literal/direct-await template and tagged-template substitutions, `typeof` over literal leaves including BigInt and regular expressions plus side-effect-free `void` literals, mixed literal/direct-await call and constructor arguments including directly awaited constructor callees, literal-index/direct-await element access, multi-await template substitutions, tagged-template substitutions, multi-await array literals, direct-await call arguments, direct-awaited dynamic call targets, direct-awaited property receivers and element indices, supported await-bearing member and constructor callees, side-effect-free prefix and `typeof` operators around direct awaits, typed object-literal properties, constructor arguments, and supported await-bearing nested call operands are covered; other non-binary deeper expression trees and branch-dependent multi-await expressions remain deferred.
  - Conditional direct awaits inside an outer `return await` are covered through recursively nested bounded trees, including mixed sync/await arms and expression-bodied async-arrow routing; bounded recursive `&&` / `||` / `??` suspension branches are also covered, while broader short-circuit branch graphs remain deferred.
  - Direct-prelude `return await` now covers await-free `if`, `switch`, loop, and `try` / `finally` statements with simple initialized or assigned-before-use `const` / `let` locals plus initialized `var` locals used within those control-flow statements, including assigned-before-use uninitialized `var` state in `if`, selected-case `switch`, self-terminating `while`, guaranteed-first-iteration `do…while`, guaranteed-one-iteration ordinary `for`, one-element `for…of`/`for…in`, normal-path `try`/`finally`, caught-path `try`/`catch`, and caught-path `try`/`catch`/`finally` preludes across declarations, methods, and function values; simple escaping `var` declarations inside all those await-free control-flow preludes are also hoisted across the final await; nested awaits, destructuring, and broader declaration-bearing control flow remain deferred.
  - Leading awaited-local chains now support a terminal post-await `throw` after multiple suspensions for async declarations, class methods, and function values; broader awaited control-flow graphs remain deferred.
  - Leading awaited-local chains now also accept initialized or uninitialized `let` aliases assigned from awaited promises across multiple suspension points, including repeated assignment to the same alias; broader mutable-local state machines remain deferred.
  - Leading awaited-local chains now support bounded conditional await steps with Promise-valued true and false branches across async declarations, class methods, and function values; incomplete or unequal nested awaited branch trees and broader suspension-aware control flow remain deferred.
  - Nested complete `if` / `else` trees now flatten into bounded equal-length conditional await steps across async declarations, class methods, and function values; unequal nested trees are supported when they join a bounded common synchronous return, while incomplete trees and broader suspension-aware control flow remain deferred.
  - Branch-block leading awaited-local chains now compose the bounded conditional await-step subset with a synchronous fallthrough arm; broader branch-local suspension graphs remain deferred.
  - Branch-block leading awaited-local chains now compose flattened nested conditional await selections of arbitrary recognized depth with a synchronous fallthrough arm across async declarations, class methods, and function values; deeper branch-local suspension graphs remain deferred.
  - Leading awaited-local chains now flatten nested `else if` conditional await selections of arbitrary recognized depth with Promise-valued leaves; nested awaited branch bodies with unequal or more general suspension graphs remain deferred.
  - Nested conditional leading await steps now preserve await-free arm preludes, equal-length sequential awaited assignments, branch-local value flow, and post-await arm state across flattened conditional selections of arbitrary recognized depth before the next suspension; unequal-length or broader awaited arm graphs remain deferred.
  - Unequal conditional leading await arms now support bounded branch-local sequencing chains that join a common synchronous return, including the covered two-await versus one-await shape across declarations, methods, and function values; unequal graphs with escaping branch state or broader joins remain deferred.
  - The same unequal shared-tail subset now threads captured outer-state updates across divergent await counts before the common return, including complete nested conditional trees that preserve outer state while joining bounded common synchronous, direct `return await`, or awaited-local tails; broader mutable branch state and nested joins remain deferred.
  - Unequal conditional branches now also join a bounded common awaited-local tail before the final return, preserving divergent two-await versus one-await sequencing across declarations, methods, and function values; broader common-tail control flow remains deferred.
  - Unequal conditional branches now also join a bounded common terminal `throw` after divergent await counts across declarations, methods, and function values, including complete nested conditional trees; broader throw/control-flow joins remain deferred.
  - Conditional leading await steps now preserve bounded await-free expression and initialized or assigned-before-await branch-local declaration preludes in both direct arms across declarations, methods, and function values; awaited, escaping, or broader branch preludes remain deferred.
  - Conditional leading await steps now also accept ordinary multi-declaration lists that mix synchronous branch-local declarators with direct awaited declarators; escaping, destructured, and broader branch-local state remains deferred. Test: `async_await_conditional_branch_local_sequence`.
  - Conditional leading await steps now preserve bounded escaping initialized or assigned-before-suspension `var` state declared in await-free branch preludes across the first branch suspension; destructured and broader mutable state remains deferred. Test: `async_await_conditional_branch_local_sequence`.
  - Conditional leading await steps now preserve assigned-before-suspension uninitialized `var` state between consecutive branch-local awaits across declarations, methods, function values, and branch fallthrough; unassigned, escaping, destructured, and broader mutable state remains deferred. Test: `async_await_conditional_branch_local_sequence`.
  - Conditional leading await steps now allow branch-local awaited declarations used only as sequencing points; bounded updates to outer captured state are covered below, while broader branch-local escapes remain deferred.
  - Conditional leading await steps now compose equal-length sequences of branch-local awaited declarations used only as sequencing points across declarations, methods, function values, and branch fallthrough; escaping values, unequal-length arms, and broader branch graphs remain deferred.
  - Conditional leading await steps now allow a branch-local first awaited value to feed the next branch-local await expression when that later step is sequencing-only; values that must persist beyond that next suspension remain deferred.
  - The same branch-local value-flow subset now composes across multiple consecutive conditional suspensions, with each branch-local result feeding the next sequencing-only await; values that must survive beyond the next branch adapter remain deferred.
  - The branch-local value-flow subset now also composes a branch-local awaited declaration arm with a sibling direct-await sequencing arm across declarations, methods, function values, and branch fallthrough; broader mixed branch graphs remain deferred.
  - Conditional arm post-await state now permits a branch-local awaited value to update an outer captured local before the next sequencing await across declarations, methods, function values, and branch fallthrough; broader branch-local state graphs remain deferred.
  - Conditional arm post-await state now also permits the final branch-local awaited value to update outer captured state before the common return across declarations, methods, function values, and branch fallthrough; broader branch-local terminal state graphs remain deferred.
  - The same sequencing subset now accepts a mixed direct-await arm and branch-local awaited declaration arm.
  - Conditional leading await steps now preserve bounded await-free post-await statements in both direct arms before the next suspension; final conditional steps and broader branch-local suspension graphs remain deferred.
  - Conditional leading await steps now also preserve bounded await-free post-await statements in the final conditional step before return across async declarations, class methods, and function values; broader branch-local suspension graphs remain deferred.
  - Await-free interstitial `try` / `catch` statements now allow simple catch bindings; awaited try/catch bodies and broader suspension-aware exception graphs remain deferred.
  - Await-free direct-`return await` preludes now also allow simple catch bindings; awaited source try/catch graphs remain deferred.
  - Source `try` / `catch` now supports a bounded leading multi-await chain in the try block across async declarations, class methods, and function values, including receiver capture, pending rejection handoff into the catch continuation, and bounded catch rethrows; broader suspension-aware exception graphs remain deferred.
  - Leading source `try` / `catch` chains now also support initialized local catch preludes before the final catch return/throw; broader catch-local state and suspension-aware exception graphs remain deferred.
  - Combined source `try` / `catch` / `finally` leading chains now also support initialized local catch preludes before catch recovery and finally routing; broader catch-local state and suspension-aware exception graphs remain deferred.
  - Source `try` / `finally` now supports a bounded leading multi-await chain in the try block across async declarations, class methods, and function values, including receiver capture, initialized and uninitialized `let` finally locals, repeated and compound local assignments, finally execution on fulfillment/rejection, finally-throw override, bounded finally-body await sequences with await-free expression/control-flow/try-catch interstitials and post-await statements, conditional finally awaits with Promise-valued branches, bounded synchronous return overrides after finally awaits across declarations, methods, and function values, and a bounded finally `return await` override across declarations, methods, and function values; deeper/nested finally suspension graphs and broader suspension-aware exception graphs remain deferred.
  - Combined source `try` / `catch` / `finally` now composes the bounded leading multi-await chain with catch recovery and finally routing, including finally-throw override; broader combined suspension-aware exception graphs remain deferred.

---

## 2. Dependent phases

- **Dispatch concurrency (optional, libdispatch-backed) — remainder** (design/status: [`dispatch_async_todo.md`](dispatch_async_todo.md))
  - Landed: `DispatchQueue` serial/concurrent queues, `dispatch.async` (Promise settled on the main loop) and `dispatch.sync` with deadlock detection, const-primitive capture discipline including recursive auditing of top-level helper functions, automatic creation-time `structuredClone` copies for const array/dynamic captures, thread-safe no-GC bump-chunk allocation under `TSC_THREADS`, error propagation, the `TSC_THREADS` thread-safety baseline (threaded Boehm GC with per-task worker registration, TLS exception state, locked shape/symbol/singleton slow paths, cross-thread completion queue, condvar event loop), usage-gated compilation/linking so non-dispatch programs are byte-identical, and e2e coverage with libdispatch-absent skip. The `--dispatch=serial` fallback now runs the same API through the single-threaded event loop without libdispatch; tests: `dispatch_serial`, `dispatch_serial_after`. Dispatch task adapters also return typed `Date`, `RegExp`, `Error`, `Buffer`, `URL`, `URLSearchParams`, `ArrayBuffer`, `DataView`, `TextEncoder`, and `TextDecoder` values across both backends; tests: `dispatch_objects`, `dispatch_serial_objects`, `dispatch_url_results`, `dispatch_serial_url_results`, `dispatch_binary_types`, `dispatch_serial_binary_types`. Promise-based `dispatch.group` joins inline task arrays, and `dispatch.barrier` provides exclusive completion on concurrent queues; tests: `dispatch_group_barrier`, `dispatch_serial_group_barrier`. Capture copy tests: `dispatch_capture_clone`, `dispatch_serial_capture_clone`. Threaded `--no-gc` dispatch test: `dispatch_no_gc`.
  - Remaining work: `Shared<T>`/queue-confinement capture escape hatches beyond const primitives; broader task return types beyond number/string/boolean/array/Map/Set/Date/RegExp/Error/Buffer/URL/URLSearchParams/ArrayBuffer/DataView/TextEncoder/TextDecoder/void; relaxing the awaited-shape dependence once Phase 6 general state-machine lowering lands (dispatch promises awaited outside the recognized continuation subsets currently hit the pending-await bail); and a many-thread GC soak + TSan CI configuration and a CI job that builds libdispatch from source.

- **Phase 7 remainder — ES language features**
  - Remaining work: general lazy generator state-machine lowering beyond the current bounded sequential branch/switch/loop, single-suspended expression-tree, finite direct binary multi-yield return with bounded prefix-unary, `typeof`, `void`, equality, relational comparison, exponentiation, comma-sequencing, `in` membership, class-identifier `instanceof`, stable generator-parameter identifier, direct `this`, direct pre-return `const`, non-mutated initialized `let`, unshadowed `NaN`/`Infinity` global, outer captured identifier, non-computed stable-receiver property, staged computed element leaves, and bounded lazy `yield*` subsets; side-effecting leaves beyond the covered direct no-yield call/new/delete/assignment/conditional/template/tagged-template/postfix/prefix-update subset, logical leaves beyond the covered string/string and dynamic-value `&&`/`||`/`??` subset, literal leaves beyond the covered no-yield array/object/regular-expression literal subset, mutable or otherwise uninitialized generator locals beyond the covered initialized mutable `let`/`var` and assigned-before-use uninitialized `let`/`var` subsets, unsupported computed receivers/indices, member-call expression graphs beyond this optional dynamic method subset, unsupported non-binary operators, and otherwise arbitrary expression graphs beyond direct binary trees whose environments are sized for every finite yield count; `try` control flow across suspension points beyond the covered non-suspending source `try` / `catch` / `finally`, bounded normal-resume `try` / `finally`, and bounded suspended throw-yield subsets; `async function*`; broader iterator protocol edge cases beyond the current array/string/Map/Set/custom/URLSearchParams/Buffer/dynamic and materialized/lazy generator-backed `yield*` subsets; and async iterables.
  - Typed lazy generators now preserve suspension when converted across compatible `Generator<...>` element representations in both boxing and unboxing directions, converting each yielded item on demand and forwarding bounded `.return(...)`/`.throw(...)` cleanup; broader iterator-object adaptation and arbitrary generator type conversions remain deferred.
  - Typed lazy generator conversion adapters now resume after a source lazy generator handles a forwarded `.throw(error)`, preserving the source return value; broader adapter protocol overrides remain deferred.
  - Lazy `yield*` now preserves delegated throw recovery when its source passes through simple typed generator conversion helpers, transparent direct-call wrappers, conditionals, nullish/logical fallbacks, or a bounded nested conversion chain, including the converted return value; broader passthrough and adapter graphs remain deferred.
  - Lazy `yield*` now preserves delegated throw recovery through a typed generator conversion arrow/closure passthrough, including the converted return value; broader callable passthrough graphs remain deferred.
  - Lazy generator `yield*` now preserves generator-backed delegation across suspended inner yields and forwards `.next(value)` one step at a time; broader delegated return/throw protocol overrides remain deferred.
  - Lazy `yield*` now preserves suspension and bounded delegated throw recovery for typed custom iterables whose `[Symbol.iterator]()` is a generator method; broader custom iterator protocol adaptation remains deferred.
  - Lazy `yield*` now pulls one result per resume from typed custom iterator objects whose `next()` returns `{ done, value }`, forwards a bounded zero- or one-argument `.next(value)`, preserves their terminal value, forwards bounded zero- or one-argument `.return()` / `.throw()` calls that complete or yield one immediate step, and propagates handled delegated return completions through the outer iterator; multi-step or suspending closure methods remain deferred.
  - Lazy generator `yield*` now carries the delegated iterator return value into bounded assignment and return expressions; delegated `throw`/`return` protocol overrides and broader result-flow graphs remain deferred.
  - Lazy `yield*` now forwards bounded outer `.return(value)` and `.throw(error)` closure into an active lazy delegated generator, resumes the outer generator when the delegated throw is caught, and unwinds nested simple `finally` cleanup; broader delegated protocol overrides remain deferred.
  - Lazy generators now route a terminal source `return` after suspended `try`-block yields through a non-suspending `finally`; broader abrupt control flow and suspension inside `finally` remain deferred.
  - Lazy generators also route a terminal literal source `throw` after suspended `try`-block yields through a non-suspending `finally`; catchable throws and side-effecting or dynamic throw expressions remain deferred.
  - Lazy generators now support bounded catch `if` / `else` direct literal/catch-value throws through a non-suspending `finally`; broader conditional catch control flow remains deferred.
  - Lazy generators now support bounded catch `if` / `else` mixed direct literal/catch-value return and throw arms through a non-suspending `finally`; broader conditional catch control flow remains deferred.
  - Lazy generators now support bounded non-suspending `finally` `if` / `else` overrides with direct literal return/throw arms, including mixed completion, on both suspended `.throw(...)` and normal completion; broader abrupt finalizer control flow remains deferred.
  - Lazy generators now catch a bounded `.throw(error)` at a suspended `try` block when the catch has no binding, a simple unused identifier, a simple identifier returned directly, a simple assigned/compound-assigned identifier, or a two-leaf string concatenation with that identifier, producing a completed iterator result; destructured used bindings, catch yields, and broader recovery graphs remain deferred.
  - Lazy generators now compose that bounded suspended catch recovery with a non-suspending `finally`, running finalization on both handled `.throw(error)` and normal completion and honoring terminal literal `throw` or `return` overrides; nested suspension, other abrupt finally paths, and broader recovery graphs remain deferred.
  - Lazy generators now execute bounded yield-free, non-abrupt catch expression-statement preludes plus simple string-literal or catch-binding `const` / `let` aliases before terminal catch recovery; nested suspension and broader recovery graphs remain deferred.
  - Lazy generators now preserve bounded chains of those simple catch-binding aliases, direct alias assignments, and simple string compound assignments before terminal recovery; broader catch-local state remains deferred.
  - Lazy generators now support a terminal literal catch `throw` after a suspended yield, including the bounded catch prelude and non-suspending `finally` ordering; dynamic catch throws and broader recovery graphs remain deferred.
  - Lazy generators now also rethrow a simple catch identifier directly after a suspended yield when no `finally` must carry the binding; compound/dynamic catch rethrows and broader recovery graphs remain deferred.
  - Lazy generators now carry a simple catch-identifier rethrow through a non-suspending `finally`, while preserving finalizer `throw` / `return` overrides; compound/dynamic catch rethrows and broader recovery graphs remain deferred.
  - Lazy generators now support two-leaf string composition in catch rethrows, including when the composed error is carried through a non-suspending `finally`; broader catch throw expressions remain deferred.
  - Lazy generators now support bounded three-leaf string composition around catch values in direct catch returns and rethrows, including through a non-suspending `finally`; broader catch expression graphs remain deferred.
  - Lazy generators now support bounded catch `if` / `else` recovery with direct literal/catch-value returns or throws, including mixed return/throw branches through a non-suspending `finally`; nested catch control flow and broader graphs remain deferred.
  - Lazy generator suspension now survives direct and aliased generator function-value calls through closure adapters; broader iterator-object adaptation and dynamic callable generator discovery remain deferred.
  - Lazy generators now cover bounded array- and string-backed `for...of` loops with suspension-safe iterator positions plus `break`/`continue`; `for...in`, async iteration, and broader iterator-source lowering remain deferred.
  - The bounded lazy `for...of` subset now also covers typed `Set` sources; typed `Map`, custom iterator objects, async iteration, and broader iterator-source lowering remain deferred.
  - The bounded lazy `for...of` subset now also covers typed `Map` sources as key/value entry pairs; custom iterator objects, async iteration, and broader iterator-source lowering remain deferred.
  - The bounded lazy `for...of` subset now also covers supported typed custom iterable/iterator classes; async iteration and broader iterator-source lowering remain deferred.
  - The bounded lazy `for...of` subset now also covers `URLSearchParams` key/value entries; async iteration and broader iterator-source lowering remain deferred.
  - The bounded lazy `for...of` subset now also covers typed Buffer byte iteration; async iteration and broader iterator-source lowering remain deferred.
  - Lazy `yield*` now covers typed `URLSearchParams` key/value entries and Buffer byte sources while preserving one-source-item suspension; broader iterator protocol and async-iterable lowering remain deferred.
  - Materialized generator `yield*` now covers typed `URLSearchParams` key/value entries and Buffer byte sources; broader iterator protocol and async-iterable lowering remain deferred.
  - Regular `for...of` and `for...in` lowering now covers typed Buffer byte values and numeric own keys; broader iterator protocol and async-iterable lowering remain deferred.
  - Lazy generators now cover bounded `for...in` loops over typed arrays, strings, Buffers, and dynamic values with suspension-safe key positions; async iteration and broader enumerable-source lowering remain deferred.
  - Lazy generators now also cover bounded `for...in` loops over typed class instances with statically resolved enumerable fields and typed interface-shaped object values, preserving suspension-safe key positions; broader object/prototype enumeration and async iteration remain deferred. Test: `generator_lazy_for_in_class`.
  - Regular and lazy generators now include inherited typed-class and interface data fields in bounded `for...in` enumeration, preserving base-before-derived field order and deduplicating overridden names across multi-level class chains, with derived class-field and interface overrides plus inherited interfaces covered in both lowering paths; broader prototype enumeration and async iteration remain deferred. Tests: `for_in`, `generator_lazy_for_in_class`.
  - Lazy generator suspended expressions now support bounded optional array, string, Buffer, and dynamic property/element access, returning the appropriate undefined/nullish result without dereferencing a nullish receiver; broader optional-chain graphs and side-effecting computed indices remain deferred. Test: `generator_lazy_yield_access`.
  - Lazy generator suspended expressions now short-circuit bounded optional dynamic calls and member calls with spread arguments, evaluating spread sources only for non-nullish targets. Test: `generator_lazy_yield_call_new`.
  - Finite lazy multi-yield returns now preserve initialized mutable `let` locals mutated between suspension points. Test: `generator_lazy_multi_yield_return`.
  - Finite lazy multi-yield returns now preserve simple uninitialized `let` locals assigned before use after a suspension point. Test: `generator_lazy_multi_yield_return`.
  - Finite lazy multi-yield returns now preserve simple uninitialized `var` locals assigned before use after a suspension point. Test: `generator_lazy_multi_yield_return`.
  - Finite lazy multi-yield returns now preserve initialized mutable `var` locals across suspension points. Test: `generator_lazy_multi_yield_return`.
  - Finite lazy multi-yield returns now evaluate direct no-yield call/new/member-call leaves, including widened rest-parameter spread calls, at the final resume, preserving side-effect timing. Test: `generator_lazy_multi_yield_return`.
  - Finite lazy multi-yield returns now verify parameter-held and top-level lifted-arrow function-value calls as direct no-yield leaves at the final resume, preserving invocation timing. Test: `generator_lazy_multi_yield_return`.
  - Finite lazy multi-yield returns now inline bounded zero-argument arrow/function-expression calls with a single yield-free return at the final resume; yielding and otherwise unsupported inline closures remain deferred. Test: `generator_lazy_multi_yield_return`.
  - Finite lazy multi-yield returns now also preserve inline arrow/function-expression calls with simple typed parameters and yield-free bodies at the final resume; nested and otherwise complex destructured parameters plus yielding closures remain deferred, while non-literal or parameter-referencing defaults remain deferred. Test: `generator_lazy_multi_yield_return`.
  - Finite lazy multi-yield returns now also inline captured zero-argument arrow/function-expression calls with a single yield-free return, carrying referenced outer captures in the lazy generator environment; yielding and otherwise unsupported captured closures remain deferred. Test: `generator_lazy_inline_captured_closure`.
  - Finite lazy multi-yield returns now also preserve captured inline arrow/function-expression calls with simple typed parameters and yield-free bodies, carrying their capture cells through the lazy generator environment. Test: `generator_lazy_inline_captured_parameter`.
  - Finite lazy multi-yield returns now also preserve inline arrow/function-expression calls with simple typed rest parameters and direct or typed-array spread arguments, including captured rest closures; non-literal or parameter-referencing defaults, object/nested destructuring, and yielding closures remain deferred. Test: `generator_lazy_inline_rest_parameter`.
  - Finite lazy multi-yield returns now also preserve inline arrow/function-expression calls with simple and nested array-destructured parameters, including captured array-destructured closures; object and otherwise complex destructuring remains deferred. Tests: `generator_lazy_inline_array_destructure`, `generator_lazy_inline_nested_array_destructure`.
  - Finite lazy multi-yield returns now also preserve inline arrow/function-expression calls with flat, nested, and mixed static-key object/array-destructured parameters, side-effect-free computed property names, and literal binding defaults, including captured destructured closures; rest, non-literal/parameter-referencing defaults, dynamic computed names, and otherwise complex destructuring remain deferred. Tests: `generator_lazy_inline_object_destructure`, `generator_lazy_inline_nested_object_destructure`, `generator_lazy_inline_mixed_destructure`, `generator_lazy_inline_destructure_defaults`, `generator_lazy_inline_computed_destructure`.
  - Finite lazy multi-yield returns now also preserve identifier-only array/object rest bindings in inline destructured closures, including nested and captured closures; rest defaults and otherwise complex rest destructuring remain deferred. Tests: `generator_lazy_inline_destructure_rest`, `generator_lazy_inline_nested_destructure_rest`.
  - Finite lazy multi-yield returns now also preserve inline arrow/function-expression calls with simple literal default parameters, including omitted arguments at the final resume; side-effect-free arithmetic/conditional, stable property/element access, template, and static array/object literal defaults are covered separately, while calls, later-parameter references, spreads/methods, and other non-literal defaults remain deferred. Tests: `generator_lazy_inline_default_parameter`, `generator_lazy_inline_parameter_default_expression`, `generator_lazy_inline_parameter_default_access`, `generator_lazy_inline_parameter_default_template`, `generator_lazy_inline_parameter_default_literal`.
  - Finite lazy multi-yield returns now evaluate direct no-yield postfix-update leaves at the final resume, preserving side-effect timing. Test: `generator_lazy_multi_yield_return`.
  - Finite lazy multi-yield returns now evaluate direct no-yield prefix-update leaves at the final resume, preserving side-effect timing. Test: `generator_lazy_multi_yield_return`.
  - Finite lazy multi-yield returns now evaluate direct no-yield delete leaves at the final resume, preserving property-deletion timing. Test: `generator_lazy_multi_yield_return`.
  - Finite lazy multi-yield returns now evaluate direct no-yield assignment leaves at the final resume, preserving mutation timing. Test: `generator_lazy_multi_yield_return`.
  - Finite lazy multi-yield returns now evaluate bounded no-yield string/string and dynamic-value logical leaves for `&&`, `||`, and `??` at the final resume, preserving short-circuit/nullish side-effect timing. Test: `generator_lazy_multi_yield_return`.
  - Finite lazy multi-yield returns now also preserve homogeneous numeric and boolean operands for no-yield `&&` / `||` leaves, including short-circuiting the right operand at the final resume. Test: `generator_lazy_multi_yield_return`.
  - Finite lazy multi-yield returns now also preserve same-kind pointer operands for no-yield `??` leaves, including nullable and non-null array fallback timing. Test: `generator_lazy_multi_yield_return`.
  - Finite lazy multi-yield returns now also preserve definitely non-null same-kind pointer operands for no-yield `&&` / `||` leaves, including array fallback timing; nullable pointer logical operands remain deferred. Test: `generator_lazy_multi_yield_return`.
  - Finite lazy multi-yield returns now preserve BigInt zero/non-zero truthiness for no-yield `&&` / `||` leaves, including right-hand short-circuit timing. Test: `generator_lazy_multi_yield_return`.
  - Finite lazy multi-yield returns now preserve bounded optional property and element leaves across suspension points, including nullish receivers and final comparisons. Test: `generator_lazy_multi_yield_return`.
  - Finite lazy multi-yield returns now evaluate direct no-yield conditional leaves at the final resume, preserving branch and side-effect timing. Test: `generator_lazy_multi_yield_return`.
  - Finite lazy multi-yield returns now evaluate direct no-yield template-expression leaves at the final resume, preserving interpolation side-effect timing. Test: `generator_lazy_multi_yield_return`.
  - Finite lazy multi-yield returns now evaluate direct no-yield tagged-template leaves at the final resume, preserving tag side-effect timing. Test: `generator_lazy_multi_yield_return`.
  - Finite lazy multi-yield returns now evaluate no-yield array/object literal leaves at the final resume, including property reads from those literals. Test: `generator_lazy_multi_yield_return`.
  - Finite lazy multi-yield returns now evaluate no-yield regular-expression literal leaves at the final resume, including property reads from those literals. Test: `generator_lazy_multi_yield_return`.

- **Phase 9 — `Proxy` + `Reflect`**
  - Remaining work: broader ECMAScript proxy edge-case validation beyond the currently covered callable/newable/object/array/function identity and forwarding paths, revocation paths, handler and inherited-trap lookup, nullish/accessor trap forwarding and validation, receiver identity, Object helper / JSON / Object.assign integration, own-key and descriptor result validation, non-configurable/non-extensible target invariants, array/function/prototype/integrity invariant subsets, construct `newTarget` identity/default/prototype paths, Reflect first-class method/missing-argument/ignored-argument validation, Reflect apply/construct arguments-list validation, and Reflect object/property target validation paths.

---

## 3. Broad surface areas

- **Phases 11–13 — async Node stdlib**
  - Phase 11 remaining work: true libuv-backed asynchronous `fs.promises` I/O beyond the currently documented sync-backed immediate/event-loop-settled `readFile` / `writeFile` / `appendFile` / `readdir` / `access` / `stat` / `lstat` / `statfs` / copy/rename/cp/mutation/link/symlink/chown/chmod/truncate/time subsets; broader filesystem option objects beyond the covered encoding, flag, recursive, stat, statfs, `cp`, signal, path-like, and ignored-argument subsets; broader `events` async iterator helpers beyond the bounded `events.on(...)` async-iterator and `events.once(...)` Promise/options subsets; full readable/writable/transform/pipe stream behavior beyond bounded process stdio metadata/write/state/predicate/no-op compatibility subsets; broader `net` sockets/connect/listen APIs beyond the covered `isIP` / `isIPv4` / `isIPv6` address-classification and `SocketAddress` constructor/parse subsets; broader `dns` options beyond the covered callback and promise `lookup`, `lookupService`, `resolve4`, `resolve6`, default-result-order, hints/order/verbatim, TTL-false, import-style, option-form, and bounded callback/promise ordering subsets; and libuv-backed scheduling beyond the bounded nextTick, queueMicrotask, Promise microtask, zero/nonzero timeout, immediate, and interval scheduler subsets.
  - Phase 12 remaining work: `http` / `https` on OpenSSL plus best-effort `http2`.
  - Phase 13 remaining work: real async child-process lifecycle handles/events/streams beyond the currently documented `exec` / `execFile` callback and sync-backed `execSync` / `execFileSync` / `spawnSync` subsets; `fork`; broader `spawn` / `spawnSync` options beyond the covered cwd/env/shell/input/encoding/buffer/maxBuffer/timeout/killSignal/stdio/detached/uid/gid/windows/argv0/error-metadata subsets; `cluster`; and `worker_threads` with structured-clone messages.

- **Phase 14 — npm integration**
  - Remaining work: arbitrary dynamic computed named-export metadata beyond the currently documented finite key subsets for direct dynamic alternatives, JSDoc/single/union literal keys, static string-array composition, tuple/object-map checker-proven element access, object enumeration helpers, `Object.fromEntries(...)` finite key alternatives, and manifest-proven AOT key sets; remaining runtime-computed whole-value `module.exports = ...` expression forms beyond the currently documented boxed AOT default subsets for `typeof` / `void` / `undefined` / BigInt / RegExp literal defaults, binary/conditional, prefix-unary, prefix-update, assignment, `in` / `instanceof`, array spread, `this`, inline-function logical/nullish/conditional, postfix-update, delete, comma, call/new/member/template/tagged-template dynamic-object defaults, supported `Object.defineProperties` / `Object.setPrototypeOf` / `Object.preventExtensions` / `Object.seal` / `Object.freeze` wrappers, and bounded runtime-computed object-literal property / array element initializers with finite named-export metadata; deeper CommonJS wrapper behavior beyond the currently documented finite static wrapper subsets for module aliases/destructuring, TypeScript-only require/module-require assertion wrappers, IIFE and unary-IIFE parameter aliases, factory call/apply/Reflect.apply/bind invocation, static require-argument factory wrappers, and zero-argument object-returning factory/IIFE wrappers including object-preserving wrappers; broader finite dynamic-require specifier proof classes beyond the currently documented finite AOT subsets for RegExp metadata/string-RegExp method fragments, Date parse/UTC fragments, URL metadata and credential/searchParams fragments, static `path.basename` / `dirname` / `extname` fragments, static `querystring.escape` / `unescape` fragments, static string-backed `util.format` fragments, and ArrayBuffer/DataView and TextEncoder/TextDecoder fragments; broader pure-JS package-source patterns beyond the documented `allowJs` ESM package-local import/re-export and package-local/external CommonJS object-literal dependency paths; and broader mixed CJS/ESM interop edge cases beyond the documented CommonJS default/named/namespace default interop and JavaScript ESM import/re-export/namespace-import paths over package-local or external CommonJS object-literal dependencies. Dependency init stays eager through the AOT module graph by design.

- **Phase 15 — perf & polish** (ongoing)
  - Remaining work: deeper hidden-class cache work beyond the current shared shape-tree/property-cache validation subset; broader escape analysis for dynamic objects, escaping closures, returns, interprocedural ownership, dynamic or non-finite loop growing array methods, and escaping receiver-returning mutating array method result flows beyond the currently documented typed object/class and typed array literal stack-allocation subsets for direct locals, same-block aliases and assignments, mutations, iteration, helper calls, selected object helper/prototype/valueOf paths, unrelated nested functions, and closure-capture rejection; broader source-level generated-C DCE beyond the current documented unreferenced top-level/local binding, class/lifted-arrow, selected dead branch/reference pruning, pure primitive/string/number/BigInt/RegExp/Date/URL/URLSearchParams/Promise/collection/object/Reflect/array-helper static-expression, and fresh allocation/helper-call pruning subsets; further binary-size optimization; and CI matrix expansion beyond Linux gcc/clang to macOS and Windows.
