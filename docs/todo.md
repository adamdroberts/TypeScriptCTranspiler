# TODO — remaining work

Scope of the full project (from the approved plan): **full ECMAScript 2026 + transpile arbitrary `node_modules`**.

Items are grouped by how soon they unblock the most user value. Within each group the ordering reflects dependency order and implementation leverage. Completed work belongs in [`done.md`](done.md), not in this TODO file.

---

## 1. Next-up unblockers

This is the next item that most directly expands what programs can be written against `tsc2c`.

- **Phase 6 — `async`/`await` + libuv event loop**
  - Remaining work: general multi-suspension `await` state-machine lowering beyond the current bounded direct tail-await, awaited-alias, assignment-awaited-alias, single-await, embedded return-await, local initializer-return, assignment-return, assigned-prelude awaited-local return, source `try`/`catch`/`finally`, branch-block awaited-local return including branch-local initialized and assigned preludes plus synchronous post-await locals/control-flow and terminal post-await `throw`, branch-block source `try` / `catch` / `finally` awaited returns, and branch-block leading awaited-local chains, and leading awaited-local continuation subsets; the direct leading awaited-local chain subset is now verified through ten awaited locals and await-free expressions plus initialized or assigned locals carried between multiple suspension points, while broader `await` inside nested control-flow constructs and loops remains beyond the covered single-await post-await synchronous statement subsets for methods/declarations/function values, branch-block awaited-local returns with branch-local initialized/assigned preludes and post-await locals/`if`/`switch`/loop return/break/continue plus post-await `try`/`catch`/`finally` before final return and terminal post-await `throw` beyond the covered branch-arm and fallthrough-tail post-`await` if/try/switch/loop-return/loop-control/terminal-throw subsets, source `try`/`catch`/`finally` awaited returns beyond the covered direct `return await` source/try/catch/finally prelude-routing subset, combined catch-plus-finally routing/prelude/finally-throw override subset, branch-block source-try branch subset, and branch-block source-try fallthrough-tail subset, direct `return await` beyond the covered branch-local and fallthrough-tail local/expression-statement prelude subsets, branch-block fallthrough-tail embedded-await return expressions beyond the covered single-embedded-await, template embedded-await, conditional mixed sync/await, and short-circuit RHS-/left-operand-await subsets, and branch-block leading awaited-local chains beyond the covered void-sequencing, six-await fallthrough-tail, and ten-await branch/fallthrough subsets; broader awaited expressions outside the current leading local declaration, expression-statement, `return await`, and source `try` recognizer edge cases beyond the covered transparent fulfilled/catch return, catch throw, catch-prelude wrapper, and terminal post-await throw subset; broader locals that must survive across general suspension points beyond the current primitive, dynamic, typed Promise, typed array/object/class/function, typed collection, typed RegExp, typed Node crypto/events/fs object, Event/EventTarget, Date/Error/AggregateError, URL/URLSearchParams, Buffer/ArrayBuffer/DataView, TextEncoder/TextDecoder, and symbol prelude captures; broader thenable assimilation edge cases outside the current immediate and microtask-delayed settled/nested/self-resolution/cycle coverage and dynamic `Promise.all`/`race`/`any`/`allSettled` iterable input shapes beyond covered delayed nested-array, delayed nested-Set, Map entry-iteration, string iterable, generator-backed iterable, custom iterable class including inherited `[Symbol.iterator]`, custom iterator-object class, and direct self-iterable iterator object cases; full async function edge cases beyond the current declaration, instance/static class method, object-literal method, named/lifted function-value, closure function-value, block-bodied async-arrow function-value, and expression-bodied async-arrow continuation routing; and future async I/O on top of libuv.

  - Verified bounded leading-chain extension: await-free `if` statements are now accepted between suspension points when their branches contain no nested await or declarations; general nested control flow and awaited branch bodies remain deferred.
  - Direct awaited `if` conditions in bounded async declarations, methods, and function values now select the branch after the awaited boolean settles; the same subset also accepts an awaited condition nested under an await-free outer `if`, while broader nested control flow remains deferred. Test: `async_await_if_condition`.
  - Bounded pending-await continuation lowering now supports `while (await promise) return ...; return ...` and the equivalent initializer-free/incrementor-free `for (; await promise;) return ...; return ...` in async function declarations, methods, and function values, including one non-nested awaited operand inside logical, conditional, or boolean-valued nullish conditions, nullable `Promise<boolean | null>` operands coalesced with `??`, plus flat arbitrary-length direct-awaited `&&` / `||` logical conditions (verified through six operands), pending condition fulfillment and false fallthrough, receiver/parameter references in the condition and return arms, multiple await-free expression statements before the loop return, multiple await-free expression statements before one initialized loop-local `const` / `let` returned immediately, bounded await-free `if` / `else` branches inside the loop body, and bounded two-stage direct body `return await` or `throw await` after one awaited operand in a direct, logical, or nullish condition, including the exact transparent nested body form `return await (await promise)`, await-free expression and non-abrupt control-flow preludes with simple branch-local declarations, ordered initialized or assigned-before-use loop-local `const` / `let` body-promise locals, body rejection/throw propagation, and false fallthrough; broader loop-local state, awaited nested control flow, and general loop bodies remain deferred. Test: `async_await_while_condition`.
  - The bounded awaited loop-return subset now also covers a direct initialized `const` / `let` alias (`const value = await promise; return value;`) after the awaited condition. Test: `async_await_while_condition`.
  - The same bounded loop subset now covers direct initialized awaited-local aliases on `throw` (`const reason = await promise; throw reason;`); broader loop-local state remains deferred. Test: `async_await_while_condition`.
  - The bounded loop subset now also covers an awaited-local alias assigned after an uninitialized `let` declaration before direct `return` or `throw`; broader loop-local state remains deferred. Test: `async_await_while_condition`.
  - Awaited loop-local aliases may now feed a synchronous final return/throw expression after the suspension; broader post-await body state remains deferred. Test: `async_await_while_condition`.
  - The same loop alias subset now allows bounded await-free expression-statement postludes before the final return/throw expression; declarations and nested control flow after suspension remain deferred. Test: `async_await_while_condition`.
  - Assigned awaited-local aliases now share that bounded expression-statement postlude support; broader post-await local state remains deferred. Test: `async_await_while_condition`.
  - Bounded postludes may now include one initialized `const` / `let` local before the final alias expression; broader post-await declarations and control flow remain deferred. Test: `async_await_while_condition`.
  - Awaited-loop aliases now preserve multiple bounded await-free expression/local postlude statements in source order; broader post-await control flow remains deferred. Test: `async_await_while_condition`.
  - The same bounded postlude subset now accepts await-free non-abrupt control-flow statements such as `if`; abrupt branches and nested awaits remain deferred. Test: `async_await_while_condition`.
  - Bounded postludes now also support uninitialized `let` locals assigned synchronously before the final alias expression; uninitialized `const` and broader local state remain deferred. Test: `async_await_while_condition`.
  - Awaited loop locals may now be used only as bounded sequencing points before the final return/throw expression; broader ignored-local and multi-suspension state remains deferred. Test: `async_await_while_condition`.
  - Awaited loop aliases may now be synchronously mutated in bounded postludes before the final return/throw; broader mutable post-await state remains deferred. Test: `async_await_while_condition`.
  - The same bounded interstitial extension now covers await-free `switch` statements without branch-local declarations; awaited branch bodies and general state-machine lowering remain deferred.
  - The bounded interstitial extension also covers await-free `while` / `do` / `for` loop statements without loop-local declarations; loops containing awaits or declaration lists remain deferred.
  - Single-statement `throw await promise` bodies now use the leading continuation adapter, preserving declaration, method, and async-arrow receiver/parameter evaluation before the suspension and rejecting with the fulfilled awaited value; broader throw-await expression graphs remain deferred.
  - Leading chains now also support a terminal `throw await promise` after an earlier awaited local, carrying the earlier awaited value into the final source across declarations, methods, function values, and branch-block arms; broader throw-await expression graphs remain deferred.
  - Terminal `throw await promise` chains may now begin after await-free captured local or expression preludes across declarations, methods, and async-arrow function values; broader pre-suspension mutable state remains deferred.
  - Conditional leading await branches now join a common terminal `throw await promise` across declarations, methods, and async-arrow function values; broader conditional throw-await expression graphs remain deferred.
  - Unequal conditional leading await branches now also join a common terminal `throw await promise` after bounded divergent sequencing lengths across declarations, methods, and async-arrow function values; broader unequal throw-await graphs remain deferred.
  - Single terminal throw expressions with one bounded non-conditional awaited operand now use the expression continuation adapter and reject with the transformed value across declarations, methods, and async-arrow function values; multiple awaited operands and conditional/short-circuit throw expressions remain deferred.
  - The same single-awaited throw-expression subset now preserves await-free captured local preludes across declarations, methods, and async-arrow function values; broader mutable pre-suspension throw state remains deferred.
  - Terminal throw expressions now also support two left-associated non-conditional awaited operands through the staged expression adapter across declarations, methods, and async-arrow function values; larger or conditional throw expression trees remain deferred.
  - The staged terminal throw-expression subset now extends to three left-associated non-conditional awaited operands across declarations, methods, and async-arrow function values; larger or conditional throw expression trees remain deferred.
  - The staged terminal throw-expression subset now extends to five-or-more left-associated non-conditional awaited operands through the generic sequence adapter across declarations, methods, and async-arrow function values; bounded two-arm conditional and `&&` / `||` / `??` short-circuit throw expressions are also covered, while broader conditional and short-circuit throw trees remain deferred.
  - The bounded conditional throw-expression subset now also preserves await-free captured local preludes across declarations, methods, and async-arrow function values; broader mutable conditional throw state remains deferred.
  - Source `try` / `catch` now catches bounded single-, flat three-await short-circuit, and flat five-await non-short-circuit throw expressions through the staged sequence adapter, including source rejection handoff and transformed catch recovery across declarations, methods, and async-arrow function values; broader awaited throw/catch graphs remain deferred.
  - Source `try` / `catch` awaited throw expressions now preserve bounded initialized local preludes inside the try block across declarations, methods, and async-arrow function values; broader mutable try-local state remains deferred.
  - The same source `try` / `catch` awaited throw-expression subset now preserves bounded await-free expression-statement preludes inside the try block; nested awaits and broader control-flow preludes remain deferred.
  - The same subset now also preserves bounded await-free non-abrupt `if` / `switch` / loop / `try` control-flow preludes inside the try block; declarations, abrupt paths, nested awaits, and broader state-machine control flow remain deferred.
  - Source `try` / `catch` awaited throw expressions now also preserve bounded await-free non-abrupt `if` / `switch` / loop / `try` control-flow preludes before catch recovery; declarations, abrupt paths, nested awaits, and broader catch state remain deferred.
  - Combined source `try` / `catch` / `finally` awaited-return chains now preserve the same bounded await-free non-abrupt control-flow preludes before catch recovery; declarations, abrupt paths, nested awaits, and broader combined exception state remain deferred.
  - Combined source `try` / `catch` / `finally` now catches bounded single-, flat three-await short-circuit, and flat five-await non-short-circuit throw expressions, routing transformed values and source rejections through catch recovery before finally execution; broader combined awaited throw graphs remain deferred.
  - Source `try` / `finally` now preserves bounded single-, flat three-await short-circuit, and flat five-await non-short-circuit throw expressions, executing finally before rejecting with the transformed value or original awaited rejection; broader try/finally awaited throw graphs remain deferred.
  - Branch-block fallthrough returns now support bounded short-circuit `&&` / `||` / `??` expressions with a direct awaited left operand and synchronous RHS across declarations, methods, and function values; multiple awaits and broader branch short-circuit graphs remain deferred.
  - Branch-block fallthrough returns now also support bounded short-circuit `&&` / `||` / `??` expressions with direct awaits on both operands, short-circuiting after the first fulfillment or sequencing the second await; longer flat chains are covered below, while broader nested or structurally mixed short-circuit graphs remain deferred.
  - Branch-block fallthrough returns now support flat left-associated `&&` / `||` / `??` chains with three-or-more direct awaited operands through the generic sequence adapter, validated through five awaits, short-circuiting at each fulfilled stage or continuing to the next await; broader nested or structurally mixed short-circuit graphs remain deferred.
  - Branch-block fallthrough conditional return expressions now compose that flat three-or-more-await short-circuit subset in their selected arms across declarations, methods, and function values; broader nested, unequal, or structurally mixed conditional short-circuit graphs remain deferred.
  - Terminal throw expressions now also support bounded short-circuit `&&` / `||` / `??` trees with direct awaits on both operands, including first-operand short-circuit rejection and second-operand awaited rejection; longer flat chains are covered below, while broader nested or structurally mixed short-circuit trees remain deferred.
  - Terminal throw expressions now support flat left-associated `&&` / `||` / `??` chains with three-or-more direct awaited operands through the generic sequence adapter, validated through five awaits, preserving short-circuit rejection values and later-await rejection values; broader nested or structurally mixed short-circuit trees remain deferred.
  - Conditional terminal throw expressions now compose that flat three-or-more-await short-circuit subset in each selected arm across declarations, methods, and function values; broader nested, unequal, or structurally mixed short-circuit conditional trees remain deferred.
  - Branch-block fallthrough returns now support flat non-short-circuit expression sequences with two, three, and five-or-more direct awaited operands in conditional arms through the staged/generic sequence adapters; nested or structurally broader branch embedded-await graphs remain deferred.
  - Branch-block fallthrough returns now also cover supported structural multi-await expressions through the generic sequence adapter, including array elements, object-literal properties, multi-await call arguments, and multi-await template expressions; dynamic arrays are boxed for generic Promise callbacks while typed array returns retain their native representation, and unsupported structural forms plus broader branch embedded-await graphs remain deferred.
  - Async source `try` / `catch` / `finally` recognizers now safely decline blocks consumed entirely by await-free preludes; empty post-prelude try bodies no longer crash recognition and remain deferred. Regression: `async_await_branch_return_await_try_prelude`.
  - Leading chains now also accept bare `Promise<void>` sequencing awaits between awaited locals; general awaited expressions and nested awaited control flow remain deferred.
  - Await-free interstitial `try` / `catch` / `finally` statements with simple initialized or assigned-before-use `const` / `let` locals are now covered; source try blocks containing awaited returns and broader declaration/destructuring forms remain deferred.
  - Leading chains now cover a terminal direct `return await`; general direct-return-await control-flow and nested awaited expressions remain deferred.
  - Direct branch `return await` arms now preserve safe initialized branch-local declaration preludes across declarations, methods, function values, and branch-block fallthrough; awaited, escaping, and broader branch return-await graphs remain deferred.
  - The same direct branch `return await` subset now preserves branch-local `let` declarations assigned before the awaited source; assignments after suspension and broader mutable branch state remain deferred.
  - Direct `return await` with await-free expression-statement and bounded control-flow preludes is covered for async declarations; nested awaits and broader state-machine forms remain deferred.
  - The same direct-prelude `return await` subset is covered for class methods and function values; broader nested-await forms remain deferred.
  - Direct-prelude `return await` accepts await-free initialized locals and uninitialized assignments before the awaited source; general locals crossing nested suspension points remain deferred.
  - Direct branch `return await` now covers bounded `switch` statements with a terminal or reordered `default`, empty fallthrough case labels, and terminating clauses across async declarations, methods, and function values; suspension-bearing case expressions, unresolved trailing fallthrough, and broader switch state remain deferred.
  - Direct branch `return await` now also preserves captured locals mutated by await-free `while`, `do while`, `for`, `for-in`, and `for-of` preludes with simple initialized or assigned-before-use `const` / `let` locals plus initialized `var` locals used within the control-flow body across declarations, methods, and function values; destructuring, uninitialized or escaping `var`, nested awaits, and broader loop state remain deferred.
  - Direct branch `return await` now also preserves captured locals mutated by an await-free `try` / `finally` prelude across declarations, methods, and function values; awaited exception paths and broader branch exception state remain deferred.
  - Branch-local source `try` / `catch` now routes direct `return await` fulfillment and rejection paths plus flat five-await throw recovery across declarations, methods, and function values; broader nested branch exception graphs remain deferred.
  - Branch-local source `try` / `finally` now runs finally bodies on direct `return await` fulfillment and rejection paths plus bounded single-, flat three-await short-circuit, and flat five-await non-short-circuit throw paths across declarations, methods, and function values; broader nested finally suspension graphs remain deferred.
  - Branch-local source `try` / `catch` / `finally` now composes direct `return await` fulfillment, catch recovery, finally routing, bounded finally-throw overrides, and flat five-await throw recovery across declarations, methods, and function values; broader nested exception graphs remain deferred.
  - The exact nested `return await (await promise)` form and one embedded inner await in a non-Promise outer expression are covered across declarations, methods, and expression-bodied async arrows; nested awaits embedded in broader expression trees and general multi-suspension expressions remain deferred.
  - Bounded two-, three-, and four-await staged inner expressions, bounded two-await `&&` / `||` / `??` inner expressions, and flat five-await non-short-circuit inner expressions with a final stable synchronous leaf are covered in direct `return await` operands for non-Promise outer expressions, including closure captures in expression-bodied async arrows; broader nested suspension trees remain deferred.
  - Nested direct `return await` now also covers bounded multi-await template expressions and call argument expressions across declarations, methods, and expression-bodied async arrows; broader structural suspension trees remain deferred.
  - Nested direct `return await` now also covers bounded multi-await array elements and object-literal properties across declarations, methods, and expression-bodied async arrows; broader structural suspension trees remain deferred.
  - Nested direct `return await` now also covers awaited property receivers, dynamic element indices, and bounded multi-await constructor arguments across declarations, methods, and expression-bodied async arrows; broader structural suspension trees remain deferred.
  - Nested direct `return await` now also covers bounded multi-await tagged-template substitutions and awaited dynamic call targets, including typed function values with or without an explicit `this` parameter, across declarations, methods, and expression-bodied async arrows; broader structural suspension trees remain deferred.
  - Nested direct `return await` now also covers an awaited typed member receiver combined with bounded multi-await method arguments across declarations, methods, and expression-bodied async arrows; broader structural suspension trees remain deferred.
  - Nested direct `return await` now also covers awaited dynamic constructor targets, including typed class values, combined with bounded multi-await constructor arguments across declarations, methods, and expression-bodied async arrows; broader structural suspension trees remain deferred.
  - Conditional nested direct `return await` now also covers selected multi-await array, call, template, and object-literal arms across declarations, methods, and expression-bodied async arrows; broader structural branch graphs remain deferred.
  - Conditional nested direct `return await` now also covers selected two-await `&&` / `||` short-circuit arms across declarations, methods, and expression-bodied async arrows; broader structural branch graphs remain deferred.
  - Conditional nested direct `return await` now also covers flat three-await `&&` / `||` short-circuit arms across declarations, methods, and expression-bodied async arrows; broader structural branch graphs remain deferred.
  - Conditional nested direct `return await` now also covers flat three-await `??` short-circuit arms across declarations, methods, and expression-bodied async arrows, including typed nullable-union awaited values; broader structural branch graphs remain deferred.
  - Conditional nested direct `return await` now also covers selected structural element-access arms and awaited constructor-property arms across declarations, methods, and expression-bodied async arrows; broader structural branch graphs remain deferred.
  - Conditional nested direct `return await` now also covers awaited property receivers and dynamic element indices in selected arms across declarations, methods, and expression-bodied async arrows; broader structural branch graphs remain deferred.
  - Conditional nested direct `return await` now also covers awaited dynamic callable targets and awaited member receivers combined with multi-await call arguments in selected arms across declarations, methods, and expression-bodied async arrows; broader structural branch graphs remain deferred.
  - Conditional nested direct `return await` now also covers recursively nested conditional selections of multi-await array and object arms across declarations, methods, and expression-bodied async arrows; broader structural branch graphs remain deferred.
  - Conditional nested direct `return await` now also covers awaited dynamic constructor targets with multi-await constructor arguments in selected arms across declarations, methods, and expression-bodied async arrows; broader structural branch graphs remain deferred.
  - Conditional nested direct `return await` now also covers nested structural arms in static class methods and object-literal async methods; broader async function edge cases remain deferred.
  - Conditional nested direct `return await` now also covers nested structural arms in lifted and named async function values; broader async function edge cases remain deferred.
  - Exact two-, three-, and four-sibling-await forms plus left-associated five-or-more sibling-await binary expressions, literal leaves, side-effect-free `void` leaves, mixed literal/direct-await array elements and object properties, mixed literal/direct-await template and tagged-template substitutions, `typeof` over literal leaves including BigInt and regular expressions plus side-effect-free `void` literals, mixed literal/direct-await call and constructor arguments including directly awaited constructor callees, literal-index/direct-await element access, multi-await template substitutions, tagged-template substitutions, multi-await array literals, direct-await call arguments, direct-awaited dynamic call targets, direct-awaited property receivers and element indices, supported await-bearing member and constructor callees, side-effect-free prefix and `typeof` operators around direct awaits, typed object-literal properties, constructor arguments, and supported await-bearing nested call operands are covered; other non-binary deeper expression trees and branch-dependent multi-await expressions remain deferred.
  - Conditional direct awaits inside an outer `return await` are covered through recursively nested bounded trees, including mixed sync/await arms and expression-bodied async-arrow routing; bounded recursive `&&` / `||` / `??` suspension branches are also covered, while broader short-circuit branch graphs remain deferred.
  - Direct-prelude `return await` now covers await-free `if`, `switch`, loop, and `try` / `finally` statements with simple initialized or assigned-before-use `const` / `let` locals plus initialized `var` locals used within those control-flow statements, including locals scoped inside branches; nested awaits, destructuring, uninitialized or escaping `var`, and broader declaration-bearing control flow remain deferred.
  - Leading awaited-local chains now support a terminal post-await `throw` after multiple suspensions for async declarations, class methods, and function values; broader awaited control-flow graphs remain deferred.
  - Leading awaited-local chains now also accept initialized or uninitialized `let` aliases assigned from awaited promises across multiple suspension points, including repeated assignment to the same alias; broader mutable-local state machines remain deferred.
  - Leading awaited-local chains now support bounded conditional await steps with Promise-valued true and false branches across async declarations, class methods, and function values; incomplete or unequal nested awaited branch trees and broader suspension-aware control flow remain deferred.
  - Nested complete `if` / `else` trees now flatten into bounded equal-length conditional await steps across async declarations, class methods, and function values; unequal nested trees are supported when they join a bounded common synchronous return, while incomplete trees and broader suspension-aware control flow remain deferred.
  - Branch-block leading awaited-local chains now compose the bounded conditional await-step subset with a synchronous fallthrough arm; broader branch-local suspension graphs remain deferred.
  - Branch-block leading awaited-local chains now compose flattened nested conditional await selections of arbitrary recognized depth with a synchronous fallthrough arm across async declarations, class methods, and function values; deeper branch-local suspension graphs remain deferred.
  - Leading awaited-local chains now flatten nested `else if` conditional await selections of arbitrary recognized depth with Promise-valued leaves; nested awaited branch bodies with unequal or more general suspension graphs remain deferred.
  - Nested conditional leading await steps now preserve await-free arm preludes, equal-length sequential awaited assignments, branch-local value flow, and post-await arm state across flattened conditional selections of arbitrary recognized depth before the next suspension; unequal-length or broader awaited arm graphs remain deferred.
  - Unequal conditional leading await arms now support bounded branch-local sequencing chains that join a common synchronous return, including the covered two-await versus one-await shape across declarations, methods, and function values; unequal graphs with escaping branch state or broader joins remain deferred.
  - The same unequal shared-tail subset now threads captured outer-state updates across divergent await counts before the common return, including complete nested conditional trees that preserve outer state while joining bounded common synchronous, direct `return await`, or awaited-local tails; broader mutable branch state and nested joins remain deferred.
  - Unequal conditional branches now also join a bounded common awaited-local tail before the final return, preserving divergent two-await versus one-await sequencing across declarations, methods, and function values; broader common-tail control flow remains deferred.
  - Unequal conditional branches now also join a bounded common terminal `throw` after divergent await counts across declarations, methods, and function values, including complete nested conditional trees; broader throw/control-flow joins remain deferred.
  - Conditional leading await steps now preserve bounded await-free expression and initialized or assigned-before-await branch-local declaration preludes in both direct arms across declarations, methods, and function values; awaited, escaping, or broader branch preludes remain deferred.
  - Conditional leading await steps now allow branch-local awaited declarations used only as sequencing points; bounded updates to outer captured state are covered below, while broader branch-local escapes remain deferred.
  - Conditional leading await steps now compose equal-length sequences of branch-local awaited declarations used only as sequencing points across declarations, methods, function values, and branch fallthrough; escaping values, unequal-length arms, and broader branch graphs remain deferred.
  - Conditional leading await steps now allow a branch-local first awaited value to feed the next branch-local await expression when that later step is sequencing-only; values that must persist beyond that next suspension remain deferred.
  - The same branch-local value-flow subset now composes across multiple consecutive conditional suspensions, with each branch-local result feeding the next sequencing-only await; values that must survive beyond the next branch adapter remain deferred.
  - The branch-local value-flow subset now also composes a branch-local awaited declaration arm with a sibling direct-await sequencing arm across declarations, methods, function values, and branch fallthrough; broader mixed branch graphs remain deferred.
  - Conditional arm post-await state now permits a branch-local awaited value to update an outer captured local before the next sequencing await across declarations, methods, function values, and branch fallthrough; broader branch-local state graphs remain deferred.
  - Conditional arm post-await state now also permits the final branch-local awaited value to update outer captured state before the common return across declarations, methods, function values, and branch fallthrough; broader branch-local terminal state graphs remain deferred.
  - The same sequencing subset now accepts a mixed direct-await arm and branch-local awaited declaration arm.
  - Conditional leading await steps now preserve bounded await-free post-await statements in both direct arms before the next suspension; final conditional steps and broader branch-local suspension graphs remain deferred.
  - Conditional leading await steps now also preserve bounded await-free post-await statements in the final conditional step before return across async declarations, class methods, and function values; broader branch-local suspension graphs remain deferred.
  - Await-free interstitial `try` / `catch` statements now allow simple catch bindings; awaited try/catch bodies and broader suspension-aware exception graphs remain deferred.
  - Await-free direct-`return await` preludes now also allow simple catch bindings; awaited source try/catch graphs remain deferred.
  - Source `try` / `catch` now supports a bounded leading multi-await chain in the try block across async declarations, class methods, and function values, including receiver capture, pending rejection handoff into the catch continuation, and bounded catch rethrows; broader suspension-aware exception graphs remain deferred.
  - Leading source `try` / `catch` chains now also support initialized local catch preludes before the final catch return/throw; broader catch-local state and suspension-aware exception graphs remain deferred.
  - Combined source `try` / `catch` / `finally` leading chains now also support initialized local catch preludes before catch recovery and finally routing; broader catch-local state and suspension-aware exception graphs remain deferred.
  - Source `try` / `finally` now supports a bounded leading multi-await chain in the try block across async declarations, class methods, and function values, including receiver capture, initialized and uninitialized `let` finally locals, repeated and compound local assignments, finally execution on fulfillment/rejection, finally-throw override, bounded finally-body await sequences with await-free expression/control-flow/try-catch interstitials and post-await statements, conditional finally awaits with Promise-valued branches, bounded synchronous return overrides after finally awaits across declarations, methods, and function values, and a bounded finally `return await` override across declarations, methods, and function values; deeper/nested finally suspension graphs and broader suspension-aware exception graphs remain deferred.
  - Combined source `try` / `catch` / `finally` now composes the bounded leading multi-await chain with catch recovery and finally routing, including finally-throw override; broader combined suspension-aware exception graphs remain deferred.

---

## 2. Dependent phases

- **Dispatch concurrency (optional, libdispatch-backed) — remainder** (design/status: [`dispatch_async_todo.md`](dispatch_async_todo.md))
  - Landed: `DispatchQueue` serial/concurrent queues, `dispatch.async` (Promise settled on the main loop) and `dispatch.sync` with deadlock detection, const-primitive capture discipline, error propagation, the `TSC_THREADS` thread-safety baseline (threaded Boehm GC with per-task worker registration, TLS exception state, locked shape/symbol/singleton slow paths, cross-thread completion queue, condvar event loop), usage-gated compilation/linking so non-dispatch programs are byte-identical, and e2e coverage with libdispatch-absent skip.
  - Remaining work: `Shared<T>`/queue-confinement capture escape hatches beyond const primitives plus auto-`structuredClone` of clonable captures; broader task return types beyond number/string/boolean/void; `dispatch.after`, dispatch groups as `Promise.all`-style joins, and concurrent-queue barriers; a `--dispatch=serial` no-libdispatch fallback mode; `--no-gc` interop (per-thread arena chunks); relaxing the awaited-shape dependence once Phase 6 general state-machine lowering lands (dispatch promises awaited outside the recognized continuation subsets currently hit the pending-await bail); a many-thread GC soak + TSan CI configuration and a CI job that builds libdispatch from source; and capture-discipline hardening for transitive mutable-global access through helper functions called inside tasks.

- **Phase 7 remainder — ES language features**
  - Remaining work: general lazy generator state-machine lowering beyond the current bounded sequential branch/switch/loop, single-suspended expression-tree, direct binary multi-yield return with literal leaves, compound-assignment, `return yield`, simple `.next(value)` assignment, suspended `throw (yield ...)`, and bounded lazy `yield*` subsets; captured identifiers, side-effecting leaves, non-binary operators, and otherwise arbitrary expression graphs beyond direct binary trees whose environments are sized for every finite yield count; `try` control flow across suspension points beyond the covered non-suspending source `try` / `catch` / `finally`, bounded normal-resume `try` / `finally`, and suspended throw-yield subsets; `async function*`; broader iterator protocol edge cases beyond the current array/string/Map/Set/custom/URLSearchParams/Buffer/dynamic and materialized/lazy generator-backed `yield*` subsets; and async iterables.
  - Typed lazy generators now preserve suspension when converted across compatible `Generator<...>` element representations in both boxing and unboxing directions, converting each yielded item on demand and forwarding bounded `.return(...)`/`.throw(...)` cleanup; broader iterator-object adaptation and arbitrary generator type conversions remain deferred.
  - Typed lazy generator conversion adapters now resume after a source lazy generator handles a forwarded `.throw(error)`, preserving the source return value; broader adapter protocol overrides remain deferred.
  - Lazy `yield*` now preserves delegated throw recovery when its source passes through simple typed generator conversion helpers, transparent direct-call wrappers, conditionals, nullish/logical fallbacks, or a bounded nested conversion chain, including the converted return value; broader passthrough and adapter graphs remain deferred.
  - Lazy `yield*` now preserves delegated throw recovery through a typed generator conversion arrow/closure passthrough, including the converted return value; broader callable passthrough graphs remain deferred.
  - Lazy generator `yield*` now preserves generator-backed delegation across suspended inner yields and forwards `.next(value)` one step at a time; broader delegated return/throw protocol overrides remain deferred.
  - Lazy `yield*` now preserves suspension and bounded delegated throw recovery for typed custom iterables whose `[Symbol.iterator]()` is a generator method; broader custom iterator protocol adaptation remains deferred.
  - Lazy `yield*` now pulls one result per resume from typed custom iterator objects whose `next()` returns `{ done, value }`, forwards a bounded zero- or one-argument `.next(value)`, preserves their terminal value, forwards bounded zero- or one-argument `.return()` / `.throw()` calls that complete or yield one immediate step, and propagates handled delegated return completions through the outer iterator; multi-step or suspending closure methods remain deferred.
  - Lazy generator `yield*` now carries the delegated iterator return value into bounded assignment and return expressions; delegated `throw`/`return` protocol overrides and broader result-flow graphs remain deferred.
  - Lazy `yield*` now forwards bounded outer `.return(value)` and `.throw(error)` closure into an active lazy delegated generator, resumes the outer generator when the delegated throw is caught, and unwinds nested simple `finally` cleanup; broader delegated protocol overrides remain deferred.
  - Lazy generators now route a terminal source `return` after suspended `try`-block yields through a non-suspending `finally`; broader abrupt control flow and suspension inside `finally` remain deferred.
  - Lazy generators also route a terminal literal source `throw` after suspended `try`-block yields through a non-suspending `finally`; catchable throws and side-effecting or dynamic throw expressions remain deferred.
  - Lazy generators now support bounded catch `if` / `else` direct literal/catch-value throws through a non-suspending `finally`; broader conditional catch control flow remains deferred.
  - Lazy generators now support bounded catch `if` / `else` mixed direct literal/catch-value return and throw arms through a non-suspending `finally`; broader conditional catch control flow remains deferred.
  - Lazy generators now support bounded non-suspending `finally` `if` / `else` overrides with direct literal return/throw arms, including mixed completion, on both suspended `.throw(...)` and normal completion; broader abrupt finalizer control flow remains deferred.
  - Lazy generators now catch a bounded `.throw(error)` at a suspended `try` block when the catch has no binding, a simple unused identifier, a simple identifier returned directly, a simple assigned/compound-assigned identifier, or a two-leaf string concatenation with that identifier, producing a completed iterator result; destructured used bindings, catch yields, and broader recovery graphs remain deferred.
  - Lazy generators now compose that bounded suspended catch recovery with a non-suspending `finally`, running finalization on both handled `.throw(error)` and normal completion and honoring terminal literal `throw` or `return` overrides; nested suspension, other abrupt finally paths, and broader recovery graphs remain deferred.
  - Lazy generators now execute bounded yield-free, non-abrupt catch expression-statement preludes plus simple string-literal or catch-binding `const` / `let` aliases before terminal catch recovery; nested suspension and broader recovery graphs remain deferred.
  - Lazy generators now preserve bounded chains of those simple catch-binding aliases, direct alias assignments, and simple string compound assignments before terminal recovery; broader catch-local state remains deferred.
  - Lazy generators now support a terminal literal catch `throw` after a suspended yield, including the bounded catch prelude and non-suspending `finally` ordering; dynamic catch throws and broader recovery graphs remain deferred.
  - Lazy generators now also rethrow a simple catch identifier directly after a suspended yield when no `finally` must carry the binding; compound/dynamic catch rethrows and broader recovery graphs remain deferred.
  - Lazy generators now carry a simple catch-identifier rethrow through a non-suspending `finally`, while preserving finalizer `throw` / `return` overrides; compound/dynamic catch rethrows and broader recovery graphs remain deferred.
  - Lazy generators now support two-leaf string composition in catch rethrows, including when the composed error is carried through a non-suspending `finally`; broader catch throw expressions remain deferred.
  - Lazy generators now support bounded three-leaf string composition around catch values in direct catch returns and rethrows, including through a non-suspending `finally`; broader catch expression graphs remain deferred.
  - Lazy generators now support bounded catch `if` / `else` recovery with direct literal/catch-value returns or throws, including mixed return/throw branches through a non-suspending `finally`; nested catch control flow and broader graphs remain deferred.
  - Lazy generator suspension now survives direct and aliased generator function-value calls through closure adapters; broader iterator-object adaptation and dynamic callable generator discovery remain deferred.
  - Lazy generators now cover bounded array- and string-backed `for...of` loops with suspension-safe iterator positions plus `break`/`continue`; `for...in`, async iteration, and broader iterator-source lowering remain deferred.
  - The bounded lazy `for...of` subset now also covers typed `Set` sources; typed `Map`, custom iterator objects, async iteration, and broader iterator-source lowering remain deferred.
  - The bounded lazy `for...of` subset now also covers typed `Map` sources as key/value entry pairs; custom iterator objects, async iteration, and broader iterator-source lowering remain deferred.
  - The bounded lazy `for...of` subset now also covers supported typed custom iterable/iterator classes; async iteration and broader iterator-source lowering remain deferred.
  - The bounded lazy `for...of` subset now also covers `URLSearchParams` key/value entries; async iteration and broader iterator-source lowering remain deferred.
  - The bounded lazy `for...of` subset now also covers typed Buffer byte iteration; async iteration and broader iterator-source lowering remain deferred.
  - Lazy `yield*` now covers typed `URLSearchParams` key/value entries and Buffer byte sources while preserving one-source-item suspension; broader iterator protocol and async-iterable lowering remain deferred.
  - Materialized generator `yield*` now covers typed `URLSearchParams` key/value entries and Buffer byte sources; broader iterator protocol and async-iterable lowering remain deferred.
  - Regular `for...of` and `for...in` lowering now covers typed Buffer byte values and numeric own keys; broader iterator protocol and async-iterable lowering remain deferred.
  - Lazy generators now cover bounded `for...in` loops over typed arrays, strings, Buffers, and dynamic values with suspension-safe key positions; async iteration and broader enumerable-source lowering remain deferred.

- **Phase 9 — `Proxy` + `Reflect`**
  - Remaining work: broader ECMAScript proxy edge-case validation beyond the currently covered callable/newable/object/array/function identity and forwarding paths, revocation paths, handler and inherited-trap lookup, nullish/accessor trap forwarding and validation, receiver identity, Object helper / JSON / Object.assign integration, own-key and descriptor result validation, non-configurable/non-extensible target invariants, array/function/prototype/integrity invariant subsets, construct `newTarget` identity/default/prototype paths, Reflect first-class method/missing-argument/ignored-argument validation, Reflect apply/construct arguments-list validation, and Reflect object/property target validation paths.

---

## 3. Broad surface areas

- **Phases 11–13 — async Node stdlib**
  - Phase 11 remaining work: true libuv-backed asynchronous `fs.promises` I/O beyond the currently documented sync-backed immediate/event-loop-settled `readFile` / `writeFile` / `appendFile` / `readdir` / `access` / `stat` / `lstat` / `statfs` / copy/rename/cp/mutation/link/symlink/chown/chmod/truncate/time subsets; broader filesystem option objects beyond the covered encoding, flag, recursive, stat, statfs, `cp`, signal, path-like, and ignored-argument subsets; broader `events` async iterator helpers beyond the bounded `events.on(...)` async-iterator and `events.once(...)` Promise/options subsets; full readable/writable/transform/pipe stream behavior beyond bounded process stdio metadata/write/state/predicate/no-op compatibility subsets; broader `net` sockets/connect/listen APIs beyond the covered `isIP` / `isIPv4` / `isIPv6` address-classification and `SocketAddress` constructor/parse subsets; broader `dns` options beyond the covered callback and promise `lookup`, `lookupService`, `resolve4`, `resolve6`, default-result-order, hints/order/verbatim, TTL-false, import-style, option-form, and bounded callback/promise ordering subsets; and libuv-backed scheduling beyond the bounded nextTick, queueMicrotask, Promise microtask, zero/nonzero timeout, immediate, and interval scheduler subsets.
  - Phase 12 remaining work: `http` / `https` on OpenSSL plus best-effort `http2`.
  - Phase 13 remaining work: real async child-process lifecycle handles/events/streams beyond the currently documented `exec` / `execFile` callback and sync-backed `execSync` / `execFileSync` / `spawnSync` subsets; `fork`; broader `spawn` / `spawnSync` options beyond the covered cwd/env/shell/input/encoding/buffer/maxBuffer/timeout/killSignal/stdio/detached/uid/gid/windows/argv0/error-metadata subsets; `cluster`; and `worker_threads` with structured-clone messages.

- **Phase 14 — npm integration**
  - Remaining work: arbitrary dynamic computed named-export metadata beyond the currently documented finite key subsets for direct dynamic alternatives, JSDoc/single/union literal keys, static string-array composition, tuple/object-map checker-proven element access, object enumeration helpers, `Object.fromEntries(...)` finite key alternatives, and manifest-proven AOT key sets; remaining runtime-computed whole-value `module.exports = ...` expression forms beyond the currently documented boxed AOT default subsets for `typeof` / `void` / `undefined` / BigInt / RegExp literal defaults, binary/conditional, prefix-unary, prefix-update, assignment, `in` / `instanceof`, array spread, `this`, inline-function logical/nullish/conditional, postfix-update, delete, comma, call/new/member/template/tagged-template dynamic-object defaults, supported `Object.defineProperties` / `Object.setPrototypeOf` / `Object.preventExtensions` / `Object.seal` / `Object.freeze` wrappers, and bounded runtime-computed object-literal property / array element initializers with finite named-export metadata; deeper CommonJS wrapper behavior beyond the currently documented finite static wrapper subsets for module aliases/destructuring, TypeScript-only require/module-require assertion wrappers, IIFE and unary-IIFE parameter aliases, factory call/apply/Reflect.apply/bind invocation, static require-argument factory wrappers, and zero-argument object-returning factory/IIFE wrappers including object-preserving wrappers; broader finite dynamic-require specifier proof classes beyond the currently documented finite AOT subsets for RegExp metadata/string-RegExp method fragments, Date parse/UTC fragments, URL metadata and credential/searchParams fragments, static `path.basename` / `dirname` / `extname` fragments, static `querystring.escape` / `unescape` fragments, static string-backed `util.format` fragments, and ArrayBuffer/DataView and TextEncoder/TextDecoder fragments; broader pure-JS package-source patterns beyond the documented `allowJs` ESM package-local import/re-export and package-local/external CommonJS object-literal dependency paths; and broader mixed CJS/ESM interop edge cases beyond the documented CommonJS default/named/namespace default interop and JavaScript ESM import/re-export/namespace-import paths over package-local or external CommonJS object-literal dependencies. Dependency init stays eager through the AOT module graph by design.

- **Phase 15 — perf & polish** (ongoing)
  - Remaining work: deeper hidden-class cache work beyond the current shared shape-tree/property-cache validation subset; broader escape analysis for dynamic objects, escaping closures, returns, interprocedural ownership, dynamic or non-finite loop growing array methods, and escaping receiver-returning mutating array method result flows beyond the currently documented typed object/class and typed array literal stack-allocation subsets for direct locals, same-block aliases and assignments, mutations, iteration, helper calls, selected object helper/prototype/valueOf paths, unrelated nested functions, and closure-capture rejection; broader source-level generated-C DCE beyond the current documented unreferenced top-level/local binding, class/lifted-arrow, selected dead branch/reference pruning, pure primitive/string/number/BigInt/RegExp/Date/URL/URLSearchParams/Promise/collection/object/Reflect/array-helper static-expression, and fresh allocation/helper-call pruning subsets; further binary-size optimization; and CI matrix expansion beyond Linux gcc/clang to macOS and Windows.
