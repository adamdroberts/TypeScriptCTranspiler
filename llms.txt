# typescriptc

> A whole-program TypeScript-to-C transpiler. Takes TS source + its local imports, emits C11, links with gcc and Boehm GC, produces a native Linux binary that behaves like `node file.ts` for the supported subset.

Current status: 251 passing e2e tests, ~10,500 LOC, covers the typed-TS core + classes + first-class typed closures over function-scope captures + foundational NaN-boxed dynamic values for `any`/`unknown`/JSON.parse/dynamic JSON stringify with object omission for undefined/function values/dynamic Array statics/dynamic Array.of/dynamic property/index reads/writes/dynamic array string-key and length writes/dynamic in/delete/dynamic operators/broader dynamic string-array methods including at/codePointAt/concat/copyWithin/entries/fill/keys/values/lastIndexOf/localeCompare/match/matchAll/search with RegExp or string patterns/normalize/pad/repeat/replace/string replace tokens/RegExp replace tokens/RegExp split captures/split(limit?)/substr/substring/slice/reverse/toReversed/toSorted/toSpliced/with/trimStart/trimEnd/flat/default and comparator sort/splice/toString/valueOf/flatMap and dynamic number toString(radix)/toFixed/toExponential/toPrecision and inline-arrow/named/closure-callback HOFs with receiver callback args and optional-initial-value reduce/reduceRight plus findLast/findLastIndex/typed coercions/callable Number, String, and Boolean coercion/basic discriminated unions/custom type predicates/dynamic Object.is/assign/array defineProperty/hasOwn/hasOwnProperty/isPrototypeOf/propertyIsEnumerable/toLocaleString/toString/valueOf/names/entries/fromEntries/getOwnPropertyDescriptors/defineProperties/descriptor defaults/descriptor field preservation/compatible descriptor redefinition/data/named-function/lifted-arrow/closure-valued accessor descriptors with boxed get/set identity, dynamic Reflect.apply dispatch, and absent-hook fields + dynamic array extensibility-seal-freeze descriptor and mutator guards/slice/reverse/prototype-chain lookup/Object.create descriptor maps/basic Reflect object helpers with receiver-aware dynamic data writes/accessor receiver this/Reflect.apply thisArg/dynamic array Object-Reflect enumeration-descriptors + typed/dynamic string Object-Reflect own-property enumeration-descriptors + typed/dynamic string charCodeAt/codePointAt/trim aliases/search positions + typed/dynamic Array.includes SameValueZero + typed Array.keys/values/entries/from-string/flat/flatMap-scalar/HOF receiver callback args/at/copyWithin/fill/concat-values/own-property-checks/property-descriptors/of/reduce/reduceRight without initial values/findLast/findLastIndex/lastIndexOf/toReversed/toSorted-closure-comparator/toSpliced/toString/valueOf/with + typed array Object.keys/values/entries/property-names + typed string at/lastIndexOf/localeCompare/search/substr/match/matchAll string patterns/Object-Reflect own-property enumeration/fromCodePoint/isWellFormed/toWellFormed/toString/valueOf + typed Number toString/toFixed/toExponential/toPrecision/toLocaleString/valueOf + typed Boolean/Symbol/BigInt toString/toLocaleString/valueOf + typed Number constants/isSafeInteger/parseInt radix/default inference + global isNaN/isFinite coercion + broader typed Math libm/int32/fround/constants methods + typed RegExp exec/source/flags/flag booleans including hasIndices/sticky/toString/toLocaleString/valueOf + typed Map/Set constructor initialization + SameValueZero numeric keys + forEach with inline and named callbacks + typed Map/Set/WeakMap/WeakSet/WeakRef toString/toLocaleString/valueOf + typed Set.keys + typed URL toString/toJSON/toLocaleString/valueOf + typed Buffer toLocaleString/valueOf + typed Object/Reflect/in get/set/hasOwn/has/ownKeys/getOwnPropertyNames/getOwnPropertyDescriptor/getOwnPropertyDescriptors/propertyIsEnumerable/toString/toLocaleString/valueOf coverage + `string | number | boolean` typeof, `in`-operator narrowing, and `Array.isArray` type-predicate narrowing + direct generic function call/callback/function-value/method specialization + static Reflect.apply/construct over function/class values with array argument lists + erased generic classes + string/Map/Set/custom iterable/custom iterator object/WeakMap/WeakSet/WeakRef iteration support + Symbol + PCRE2 regex + sync Node stdlib + hard-error diagnostics for permanent AOT limits + generated-C `#line` markers + `--release` builds. Phases not yet implemented: Phase 3 hidden classes/inline caches/remainder, async/await (Phase 6), npm integration (Phase 14). See [docs/todo.md](docs/todo.md) for the full remaining list.

## How to route questions

- **"Can I use feature X today?"** → [docs/done.md](docs/done.md)
- **"Is feature X planned?"** → [docs/todo.md](docs/todo.md)
- **"How does the pipeline work?"** → [docs/architecture.md](docs/architecture.md) (Mermaid diagrams)
- **"What does flag X do?"** → [docs/cli.md](docs/cli.md)
- **"What's the C API?"** → [docs/runtime-reference.md](docs/runtime-reference.md)
- **"How do I add a test?"** → [docs/testing.md](docs/testing.md)
- **"What changed recently?"** → [CHANGELOG.md](CHANGELOG.md)

## Docs

- [README.md](README.md): product overview, install, feature tour, full example code
- [docs/README.md](docs/README.md): documentation index
- [docs/architecture.md](docs/architecture.md): pipeline stages, emission passes, module graph, value model, with Mermaid diagrams
- [docs/cli.md](docs/cli.md): CLI flags, exit codes, env vars
- [docs/runtime-reference.md](docs/runtime-reference.md): every `tsc_*` C symbol, grouped by feature
- [docs/testing.md](docs/testing.md): e2e harness, adding a case
- [docs/done.md](docs/done.md): every implemented capability with test pointers
- [docs/todo.md](docs/todo.md): every unimplemented capability with effort estimates
- [CHANGELOG.md](CHANGELOG.md): session-by-session history
- [examples/README.md](examples/README.md): 7 runnable example programs

## Agent skills

- [.claude/skills/tsc2c-add-feature/SKILL.md](.claude/skills/tsc2c-add-feature/SKILL.md): procedural guide for adding a new TS language feature. Use when a user asks to extend `tsc2c` with a new operator, built-in method, or language construct.

## Full ingestion bundle

- [llms-full.txt](llms-full.txt): all canonical docs aggregated into a single file for LLM ingestion.

## Architecture one-liner

```
entry.ts
  ↓  ts.createProgram (official TypeScript compiler API)
  ↓  src/resolve.ts — walks imports, topo-sorts modules
  ↓  src/emit/index.ts — 5-pass emitter producing one main.c
  ↓  src/link/cc.ts — gcc -O2 -std=c11 main.c runtime/tsc_runtime.c -lgc -lm, or -Os -s with --release
binary
```

## Hard limits (permanent)

Three JS features cannot be AOT-compiled at any engineering investment:

1. Native C++ addons under `node_modules/*/build/Release/*.node` — literal `.node` specifiers are rejected now; full package-resolution detection is Phase 14.
2. Runtime code compilation (JS constructs that compile strings at runtime) — rejected before TypeScript diagnostics.
3. Dynamic `require(variable)` with a non-literal specifier — rejected before emission.

Documented in [docs/todo.md](docs/todo.md#5-permanent-limits-will-never-be-done).

## Key files for code contributors

| File | Purpose |
|------|---------|
| `src/emit/index.ts` | the emitter — one class, ~6,000 LOC |
| `src/emit/types.ts` | TS → C type mapping |
| `src/resolve.ts` | module graph walker |
| `src/compile.ts` | pipeline orchestrator |
| `runtime/tsc_runtime.h` | authoritative list of runtime capabilities |
| `stdlib/lib.core.d.ts` | TS declarations we accept |
| `tests/e2e/run.ts` | test harness |
