# Property evidence

Add compact semantic-partition specifications as `*.property.test.ts`. `run.ts` discovers every such file recursively and fails when none exist, so an empty suite cannot satisfy the ECMAScript claim gate.

Each terminal matrix partition must reference the exact property specification that exercises its canonical collection, tree, graph, or worklist. An authored finite/unbounded label never changes that obligation. Property evidence is distinct from the generated representative stress cases under `tests/e2e/cases/`.

`module-linking.property.test.ts` derives import/export edges as canonical ModuleRequest Records whose decoded attributes are sorted and deduplicated independently of source order or declaration count. It checks request equality, duplicate-key early errors, supported-key and resource-type resolution partitions, binding-identity resolution through cyclic/deep graphs, and one representative wide parser worklist. The request, declaration, and graph widths are stress guards rather than coverage denominators.

`async-cfg.property.test.ts` is the first architectural specification: it generates recursive combinations of statement and expression partitions, independently reconstructs the returned edge graph, and checks closure, reachability, one suspension owner per source `await`, and fail-closed rejection. It establishes the property gate itself; it is not clause evidence until an exact terminal clause partition reciprocally registers it with distinct stress and implementation evidence.

`async-logical-value.property.test.ts` derives `&&`, `||`, and `??` expectations from one ECMAScript truthy/nullish value partition and checks evaluate-once left calls, exact short-circuit values/identity, recursive and conditional left trees, a suspending left subtree, getters, typed nullable class fields, and exact abrupt identity in both memory modes. The planner property separately proves unique AST-owner/result-slot evaluation, while `async_logical_expression_depth` traverses one representative high-depth tree; neither the probe inventory nor depth is a completion denominator.

`dispatch-capture.property.test.ts` derives dispatch snapshots from one capture-descriptor collection and independently checks top-level/local, Script/Module, typed-array/dynamic-object/nullish/primitive, post-schedule container and parameter mutation, lexical-initialization, physical integer storage, and stable mutable-capture rejection partitions in both memory modes. Its pre-initialization probe schedules through a hoisted function and verifies that the copied TDZ bit cannot be changed by later declaration evaluation. `dispatch_capture_worklist` separately stresses one representative wide descriptor collection; neither capture width nor the dispatch fixture inventory is completion evidence. Dispatch is an optional extension and this property is not ECMAScript clause evidence.

`dns-lookup.property.test.ts` derives supported numeric, object, shorthand, null, ordering, all-results, and bitwise-hint options from one static-alias plan and independently checks source-ordered deferred callback/Promise completion, ignored synchronous evaluation, stable accessor/effectful/mutable rejection, and decoded-root survival in both memory modes. `dns_lookup_option_alias_worklist` separately traverses one representative earlier-`const` alias depth; neither its depth nor the DNS fixture inventory is a completion denominator. DNS is a Node extension and this property is not ECMAScript clause evidence.

`call-activation.property.test.ts` generates JavaScript formal/actual argument plans and independently models expected argument counts, default initialization, mapped versus unmapped parameter writes, excess arguments, abrupt nested calls, lexical arrow access, activation-sensitive ordinary IIFEs, and mutation through the activation's actual dynamic storage type even when JSDoc control-flow narrowing exposes a static view. One representative wide input exercises the same activation worklist as a stress guard; its size is not used as completion evidence.

`arrow-function.property.test.ts` derives Arrow early errors from one source worklist and recursive BindingName tree, then independently crosses the runtime FormalParameters and lexical-environment partitions in both memory modes. One ordered descriptor collection carries lexical `this`, implicit `arguments`, and `new.target` through escape, nesting, call/apply/bind, sloppy mapped and strict/non-simple unmapped arguments, explicit shadowing, ordinary/reflective construction, alternate new targets, and parameter initializers. `arrow_formal_binding_tree` separately traverses representative high-depth binding, directive, and lexical-environment trees; its depth and the probe inventory are not coverage denominators. Broader Arrow clauses remain unclaimed until `super`, metadata/private-environment, eval/body-environment, and exact pinned-query partitions are complete.

`array-representation.property.test.ts` derives compatible typed/dynamic alias mutations from one operation worklist and independently models identity, writes, deletion, reversal, holes, key order, and primitive element boxing. One representative wide operation collection guards the same per-array codec path against fixed-shape lowering; its width is not completion evidence.

`array-static-factory.property.test.ts` independently crosses generic constructor/non-constructor receivers, iterable/generator/sparse/array-like sources, computed iterator methods, ordered mapper and iterator effects, indexed property descriptors, length finalization, and the distinct IteratorClose abrupt-completion partitions for `Array.from` and `Array.of` in both memory modes. Source elements and runtime items enter canonical AST and iterator/argument worklists; `array_static_factory_item_width` is the separate representative runtime worklist stress, not a completion denominator.

`computed-property-key.property.test.ts` derives the `ToPropertyKey` primitive/object partition and checks dynamic read, write, update, `in`, delete, optional access, and shared-key aliasing through one runtime key path. Its object-initializer partition checks once-only source ordering and symbol identity for computed data, method, getter, and setter definitions, including descriptor flags and getter/setter merging. The representative wide runtime key collection guards the same coercion path; its width is not completion evidence for Object Initializer.

`object-intrinsic-alias.property.test.ts` generates descriptor plans and independently checks their flags, keys, bound-call forwarding, primitive wrappers, and dynamic-array representation under a narrowed `string[]` source view. Its accessor partitions independently check receiver binding, absent-formal `undefined`, defaults, zero-formal setters, rest collection, and ordinary return-`undefined` behavior through the same callback-formal worklist. One representative wide descriptor collection and one wide formal list exercise those worklists; their sizes are not completion evidence.

`script-global-binding.property.test.ts` generates Script binding, Reference, and `var`-scope plans and independently checks bidirectional identifier/property reflection, descriptors, declaration instantiation, strict/sloppy `PutValue`, rejected writes, updates, deletion, evaluation order, lexical non-reflection, function boundaries, loop-head bindings, and function `this` normalization against one canonical global-object environment. One representative deeply nested statement combines declaration and unresolved-reference writes through the same source-tree and Reference algorithms; its depth is not completion evidence. This remains architectural evidence until exact terminal clause partitions reciprocally register distinct implementation, generated-regression, stress, and E2E artifacts.

`json-syntax.property.test.ts` checks the strict JSON lexical and container partitions used by module resolution, including exact whitespace, strings/escapes, numbers, separators, and trailing input. One representative deeply nested container exercises the explicit validation-frame worklist without treating successive depths as completion units.

`json-parse-materialization.property.test.ts` derives valid primitive/container/materialization cases and invalid lexical/structural cases from one semantic probe collection, then independently checks native values and exact `SyntaxError` identity in both memory modes. `json_parse_deep_worklist` supplies one representative deeply nested materialization input through the same runtime frame algorithm; its depth is not a coverage denominator.

`json-module.property.test.ts` derives every JSON root-value partition plus direct-default, aliased-default, namespace, identity, mutation, and namespace-exotic observations from one synthetic-binding plan. It compiles the generated module graph in both memory modes and independently checks the native transcript; partition and import counts are not completion evidence.

`dynamic-import.property.test.ts` derives finite target alternatives, fail-closed non-finite shapes, and source traversal from one canonical worklist independently of runtime options. It compiles a representative deep dependency graph in both memory modes and checks argument order, ordinary object/Proxy attribute collection, abrupt-value identity, resource matching, lazy dependency-first initialization, stable namespace identity, JSON import, Promise job ordering, boxed untyped-array combinator intake, and runtime-valued handlers; graph depth and request counts are stress guards rather than coverage denominators.

`test262-negative-phase.property.test.ts` derives contextual `return`, `break`, `continue`, label, function-boundary, switch/iteration, and update-target early-error partitions from one compiler-frontend source-tree worklist shared with the host. Its Module plan independently crosses lexical/var/export name relationships, binding-tree depth, inherited strict restrictions, `super`/`new.target` containment boundaries, nested lexical private environments, and well-formed versus unpaired-surrogate ModuleExportName strings. Representative deeply nested control and Module statement trees exercise the same worklists without using nesting depth as a coverage denominator. Together with separate generated width/depth stresses, native class-expression execution, and the exact pinned query, this is reciprocal evidence for the complete ModuleBody early-error clause; the broader Module host remains disabled for independent loading/evaluation gaps.

`class-expression.property.test.ts` derives supported dynamic base/default-constructor class definitions from one instance/static method descriptor collection. It independently observes fresh per-evaluation identity, enclosing-cell capture isolation, instance and static `this`, constructor/prototype linkage, inferred and explicit names, call rejection, exact method/prototype-constructor descriptors, and stable fail-closed heritage/member partitions in both memory modes. `class_expression_method_width` is one separate representative member-worklist stress; method counts are not completion evidence, and unsupported class-element families remain unclaimed.

`test262-eval-script.property.test.ts` derives nested, branched, and sibling finite `$262.evalScript`, Realm-bound `evalScript`, direct-`eval`, and indirect-`eval` source alternatives through one transitive AST worklist. A conservative eval-identity graph follows comma/conditional, alias, property, bound, `.call`, `.apply`, and foreign-Realm carriers without replacing the decisive runtime identity check. It compiles binder-isolated Script and distinct finite direct/indirect PerformEval records in both runtime memory modes and independently checks exact-source dispatch, reevaluation on every call, Realm global/declarative isolation and abrupt restoration, Realm-owned constructor/prototype/namespace/method identity, shared well-known Symbols, creation-Realm ownership of Array prototype symbol descriptors, foreign-prototype symbol mutation/deletion/restoration with caller-Realm isolation, callable and bound-function `[[Realm]]`, native-error origin, intrinsic-default prototype selection across the non-Object value categories, a representative Realm-state worklist, strict-caller and source-strict isolation, non-String identity, intrinsic reassignment, argument evaluation, completion/abrupt identity, deletable sloppy eval declarations, unknown-source rejection, indirect callee-Realm selection, and later Script lexical shadowing. The same specification checks global `var`/function visibility, reverse-order duplicate-function selection, invalid-source `SyntaxError`, lexical TDZ/mutation/immutability/class bindings, configurable-property shadowing, atomic cross-record collision rejection, failed-instantiation absence/read/`typeof`/delete behavior, existing global properties, function definability, lexical creation on a non-extensible global object, global class mutation, top-level Script `this`, and the bound harness primordials used by Test262. Its Annex B insertion partitions check conditional and switch candidates, source-ordered updates, hypothetical-`var` early-error suppression, cross-record collisions, existing-property descriptors, strict-code exclusion, non-extensible suppression, and one representative deep source-tree candidate through the same containment worklist. The same specification checks ScriptEvaluation completion propagation through empty/declaration, branch, loop, caught-abrupt, normal-finally, and object-identity partitions, plus global object/array binding trees, computed keys, defaults, object/array rest, nested-block and iteration `var` bindings, closure reads, normal/abrupt IteratorClose, and representative deeply nested statement, BindingName, and duplicate-declaration worklists. All discovered sources enter one record worklist, all Realm subsystem state enters one key-addressed collection, all declarations enter one normalized runtime Global Environment worklist, all BindingNames enter one recursive initializer, and all normal values enter one UpdateEmpty completion slot; the individual probe inventory and stress dimensions are not coverage denominators. This is architectural evidence for the enabled finite-AOT `$262.evalScript` host capability and its Realm/eval dependencies; clause support still requires reciprocal matrix evidence and a passing pinned exhaustive report.

`strict-equality.property.test.ts` derives comparisons from one canonical collection of ECMAScript language Types and independently applies the Strict Equality algorithm. The generated program verifies every represented Type pairing, identity/value partitions, `NaN`, signed zero, negation, pointer-backed single-nullish unions versus boxed dual-nullish unions, left-to-right operand evaluation, and left/right abrupt-completion propagation with exact thrown-value identity in both runtime memory modes; individual pair counts are not completion evidence.

`switch-selection.property.test.ts` derives discriminators and case expressions from one canonical ECMAScript Type/identity collection and independently applies Strict Equality to predict the selected clause and exact evaluated prefix. The generated JavaScript crosses mixed static/dynamic Types, `NaN`, signed zero, object/Symbol/Function identity, source order, default-in-the-middle search, fallthrough, abrupt discriminator/case completion, nested awaited discriminators, and awaited cases in both memory modes. `switch_clause_worklist_width` is one distinct representative clause-width stress through the same lowering; neither its width nor the generated partition inventory is a completion denominator.

`switch-evaluation.property.test.ts` independently checks the CaseBlock environment boundary: the discriminator runs in the outer environment, every selector and clause shares one fresh lexical environment, and one recursive `BoundNames`/`BindingInitialization` tree initializes direct `let`/`const` identifiers or nested object/array pattern leaves into the same preallocated TDZ/immutability cells. The same source-ordered declaration plan instantiates direct ordinary, generator, and async functions before selector evaluation, including unselected clauses and sloppy duplicates, while shared cells preserve function metadata/construction, recursion/cross-reference, later-binding TDZ, native abrupt identity, escape, mutable identity, Annex B updates, outer async-CFG suspension, lazy-generator resume, and function-bounded async continuation state. Direct empty base classes initialize their preallocated cells only at declaration evaluation and cross selector/unselected TDZ, escape, mutable rebinding, repeated-evaluation identity, metadata, call rejection, and default construction; the same cell plan survives awaited selection and later async-CFG suspension, while unsupported class shapes are required to fail closed. Resumable outer captures are induced maps of value/root/initialization pointers rather than await/yield-shape inventories. The property crosses computed keys, defaults, elisions, array/object rest, source order, leaf-to-leaf TDZ, immutable writes, closure creation before initialization, restoration, and async-await execution. `test262-eval-script.property.test.ts` separately checks Switch ScriptEvaluation completion values, default/fallthrough, labelled break, abrupt selector identity, ordinary direct eval in the pre-environment discriminator, and the stricter finite selector/body direct-eval admission proof. `switch_caseblock_environment_depth`, `switch_caseblock_binding_depth`, `switch_caseblock_function_worklist_width`, `switch_caseblock_generator_capture_width`, `switch_caseblock_async_capture_width`, and `switch_caseblock_class_worklist_width` are distinct representative environment, binding-tree, declaration-worklist, class-definition-worklist, and resumable-capture stresses; none is a completion denominator. Direct async-generator declarations, non-empty/derived class definitions, and the resumable lazy-generator switch path remain explicit fail-closed gaps, so these tests are not a whole-clause claim.

`declaration-reachability.property.test.ts` derives a declaration-reference graph and independently traverses it with an explicit worklist before compiling the reverse-ordered source graph in both memory modes. A second isolated-Script probe connects a global initializer to captured JavaScript functions with different inferred return shapes and ECMAScript RegExp escape spellings. The graph depth is a representative order/stack stress for the same algorithm, not a sequence of declaration-count tasks.

`exception-frame.property.test.ts` sends return, catch-return, loop break/continue, and a representative deeply nested return through one lexical-completion model, then independently throws an identity sentinel after every probe. Both runtime memory modes must preserve the sentinel and leave no expired native exception frame; nesting depth is a stress guard, not completion evidence.

`conversion-consumers.property.test.ts` derives Date construction, `TimeClip`, Number construction, and global numeric-predicate probes from one conversion plan. It independently checks Date internal-slot cloning, default-hint exotic and ordinary `ToPrimitive`, String parsing versus non-String `ToNumber`, clipping/truncation/positive-zero boundaries, Number's explicit BigInt exception to ordinary `ToNumber`, Symbol rejection, and exact abrupt-value identity in both runtime memory modes. A separate exact-integer oracle generates binary64 below/tie/above-halfway, parity, carry, sign, finite-boundary, and overflow partitions for BigInt conversion. One representative generated Date worklist guards the shared consumer path; neither its width nor the generated rounding probes are a coverage denominator.

`global-number-functions.property.test.ts` derives `parseInt` radix/prefix/digit/partial/invalid/sign/rounding partitions and `parseFloat` decimal/exponent/Infinity/partial/rounding partitions from canonical probe collections. An independent exact-integer-to-binary64 oracle checks arbitrary-radix generated inputs, while both optimized calls and Realm-global reflective calls consume the same expectations in both memory modes. The same specification checks omitted arguments, ECMAScript whitespace, ordered coercion and abrupt identity, function metadata/global descriptors, and one representative long input through the length-driven parser; probe inventory and input length are not coverage denominators.

`string-to-number.property.test.ts` derives the complete-input StringNumericLiteral partitions for empty/whitespace, signed decimal and Infinity, decimal points/exponents, binary/octal/hex integers, invalid prefixes/digits/suffixes/separators, signed zero, and exact non-decimal binary64 rounding. `Number`, unary plus, `isFinite`, and `isNaN` consume the same expectation collection in both memory modes, then cross Boolean/nullish, array/object ordinary-conversion, BigInt, Symbol, and TypeError partitions through the shared `ToNumber` boundary. It also guards source NumericLiteral overflow, underflow, and large non-decimal normalization because the pinned clause scenarios use those values as comparison operands. One representative long decimal joins the separately generated `global_number_parse_length` stress guard; neither source length nor probe inventory is a coverage denominator. Reciprocal matrix evidence binds the parent String-input `ToNumber` clause, while its child clauses and the global signed report remain separate obligations.

`uri-codec.property.test.ts` derives URI/component unescaped and reserved sets, scalar UTF-8 width boundaries, percent spelling, malformed canonical-sequence classes, isolated-surrogate preservation/repair/rejection, ordered `ToString`, abrupt identity, and scalar round trips from compact semantic collections. Optimized and Realm-global reflective calls consume the same expectations in both memory modes. The separately generated `uri_percent_codec_length` case is one representative stress for the length-driven codec; neither that length nor the probe inventory is a coverage denominator.

`string-static-code-units.property.test.ts` derives the `String.fromCharCode` modulo-2^16 and `String.fromCodePoint` integer/range partitions, conversion types, surrogate preservation, argument-evaluation versus conversion order, exact abrupt identity and short-circuiting, spread intake, ordinary static-method descriptors/identity/mutation/accessor lookup, reflective aliases, `String.raw`, and dynamic String `ToPropertyKey` observations from compact semantic collections. Both memory modes consume the same runtime value worklist. The separately generated `string_code_unit_list_width` and `string_code_point_list_width` cases are distinct representative stresses for code-unit conversion and mixed-BMP/astral validation/materialization respectively; neither width nor the probe inventory is a coverage denominator. The exact `sec-string.fromcharcode` and `sec-string.fromcodepoint` terminal partitions reciprocally register this property, their distinct stress, native E2E, declaration surface, and canonical emitter/runtime worklists. `String.raw` and the remaining String methods stay separate clause obligations.

`string-utf16-exotic.property.test.ts` independently derives empty, ASCII, BMP, astral, isolated-high-surrogate, isolated-low-surrogate, and mixed String inputs into expected UTF-16 code-unit collections. In both memory modes it checks primitive and boxed lengths/indexes, numeric and length descriptors, ordinary-property fallback, canonical-index rejection, own-key order, ownership/enumerability, values/entries, copying, compatible and incompatible redefinition, deletion, extensibility, and the deliberately distinct code-point iterator projection. `string_utf16_exotic_width` derives expected code units independently from one mixed code-point collection and is the separate representative stress for the same canonical projection; neither its width nor probe inventory is a completion denominator. Reciprocal evidence binds only the exact String exotic `[[GetOwnProperty]]` clause; StringCreate, StringGetOwnProperty, the other exotic internal methods, and String prototype methods remain separate obligations.

`string-at.property.test.ts` derives the `String.prototype.at` intrinsic surface, receiver, index, ordering, abrupt, relative-range, and UTF-16 code-unit partitions from compact semantic collections. It checks primitive, boxed, ordinary-object, array, BigInt, Symbol, and nullish receivers; omitted, Boolean, null, String, array, object, BigInt, Symbol, NaN, fractional, infinite, positive, negative, and out-of-range indices; exact conversion count/order and thrown-value identity; astral halves and lone surrogates; and descriptor mutation/restoration in both memory modes. The existing `string_utf16_exotic_width` case separately derives expected units from mixed code points and stresses direct, relative, generic, and undefined-range paths at representative width. Neither semantic probe inventory nor input width is a completion denominator. Reciprocal evidence binds the exact `sec-string.prototype.at` intrinsic; adjacent String methods remain separate obligations.

`string-concat.property.test.ts` derives the `String.prototype.concat` intrinsic surface, generic receiver, argument conversion, call-evaluation, abrupt-completion, UTF-16 preservation, and mutable-lookup partitions from compact semantic collections. It distinguishes receiver/property/argument-expression evaluation from the intrinsic's receiver-first `ToString` walk, checks exact conversion and thrown-value identity, and independently compares every resulting UTF-16 code unit against one canonical mixed-unit plan in both memory modes. A separately generated argument-worklist-width case is the representative stress for the same dynamically sized collection and single-allocation join; neither its width nor the semantic probe inventory is a completion denominator. Reciprocal evidence binds only the exact `sec-string.prototype.concat` intrinsic.

`string-search.property.test.ts` derives `String.prototype.includes`, `indexOf`, `lastIndexOf`, `startsWith`, and `endsWith` from one shared receiver/search/position semantic plan. It independently crosses intrinsic metadata, generic and borrowed receivers, omitted and coercible inputs, default/NaN/fractional/infinite positions, RegExp and `Symbol.match` partitions, exact receiver/match/search/position abrupt order, mutable lookup, and scalar/surrogate UTF-16 matching in both memory modes. The existing `string_utf16_exotic_width` case is the distinct representative stress for the same canonical code-unit matcher; neither its width nor the semantic probe inventory is a completion denominator. Reciprocal evidence is registered separately for each exact terminal intrinsic clause.

`string-fill.property.test.ts` derives `String.prototype.repeat`, `padStart`, and `padEnd` from one shared receiver/count/target/filler semantic plan over the canonical UTF-16 fill worklist. It independently crosses intrinsic metadata, generic and borrowed receivers, omitted/undefined/NaN/fractional/array/object counts and targets, default-space and null/Boolean/BigInt fillers, negative and infinite `repeat` RangeError partitions, BigInt/Symbol count/target and Symbol filler rejection, empty-filler and long-enough early returns with skipped filler conversion, exact receiver/count/target/filler abrupt identity and order, mutable lookup, and scalar/explicit-pair/lone-surrogate code-unit results in both memory modes. The existing `string_utf16_exotic_width` case is the distinct representative stress for the same canonical fill worklist; neither its width nor the semantic probe inventory is a completion denominator. Reciprocal evidence is registered separately for each exact terminal intrinsic clause.

`string-trim.property.test.ts` derives `String.prototype.trim`, `trimStart`, `trimEnd`, and the Annex B `trimLeft`/`trimRight` aliases from one shared receiver semantic plan over the canonical UTF-16 boundary worklist. It independently crosses intrinsic metadata, alias identity/name/descriptor with `trimStart`/`trimEnd`, generic and borrowed receivers, extra-argument evaluation with ignoring, every WhiteSpace and LineTerminator code unit, kept non-whitespace controls, empty and all-whitespace inputs, exact receiver abrupt identity and order, mutable lookup, and scalar/explicit-pair/lone-surrogate preservation in both memory modes. The existing `string_utf16_exotic_width` case is the distinct representative stress for the same canonical boundary worklist; neither its width nor the semantic probe inventory is a completion denominator. Reciprocal evidence is registered separately for each exact terminal intrinsic and alias clause.
`string-case.property.test.ts` derives `String.prototype.toLowerCase`, `toUpperCase`, and `normalize` from one shared receiver/form semantic plan over the canonical UTF-16 mapping worklist. It independently crosses intrinsic metadata, generic and borrowed receivers, extra-argument evaluation with ignoring, ASCII folding, sharp-s/ligature/dotted-capital expansions, context-sensitive final sigma, astral pairs, all four normalization forms with default/undefined/object forms, invalid-form RangeError partitions, exact receiver/form abrupt identity and order, mutable lookup, and lone-surrogate preservation in both memory modes. The existing `string_utf16_exotic_width` case is the distinct representative stress for the same canonical mapping worklist; neither its width nor the semantic probe inventory is a completion denominator. Reciprocal evidence is registered separately for each exact terminal intrinsic clause.

`string-wellformed.property.test.ts` derives `String.prototype.isWellFormed` and `toWellFormed` from one shared receiver semantic plan over the canonical UTF-16 scan worklist. It independently crosses intrinsic metadata, non-constructability, direct method-value surface, generic and borrowed receivers, extra-argument evaluation with ignoring, empty/scalar/astral inputs, lone lead/trail/split/embedded/trailing partitions, U+FFFD substitution with length preservation, exact receiver abrupt identity, mutable lookup with getter-before-arguments, and surface restore in both memory modes. The existing `string_utf16_exotic_width` case is the distinct representative stress for the same canonical scan worklist; neither its width nor the semantic probe inventory is a completion denominator. Reciprocal evidence is registered separately for each exact terminal intrinsic clause.

`string-tostring-value.property.test.ts` derives `String.prototype.toString` and `valueOf` from one shared receiver semantic plan over the canonical UTF-16 thisStringValue projection. It independently crosses intrinsic metadata, non-constructability, direct method-value surface, primitive/boxed/prototype receivers, incompatible-receiver TypeError partitions, extra-argument evaluation with ignoring, lone-surrogate preservation, mutable lookup with surface restore, and primitive results in both memory modes. The existing `string_utf16_exotic_width` case is the distinct representative stress for the same canonical projection; neither its width nor the semantic probe inventory is a completion denominator. Reciprocal evidence is registered separately for each exact terminal intrinsic clause.

`string-iterator.property.test.ts` derives `String.prototype[Symbol.iterator]` from one shared receiver semantic plan over the canonical UTF-16 code-point iteration worklist. It independently crosses intrinsic metadata, non-constructability, direct method-value surface, primitive/boxed/prototype receivers, null/undefined/Symbol rejection with abrupt ToString identity, extra-argument evaluation with ignoring, empty/ASCII/astral/lone-surrogate chars, fresh independent iterators, borrowed-next rejection, iterator self-reference, lookup-before-arguments, spread/for-of/Array.from equivalence, mutable lookup with surface restore, and result objects in both memory modes. The existing `string_utf16_exotic_width` case is the distinct representative stress for the same canonical worklist; neither its width nor the semantic probe inventory is a completion denominator. Reciprocal evidence is registered separately for the exact terminal intrinsic clause.

`string-raw.property.test.ts` derives `String.raw` from one shared constructor semantic plan over the canonical UTF-16 template worklist. It independently crosses constructor surface, non-constructability, segment-only/interleaved/skipped/ignored partitions, fractional/string/negative lengths, coerced segments, nullish template/raw TypeError partitions, Symbol rejection, abrupt identity with segment-before-substitution order, tagged-vs-direct equivalence with raw-backslash preservation, extra-argument evaluation, mutable lookup with surface restore, and results in both memory modes. The existing `string_raw_template_width` case is the distinct representative stress for the same canonical worklist; neither its width nor the semantic partition inventory is a completion denominator. Reciprocal evidence is registered separately for the exact terminal intrinsic clause.

`string-utf16-sequence.property.test.ts` constructs two unequal WTF-8 storage forms for one UTF-16 String value and independently checks their equality, UTF-16 relational order, Map/Set/PropertyKey hash equivalence, search positions, code-unit range splitting, padding truncation, iterator pairing, and well-formedness/repair behavior in both memory modes. These partitions all consume one lazily published immutable code-unit collection. The existing `string_utf16_exotic_width` input now sends one independently derived mixed collection through representative equality/hash, search, range, padding, iterator, exotic-property, and `at` operations; its width is a stress guard, not a completion denominator. This property is architectural evidence and does not by itself discharge any String intrinsic's receiver coercion, descriptor, exact Test262 query, or reciprocal clause evidence.

`string-utf16-replace-split.property.test.ts` independently crosses semantically equal scalar/explicit-surrogate search strings, matches within surrogate pairs, literal replace versus non-overlapping replaceAll, matched/prefix/suffix/dollar replacement tokens, replacement storage representation, empty search/source/separator, split limits, and isolated-surrogate results in both memory modes. Literal replacement and string-separator split share one canonical UTF-16 matcher and output collection; the byte-offset string-substitution predecessor is deleted. The existing `string_utf16_exotic_width` stress now scans its one mixed collection through representative replace/split operations. Neither that width nor the probe inventory is a completion denominator, and regex/callback paths plus each intrinsic's generic surface and exact reciprocal evidence remain separate.

`reflect-construct.property.test.ts` derives the Realm-global `%Reflect%` surface and `Reflect.construct` target, explicit/omitted `newTarget`, constructor-result, prototype, array-like, sparse, ordered `ToLength`/indexed-Get, abrupt, bound, and Proxy partitions from one semantic plan. JavaScript mutation probes force ordinary global/property lookup, while every successful intrinsic call copies its source into one validated argument List before construction in both memory modes. `reflect_construct_argument_width` separately sends one representative array-like width through that List and the boxed typed rest-parameter slice; neither its width nor the semantic probe inventory is a completion denominator. Reciprocal matrix evidence binds the parent Reflect object and exact construct algorithm, while `arrow-function`, other Reflect operations, and the exhaustive signed report remain separate obligations.

`date-intrinsic.property.test.ts` derives the supported Date constructor, prototype, method, static-function, and alias metadata from one independent descriptor plan. It crosses stable identity and branding, internal-slot receiver validation, UTC/local operation categories, ordered `ToNumber`, `TimeClip`, stringification, generic `toJSON`, and `@@toPrimitive` hint partitions in both runtime memory modes. One deterministic mutation worklist exercises the same dispatcher at representative depth; neither its width nor the descriptor inventory is a coverage denominator.

`dynamic-primitive.property.test.ts` derives BigInt and Symbol probes from one semantic collection after both values cross the canonical dynamic representation boundary. It independently checks type, truthiness, value versus identity equality, explicit string conversion, primitive and wrapper branding, prototype selection, wrapper extraction, hash-key behavior, the BigInt arithmetic/bitwise/shift/update/relational and mixed-Type error partitions, and survival across forced collection in both runtime memory modes; the probe inventory is a partition specification, not a coverage denominator.

`symbol-coercion.property.test.ts` treats Symbol identities and PropertyKeys as one canonical semantic collection. It independently checks well-known metadata, arbitrary same-description identities, collision freedom from ordinary strings, prototype/wrapper and `@@toPrimitive` partitions, computed get/set/has/delete/descriptor operations, String-versus-Symbol own-key ordering, Reflect receivers, descriptor-map and copy operations, Proxy key projection/invariants, and exact abrupt constructors in both memory modes. One representative wide Symbol-key worklist guards the identity-bearing carrier; its width is stress evidence, never a coverage denominator.

`proxy-constructor.property.test.ts` derives the `%Proxy%` constructor, `Proxy.revocable`, target/handler category, revocation, global mutation, lookup/evaluation-order, native-error, and callable-source partitions from one semantic probe plan. It independently checks that exotic construction neither exposes an own `prototype` nor reads `newTarget.prototype`, while all actual arguments cross the ordinary runtime argument-array boundary in both memory modes. One representative wide argument tail guards that same worklist; its width and the existing Proxy fixture inventory are not coverage denominators. This is architectural evidence until exact terminal Proxy clauses also have reciprocal matrix, distinct generated stress, declaration-surface, implementation, and pinned Test262 evidence.

`mutable-function-binding.property.test.ts` derives arrow, ordinary-function, null, and undefined writes from one canonical binding worklist and independently models every observation. The same worklist crosses module/script lexical storage and a closure-captured local in both runtime memory modes; its representative stress tail guards the shared storage decision without making write count a coverage measure.

`html-dda.property.test.ts` derives the Test262 host-global, hidden `%AbstractModuleSource%`, and normative Annex B `[[IsHTMLDDA]]` behavior from one semantic-probe collection. It independently checks `$262`/`print` global identity and descriptors, every abstract-module-source intrinsic/prototype partition, and the callable exotic object's `typeof`, truthiness, abstract/strict/SameValue equality, nullish behavior, and required call results in both runtime memory modes; the probe inventory is a partition specification, not a coverage denominator.

`iteration-environment.property.test.ts` derives module/function ownership, `for-of`/`for-in`, lexical declaration/mutation, and existing-reference assignment from one semantic-plan collection. It independently models fresh per-iteration cells versus the deliberately shared assignment target, invokes every closure only after its loop has exited, and repeats mutable observations in both runtime memory modes. One representative wide value collection guards the same binding path; its width is not completion evidence.

`module-declaration-instantiation.property.test.ts` derives module-scoped `var` declarations from one source-tree worklist and independently checks preinitialization, mutation, and loop/block ownership. Its cyclic-graph probe separately verifies that every reachable Module Record is instantiated before any evaluation: an imported `var` is `undefined`, an imported lexical binding remains in the TDZ, and a hoisted function binding is callable across a back edge. One representative deep cycle exercises the same explicit graph frames in both memory modes; graph depth is not a coverage denominator.
