// @ts-nocheck: CommonJS fixture shapes are validated by tsc2c AOT, not TypeScript JS inference.
// @ts-ignore: TypeScript does not infer exports from placeholder CommonJS assignments.
import greet, { alias, count, label } from "tsc2c-cjs-export-placeholders";
import * as placeholders from "tsc2c-cjs-export-placeholders";

const pkg = require("tsc2c-cjs-export-placeholders");

console.log(greet("Ada"), label, count, alias("Lin"));
// @ts-ignore: TypeScript does not infer namespace members from placeholder CommonJS assignments.
console.log(placeholders.default("Grace"), placeholders.label, placeholders.count, placeholders.alias("Alan"));
console.log(pkg.default("Katherine"), pkg.label, pkg.count, pkg.alias("Edsger"));
