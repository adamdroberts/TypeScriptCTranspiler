import { mode as importMode, pick as importPick } from "tsc2c-dual-cjs-esm-package";

const required = require("tsc2c-dual-cjs-esm-package");

console.log("import:", importMode, importPick(4));
console.log("require:", required.mode, required.pick(5));
