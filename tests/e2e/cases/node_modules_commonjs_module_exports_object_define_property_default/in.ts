const pkg = require("tsc2c-cjs-object-define-property-default-package");
const hidden = Object.getOwnPropertyDescriptor(pkg, "hidden");

console.log(
    Object.keys(pkg).join("|"),
    pkg.visible,
    pkg.hidden,
    hidden.enumerable,
);
