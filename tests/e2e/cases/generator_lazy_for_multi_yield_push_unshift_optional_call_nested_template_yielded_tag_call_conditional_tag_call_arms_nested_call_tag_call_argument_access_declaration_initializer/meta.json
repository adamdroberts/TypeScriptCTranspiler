{
  "description": "lazy generators stage a nested dynamic tag call after a branch-local optional call inside both conditional dynamic tag-call arms before outer yielded tag-call substitutions inside optional push and unshift calls before mutating computed element access"
}
