import pkg, { count, default as namedDefault, enabled, label } from "tsc2c-cjs-module-exports-object-reverse-value-chain";

const required = require("tsc2c-cjs-module-exports-object-reverse-value-chain");

console.log("default:", pkg.label, pkg.count, pkg.enabled);
console.log("named default:", namedDefault.label, namedDefault.count, namedDefault.enabled);
console.log("named:", label, count, enabled);
console.log("require:", required.label, required.count, required.enabled);
