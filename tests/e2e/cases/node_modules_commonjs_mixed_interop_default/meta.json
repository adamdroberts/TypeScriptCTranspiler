{
  "description": "mixed CommonJS/ESM interop: default, named-default, and namespace default imports fallback to the whole module.exports object when no __esModule marker and no default property are defined"
}
