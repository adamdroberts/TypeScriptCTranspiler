{
  "description": "mixed CommonJS/ESM interop: default and namespace default imports fallback to the whole module.exports object for unmarked CommonJS packages, while named default imports use an explicit default property"
}
