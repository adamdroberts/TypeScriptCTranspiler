const pkg = require("tsc2c-cjs-object-create-default-package");

console.log(
    Object.keys(pkg).join("|"),
    pkg.visible,
    pkg.hidden,
    Object.isFrozen(pkg),
    Object.getPrototypeOf(pkg) === null,
);
