const { label, "item-count": itemCount, add: renamedAdd, enabled } = require("tsc2c-cjs-destructure-package");

function total(): number {
    return renamedAdd(30, 12);
}

console.log(label, itemCount, enabled, total());
