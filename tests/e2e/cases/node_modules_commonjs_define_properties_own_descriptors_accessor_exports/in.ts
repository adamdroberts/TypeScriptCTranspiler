// @ts-ignore: TypeScript does not infer exports from Object.defineProperties(..., Object.getOwnPropertyDescriptors(...)).
import greet, { count, double, label } from "tsc2c-cjs-define-properties-own-descriptors-accessors";

const moduleAccessors = require("tsc2c-cjs-module-define-properties-own-descriptors-accessors");

console.log(greet("Ada"), label, count, double(2));
console.log(
    moduleAccessors.default,
    moduleAccessors.label,
    moduleAccessors.count,
    moduleAccessors.flip(false),
);
