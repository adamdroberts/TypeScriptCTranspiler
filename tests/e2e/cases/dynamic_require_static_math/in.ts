const fromFloor = require("./m" + Math.floor(4.9));
const fromPow = require("./p" + Math.pow(2, 5));
const fromMax = require("./x" + Math.max(5, 3, 9));

console.log(fromFloor.label, fromPow.label, fromMax.label);
