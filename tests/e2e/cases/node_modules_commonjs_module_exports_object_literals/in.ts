import { add, count, enabled, label } from "tsc2c-cjs-object-literals-package";

const pkg = require("tsc2c-cjs-object-literals-package");

console.log(label, count, enabled, add(5, 6));
console.log(pkg.label, pkg.count, pkg.enabled, pkg.add(7, 8));
