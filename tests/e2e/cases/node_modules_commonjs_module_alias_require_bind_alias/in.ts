// @ts-nocheck: CommonJS fixture shapes are validated by tsc2c AOT, not TypeScript JS inference.
import greet, { count, double, label } from "tsc2c-cjs-module-alias-require-bind-alias";
import * as bound from "tsc2c-cjs-module-alias-require-bind-alias";

const pkg = require("tsc2c-cjs-module-alias-require-bind-alias");

console.log(greet("Ada"), label, count, double(6));
console.log(bound.default("Lin"), bound.label, bound.count, bound.double(7));
console.log(pkg.default("Grace"), pkg.label, pkg.count, pkg.double(8));
