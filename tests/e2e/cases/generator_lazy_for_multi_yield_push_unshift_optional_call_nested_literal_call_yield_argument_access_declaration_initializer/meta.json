{
  "description": "lazy generators stage direct yielded arguments of ordinary or optional calls nested in dense array and static-key object literal optional-call arguments in push and unshift before mutating computed element access"
}
