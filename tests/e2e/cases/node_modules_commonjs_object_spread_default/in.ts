const pkg = require("tsc2c-cjs-object-spread-default-package");

console.log(
    Object.keys(pkg).join("|"),
    pkg.label,
    pkg.count,
    pkg.extra,
    Object.keys(pkg.nested).join("|"),
    pkg.nested.ready,
);
