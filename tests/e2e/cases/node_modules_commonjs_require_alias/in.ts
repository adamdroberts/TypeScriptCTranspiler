// @ts-nocheck: CommonJS fixture shapes are validated by tsc2c AOT, not TypeScript JS inference.
// @ts-ignore: TypeScript does not infer exports assigned through require aliases.
import greet, { count, double, label } from "tsc2c-cjs-require-alias";
import * as aliased from "tsc2c-cjs-require-alias";

const pkg = require("tsc2c-cjs-require-alias");

console.log(greet("Ada"), label, count, double(6));
// @ts-ignore: TypeScript does not infer namespace members assigned through require aliases.
console.log(aliased.default("Lin"), aliased.label, aliased.count, aliased.double(7));
console.log(pkg.default("Grace"), pkg.label, pkg.count, pkg.double(8));
console.log(
    require("tsc2c-cjs-require-alias").default("Ken"),
    require("tsc2c-cjs-require-alias").label,
    require("tsc2c-cjs-require-alias").count,
    require("tsc2c-cjs-require-alias").double(9),
);
