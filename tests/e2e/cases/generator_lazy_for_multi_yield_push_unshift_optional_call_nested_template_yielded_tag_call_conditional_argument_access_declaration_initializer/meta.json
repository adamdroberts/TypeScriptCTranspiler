{
  "description": "lazy generators stage yielded conditional tag-call arguments before nested tagged-template substitutions inside optional push and unshift calls before mutating computed element access"
}
