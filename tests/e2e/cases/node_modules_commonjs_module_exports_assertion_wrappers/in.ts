const {
    default: assertedDefault,
    label,
    count,
    double,
} = require("tsc2c-cjs-module-asserted-object");
const {
    default: factoryDefault,
    label: factoryLabel,
    count: factoryCount,
    triple,
} = require("tsc2c-cjs-module-asserted-factory-object");

const assertedPkg = require("tsc2c-cjs-module-asserted-object");
const factoryPkg = require("tsc2c-cjs-module-asserted-factory-object");

console.log("asserted:", assertedDefault, label, count, double(6), assertedPkg.label, assertedPkg.default);
console.log("factory:", factoryDefault, factoryLabel, factoryCount, triple(5), factoryPkg.label, factoryPkg.default);
