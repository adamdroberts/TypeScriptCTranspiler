import { count, double, enabled, extra, label } from "tsc2c-cjs-module-object-spread-named";

const pkg = require("tsc2c-cjs-module-object-spread-named");

console.log(label, count, enabled, extra, double(7));
console.log(pkg.label, pkg.count, pkg.enabled, pkg.extra, pkg.double(8));
