// @ts-nocheck: CommonJS fixture shapes are validated by tsc2c AOT, not TypeScript package inference.

const pkg = require("tsc2c-cjs-require-assertion-wrappers");
const total = pkg.count + pkg.boundCount + pkg.calledCount + pkg.appliedCount + pkg.reflectedCount + pkg.nonNullCount;

console.log(pkg.default("Grace"), pkg.label, total, pkg.double(8));
