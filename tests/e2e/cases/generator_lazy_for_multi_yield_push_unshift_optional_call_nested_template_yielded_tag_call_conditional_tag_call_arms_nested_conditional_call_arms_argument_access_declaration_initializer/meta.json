{
  "description": "lazy generators stage optional calls in both branches of nested conditionals inside both conditional dynamic tag-call arms before outer yielded tag-call substitutions inside optional push and unshift calls before mutating computed element access"
}
