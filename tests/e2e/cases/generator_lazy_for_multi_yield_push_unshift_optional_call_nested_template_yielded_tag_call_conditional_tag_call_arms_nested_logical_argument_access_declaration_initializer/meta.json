{
  "description": "lazy generators stage nested short-circuit logical/tag-call argument paths in both conditional dynamic tag-call arms before outer yielded tag-call substitutions inside optional push and unshift calls before mutating computed element access"
}
