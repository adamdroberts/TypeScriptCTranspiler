import details, { add, entries, label, metadata } from "tsc2c-js-package";
import * as jsns from "tsc2c-js-package";

const value: any = add(2, 5);
const fromEntries: any = Object.fromEntries(entries as any);

console.log(
    label,
    value,
    Object.keys(metadata).join("|"),
    metadata.label,
    metadata.values.length,
    fromEntries.answer,
    details.label,
    details.values.length,
    jsns.default.label,
);
