{
  "description": "lazy generators stage both nested conditional logical/tag-call branches in both conditional dynamic tag-call arms before outer yielded tag-call substitutions inside optional push and unshift calls before mutating computed element access"
}
