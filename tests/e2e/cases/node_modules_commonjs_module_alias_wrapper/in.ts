// @ts-nocheck: CommonJS fixture shapes are validated by tsc2c AOT, not TypeScript JS inference.
// @ts-ignore: TypeScript does not infer exports assigned through an aliased CommonJS module wrapper.
import { add, count, filenameMatches, label, loaded, pathMatches, pathsLength } from "tsc2c-cjs-module-alias-wrapper-package";

const pkg = require("tsc2c-cjs-module-alias-wrapper-package");

console.log("imports:", label, count, add(20, 22), filenameMatches, pathMatches, loaded, pathsLength);
console.log("require:", pkg.label, pkg.count, pkg.add(10, 5), pkg.filenameMatches, pkg.pathMatches, pkg.loaded, pkg.pathsLength);
