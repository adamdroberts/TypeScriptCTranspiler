const pkg = require("tsc2c-cjs-computed-object-wrapper-enum-exports");
const {
    label,
    double,
    entryLabel,
    nameLabel,
} = require("tsc2c-cjs-computed-object-wrapper-enum-exports");

console.log(pkg.label, pkg.entryLabel, pkg.nameLabel);
console.log(label, double(6), entryLabel, nameLabel);
