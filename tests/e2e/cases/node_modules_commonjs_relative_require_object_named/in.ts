import { count, double, label } from "tsc2c-cjs-relative-require-object";

const pkg = require("tsc2c-cjs-relative-require-object");

console.log("imports:", label, count, double(8));
console.log("require:", pkg.label, pkg.count, pkg.double(9));
