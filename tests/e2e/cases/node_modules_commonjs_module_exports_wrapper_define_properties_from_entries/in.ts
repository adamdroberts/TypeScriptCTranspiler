// @ts-nocheck: CommonJS fixture shapes are validated by tsc2c AOT, not TypeScript JS inference.
import greet, { count, double, extra, label } from "tsc2c-cjs-object-wrapper-define-properties-from-entries";
import * as wrapped from "tsc2c-cjs-object-wrapper-define-properties-from-entries";
import mapGreet, { count as mapCount, double as mapDouble, extra as mapExtra, label as mapLabel } from "tsc2c-cjs-object-wrapper-define-properties-from-entries-map";

const pkg = require("tsc2c-cjs-object-wrapper-define-properties-from-entries");
const mapPkg = require("tsc2c-cjs-object-wrapper-define-properties-from-entries-map");

console.log(greet("Ada"), label, count, double(6), extra);
console.log(wrapped.default("Lin"), wrapped.label, wrapped.count, wrapped.double(7), wrapped.extra);
console.log(pkg.default("Grace"), pkg.label, pkg.count, pkg.double(8), pkg.extra);
console.log(mapGreet("Ada"), mapLabel, mapCount, mapDouble(6), mapExtra);
console.log(mapPkg.default("Lin"), mapPkg.label, mapPkg.count, mapPkg.double(7), mapPkg.extra);
