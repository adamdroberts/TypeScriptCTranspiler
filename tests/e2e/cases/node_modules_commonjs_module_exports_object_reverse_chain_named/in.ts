import { count, enabled, greet, label, quadruple } from "tsc2c-cjs-module-object-reverse-chain";

const pkg = require("tsc2c-cjs-module-object-reverse-chain");

console.log("imports:", label, count, quadruple(3), greet("Ada"), enabled);
console.log("require:", pkg.label, pkg.count, pkg.quadruple(4), pkg.greet("Lin"), pkg.enabled);
