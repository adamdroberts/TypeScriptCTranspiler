// @ts-ignore: TypeScript does not infer exports from Object.defineProperties(..., Object.getOwnPropertyDescriptors(...)).
import greet, { count, double, label } from "tsc2c-cjs-define-properties-own-descriptors";

const moduleDescriptors = require("tsc2c-cjs-module-define-properties-own-descriptors");

console.log(greet("Ada"), label, count, double(2));
console.log(
    moduleDescriptors.default,
    moduleDescriptors.label,
    moduleDescriptors.count,
    moduleDescriptors.enabled,
);
