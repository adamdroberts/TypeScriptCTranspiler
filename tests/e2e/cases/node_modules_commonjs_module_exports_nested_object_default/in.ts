const pkg = require("tsc2c-cjs-nested-object-default-package");

console.log(
    Object.keys(pkg).join("|"),
    Object.keys(pkg.meta).join("|"),
    pkg.meta.label,
    pkg.meta.flags[1],
    pkg.values[2],
);
