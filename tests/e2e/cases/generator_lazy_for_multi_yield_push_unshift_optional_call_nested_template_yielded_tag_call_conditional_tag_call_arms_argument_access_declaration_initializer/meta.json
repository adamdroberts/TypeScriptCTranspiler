{
  "description": "lazy generators stage both dynamic tag-call arms of conditional yielded tag-call arguments before outer tagged-template substitutions inside optional push and unshift calls before mutating computed element access"
}
