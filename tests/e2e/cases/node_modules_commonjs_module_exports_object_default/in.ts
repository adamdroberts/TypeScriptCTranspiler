import imported from "tsc2c-cjs-object-default-package";
import * as ns from "tsc2c-cjs-object-default-package";

const pkg = require("tsc2c-cjs-object-default-package");

console.log(
    Object.keys(pkg).join("|"),
    pkg["label"],
    require("tsc2c-cjs-object-default-package")["count"],
    imported.label,
    imported.count,
    ns.label,
    ns.count,
);
