// @ts-nocheck: CommonJS fixture shapes are validated by tsc2c AOT, not TypeScript JS inference.
import {
    filenameOk,
    firstPathOk,
    idOk,
    loadedValue,
    pathOk,
    pathsLength,
    read,
} from "tsc2c-cjs-module-metadata-destructure";

const pkg = require("tsc2c-cjs-module-metadata-destructure");

console.log("imports:", filenameOk, idOk, pathOk, loadedValue, pathsLength, firstPathOk, read());
console.log("require:", pkg.filenameOk, pkg.idOk, pkg.pathOk, pkg.loadedValue, pkg.pathsLength, pkg.firstPathOk, pkg.read());
