// @ts-nocheck: CommonJS fixture shapes are validated by tsc2c AOT, not TypeScript JS inference.
import {
    childrenLength,
    filenameOk,
    firstPathOk,
    idOk,
    loadedValue,
    parentIsNull,
    pathOk,
    pathsLength,
    read,
} from "tsc2c-cjs-module-metadata-destructure";

const pkg = require("tsc2c-cjs-module-metadata-destructure");

console.log("imports:", filenameOk, idOk, pathOk, loadedValue, pathsLength, firstPathOk, parentIsNull, childrenLength, read());
console.log("require:", pkg.filenameOk, pkg.idOk, pkg.pathOk, pkg.loadedValue, pkg.pathsLength, pkg.firstPathOk, pkg.parentIsNull, pkg.childrenLength, pkg.read());
