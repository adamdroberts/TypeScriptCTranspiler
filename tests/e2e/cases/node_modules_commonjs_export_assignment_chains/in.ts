// @ts-ignore: TypeScript does not infer exports from chained CommonJS assignments.
import greet, { alias, count, label } from "tsc2c-cjs-export-assignment-chains";
import * as chained from "tsc2c-cjs-export-assignment-chains";

const pkg = require("tsc2c-cjs-export-assignment-chains");

console.log(greet("Ada"), label, count, alias("Lin"));
// @ts-ignore: TypeScript does not infer namespace members from chained CommonJS assignments.
console.log(chained.default("Grace"), chained.label, chained.count, chained.alias("Alan"));
console.log(pkg.default("Katherine"), pkg.label, pkg.count, pkg.alias("Edsger"));
