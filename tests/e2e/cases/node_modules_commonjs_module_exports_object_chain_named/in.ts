import { count, enabled, greet, label, triple } from "tsc2c-cjs-module-object-chain";

const pkg = require("tsc2c-cjs-module-object-chain");

console.log("imports:", label, count, triple(4), greet("Ada"), enabled);
console.log("require:", pkg.label, pkg.count, pkg.triple(5), pkg.greet("Lin"), pkg.enabled);
