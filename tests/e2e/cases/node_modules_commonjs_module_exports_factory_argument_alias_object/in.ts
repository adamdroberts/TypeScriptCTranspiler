// @ts-nocheck: CommonJS fixture shapes are validated by tsc2c AOT, not TypeScript JS inference.
import factoryDefault, { count, double, label } from "tsc2c-cjs-module-factory-argument-alias-object";
import * as factoryNs from "tsc2c-cjs-module-factory-argument-alias-object";

const factoryPkg = require("tsc2c-cjs-module-factory-argument-alias-object");

console.log(
    factoryDefault,
    label,
    count,
    double(7),
    factoryNs.default,
    factoryNs.label,
    factoryPkg.default,
    factoryPkg.count,
);
