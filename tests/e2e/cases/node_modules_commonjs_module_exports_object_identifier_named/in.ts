import { count, double, enabled, greet, label } from "tsc2c-cjs-module-object-identifier";

const pkg = require("tsc2c-cjs-module-object-identifier");

console.log("imports:", greet("Ada"), label, count, double(5), enabled);
console.log("require:", pkg.default, pkg.greet("Lin"), pkg.label, pkg.count, pkg.double(6), pkg.enabled);
