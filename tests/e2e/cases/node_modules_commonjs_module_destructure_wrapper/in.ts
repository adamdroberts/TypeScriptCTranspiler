// @ts-nocheck: CommonJS fixture shapes are validated by tsc2c AOT, not TypeScript JS inference.
import { add, count, label, read } from "tsc2c-cjs-module-destructure-wrapper";

const pkg = require("tsc2c-cjs-module-destructure-wrapper");

console.log("imports:", label, count, add(20, 22), read());
console.log("require:", pkg.label, pkg.count, pkg.add(10, 5), pkg.read());
