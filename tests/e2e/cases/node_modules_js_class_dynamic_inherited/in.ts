import { Child, label } from "tsc2c-js-class-extends";

const child: any = new Child(5, 2);

console.log(label, child.next(4), child.total(4), child.value, child.bonus);
child.value = 10;
child.bonus = 3;
console.log(child.next(1), child.total(1));
