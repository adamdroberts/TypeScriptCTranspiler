// @ts-nocheck: CommonJS fixture shapes are validated by tsc2c AOT, not TypeScript JS inference.
import greet, { count, double, label } from "tsc2c-cjs-factory-wrapper-require-argument";
import * as wrapped from "tsc2c-cjs-factory-wrapper-require-argument";

const pkg = require("tsc2c-cjs-factory-wrapper-require-argument");

console.log(greet("Ada"), label, count, double(4));
console.log(wrapped.default("Lin"), wrapped.label, wrapped.count, wrapped.double(5));
console.log(pkg.default("Grace"), pkg.label, pkg.count, pkg.double(6));
