const first = Date.now();
const second = Date.now();

console.log("first:", typeof first, Number.isFinite(first), first > 0);
console.log("second:", typeof second, Number.isFinite(second), second > 0);
