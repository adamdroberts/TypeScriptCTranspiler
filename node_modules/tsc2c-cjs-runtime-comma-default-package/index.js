let commaCount = 0;

function commaMark() {
  commaCount++;
  return "ignored";
}

function commaMake() {
  return { label: "comma-default", count: commaCount, nested: { ok: true } };
}

module.exports = (commaMark(), commaMake());
