function format(strings, label, count) {
  return {
    label: strings[0] + label + strings[1],
    count,
    raw: String.raw`raw\n${label}\t`
  };
}

module.exports = format`tag-${"default"}-${7}`;
