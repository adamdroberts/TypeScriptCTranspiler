export class Counter {
    constructor(start) {
        this.value = start;
    }

    inc(delta) {
        this.value = this.value + delta;
        return this.value;
    }
}

export const label = "js-named-class";
